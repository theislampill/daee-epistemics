---
id: M7-definition-anchor
module_class: tactic
canonical_path: skill/references/tactics/M7-definition-anchor.md
contract_version: "0.4.0.0"
load_when:
  - interlocutor challenges a word rather than engaging the underlying claim
  - terminological dispute used to avoid substantive engagement
blocks:
  - broader conception-capture problems (silent redefinition, public-language capture, universals/particulars confusion) — route to definition-discipline instead
companions:
  - definition-discipline
  - M6-excluded-middle
output_shapes:
  - bounded-single-pass
layer_constraint: layer-b-permitted
catalogue_registered: true
verification_status: L_check
direct_read_verified: true
failure_conditions_present: true
ir_consequences_present: true
minimal_pairs_present: true
hold_release_rules_present: true
compiled_runtime_eligible: true
operator_pack_eligible: true
---

# M7 - The Definition Anchor

## Runtime operator contract

- Activation: interlocutor challenges a word rather than engaging the underlying claim.
- Field target: the live burden or submove pressure that made tactic `M7-definition-anchor` eligible; activation cue: interlocutor challenges a word rather than engaging the underlying claim.
- Burden/submove form: tactic `ⁿBᵢ[OPᵢ]`: target -> operation -> result; the result contributes to `Land(ⁿB)` only when live for the current burden.
- Δ effect: `ΔⁿB` is the local target/operation/result transition; `Δκ` changes only if the operation affects closure, dependency radius, or held routes.
- Possible ∇ reread: after the tactic lands, check target-explicit `∇·B` for remaining burden pressure or `∇×ξ`/`∇×κ` for circular criterion/dependency pressure when relevant.
- R(H,Δ) obligation: after this owner acts, reread H, remaining burdens, alternate routes, register pressure, dependency loops, and closure state before STOP/RECURSE/PARTIAL.
- Hold/release/closure effect: release only the bounded result that has landed; hold, integrate, discharge as derivative, or carry forward unresolved pressure with reason.
- Output boundary: `layer-b-permitted` with output shapes `bounded-single-pass`. Default render may show compact state markers when control-bound; long formalism stays audit/formalism-expanded.
- Negative constraints: no argument-bank drift, no scalar closure, no deterministic route freezing, no indiscriminate TTP spraying, no ∇ as Δ replacement, no proof-by-symbol, and no Shannon/reconstruction-branding/∇ truth-or-warrant claim.
- Fixture/checker: Catalogue/frontmatter integrity is guarded by `tools/check_ttp_operator_contracts.py`; direct structural routing coverage remains a remediation item.


**Type:** Meta-tactic
**Deploy when:** The interlocutor challenges a word rather than engaging the underlying claim; a terminological dispute is being used to avoid substantive engagement.

Common forms: "religion is too wide a concept," "that is not what guidance means," "you are using 'reason' differently than I do."

This is either genuine need for clarification or a deflection tactic. Either way the procedure is the same.

## Four-step procedure

1. Acknowledge that terminology can be contested.
2. Offer a precise working definition for the current discussion.
3. Invite agreement or counter-proposal.
4. Proceed with the agreed or stipulated definition, noting explicitly that the argument still stands under it.

The key move is to show that the argument's force does not depend on the particular word chosen.

## Scope boundary

M7 is not the owner of broader conception-capture problems. When contradiction pressure is being generated by silent redefinition, public-language capture, universals/particulars confusion, or mental vs. extra-mental confusion, route to `references/diagnostics/definition-discipline.md`. M7 handles the local lexical dispute; the diagnostic file handles the wider definition-governance burden.
