/daee-epistemics release-smoke witness capture mode

Smoke case: CR-03 ordinary bounded governed answer.

Render the normal governed answer from the packaged runtime, and because this is release-smoke witness capture mode, include the literal checker-readable witness surfaces required by the package:
- noetic-field execution banner;
- literal `## Burden-Cycle 1`;
- compact Layer A with `initial burden set: [B1, ...]` and a non-empty `gate/release decision: ∇ route: ...`;
- burden-local Target/Operation/Result submoves;
- `Land(Bn)`, `Field diagnostics:`, `LoopBreak:`, and `R(H,Delta):` for every released burden;
- every licensed `LoopBreak:` line must include the literal fields `target`, `ground/source`,
  `Delta effect`, and `reread`; if null, write `LoopBreak: not needed`;
- every `R(H,Delta):` line must name what was reread, what is held, what remains live or cleared, and the next STOP/HOLD/PARTIAL/RECURSE/COMPLETE status;
- `State/noetic re-read` with `What changed / cumulative-state delta:` and a prose `Release status:`;
- `## Closure/Reconstruction Witness` with `Initial burden set`, `Terminal states`, `Burden dependency graph:`, `∇·B:`, `∇×κ:`, `𝒞(Ψᴺ):`, and `T_lang: Ψᴺ ⇢ Ψᴵ:`;
- render `Terminal states:` as one parseable row per burden: `B1: cleared / <operator> / <target -> operation -> result or compact delta>`; do not write `B1 <title>: cleared`;
- render `∇·B:` as `neutral / ...` or `non-neutral / ...`, and render `∇×κ:` as `null / ...`, `resolved / ...`, or `non-null / ...`;
- render `𝒞(Ψᴺ):` with the literal phrase `agent/runtime execution field`;
- `## Restorative Response` and `## Closing Formulation`;
- non-claim boundary: not a truth meter, not guaranteed uptake, not soul/interlocutor rewrite, and not proof beyond this local package-bound smoke.

User prompt:
If God is perfectly merciful, why would He hold people accountable when sincere people disagree and some never receive a clear sign?
