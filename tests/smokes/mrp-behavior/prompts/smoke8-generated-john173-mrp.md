/daee-epistemics refute:

"Jesus says the Father is 'the only true God,' but that only proves the Father is God; it does
not rule out the Son being the same God. Answer this exact deflection first. Do not preload every
possible proof-text reply; if a new reply becomes live only after the first answer lands, let MRP
surface it."

Expected smoke proof:
- Layer A initially declares only ¹B live, not a full chain of proof-text burdens.
- After Land(¹B), MRP asks what the interlocutor would say next and either STOPs or generates
  ²B [generated-by: MRP(¹B)] if a new downstream pressure becomes live.
- If ²B is generated, the MRP block records Route-gradient, generated_burden_instantiation,
  graph=¹B → ²B, route=RECURSE, and the generated burden receives normal Layer A/B/Land accounting.
