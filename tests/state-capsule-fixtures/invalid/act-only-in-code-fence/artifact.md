# Case

Discussion of the ACT row format for reviewers:

```
ACT B1_1 :: M3.activation :: register_axis=xi :: Delta=uptake-recorded :: Land(B1)+
```

No real ACT row appears outside the fence above; the token is quoted as an
example only, not asserted as having happened.
