NOETIC FIELD EXECUTION
field: MIXED NOETIC FIELD
user task: REFUTE
external source request: EXPLICIT
authority frame: LIVE
state: RECURSE

## Layer A - Compact DSL/IR Header
- read status: one loaded assumption is carrying several downstream claims, so clearing the node must not become a tour through every downstream topic.
- claim_level: pattern-profile / dependency-radius pressure
- pattern_profile: MM-8 collapse-radius mapping; downstream claims are not answered or left live silently.
- reason-category: 2 / loaded node controls downstream release
- Concealment mode: mixed - dominant iʿrāḍ + juḥūd pressure on the diagnostic target; collapse-radius mapping is routed through pattern profiling. basis: visible discourse pattern; held, not certified. Boundary: diagnostic noetic covering only; no hidden soul-state or takfīr judgment.
- collapse-radius map: the loaded node and dependency radius are typed; immediate routes and downstream routes depend on the node.
- DO-orient: map collapse radius by naming immediate and downstream routes that depend on the node and bind reread to that downstream dependency set.
- restoration target: reread dependency radius before answering downstream topics.

Layer B maps the radius: the loaded node is the imported criterion, immediate routes depend on exposing that node, downstream routes remain held unless the dependency map and reread permit release. Reread all dependent claims/routes and decide STOP, HOLD, PARTIAL, RECURSE, or NewB before any downstream topic answers release.
