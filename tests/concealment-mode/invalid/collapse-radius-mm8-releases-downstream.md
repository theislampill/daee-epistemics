NOETIC FIELD EXECUTION
field: MIXED NOETIC FIELD
user task: REFUTE
external source request: EXPLICIT
authority frame: LIVE
state: RECURSE

## Layer A - Compact DSL/IR Header
- read status: one loaded assumption is carrying several downstream claims, so clearing the node must not become a tour through every downstream topic.
- claim_level: pattern-profile / dependency-radius pressure
- pattern_profile: MM-8 collapse-radius mapping.
- reason-category: 2 / loaded node controls downstream release
- Concealment mode: mixed - dominant iʿrāḍ + juḥūd pressure on the diagnostic target. Boundary: diagnostic noetic covering only; no hidden soul-state or takfīr judgment.
- collapse-radius map: the loaded node and dependency radius are typed; immediate routes and downstream routes depend on the node.
- DO-orient: map collapse radius by naming immediate and downstream routes that depend on the node and bind reread to that downstream dependency set.
- restoration target: answer every downstream topic; downstream claims may proceed before reread.

Layer B maps the radius but releases downstream topic answers without deciding STOP, HOLD, PARTIAL, RECURSE, or NewB.
