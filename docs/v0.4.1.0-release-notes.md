# v0.4.1.0 Release Notes

v0.4.1.0 is a cleanup and hardening release on the `0.4.0.0` operative
contract line. It does not introduce a hard schema/register migration, and it
untracks generated `skill/**` runtime output from source control.

## What Changed

- Consolidated docs, audit, and release-history surfaces so current guidance is
  smaller and historical evidence remains preserved in history files.
- Deprecated active Level3 wording. Legacy route/check harness filenames remain
  compatibility details for maintainer workflows, not public runtime identity.
- Deprecated active branch-era pipeline labels in favor of the schema-light
  register-formalism bridge and compact DSL/IR runtime spine.
- Added final-gate audits for algebraic notation operativity, divergence/curl
  diagnostic boundaries, root SKILL/DSL/IR/NLA compliance, TTP/module
  operativity, and CI/release workflow behavior.
- Added a manual GitHub Actions artifact workflow that builds from atomics into
  the consolidated runtime package and uploads workflow artifacts only.
- Corrected source/runtime tracking: `atomics/skill/**` remains tracked source,
  while generated `skill/**` is rebuilt locally/CI and ignored.
- Prepared a corrected local replacement candidate after the generated runtime
  untracking correction. The currently published v0.4.1.0 asset/provenance is
  superseded pending explicit replacement authorization; it is not corrected
  merely by this source/doc patch.

## What Is Not Claimed

- This source commit does not itself publish a GitHub Release; release
  publication is the later tag/release step.
- No hard register fields are added to Diagnostic IR.
- Ignored local smokes and retained `.daee` evidence remain local evidence, not
  package/release proof.
- Divergence/curl language is audit/formalism visibility with control
  implications, not default decorative runtime notation.
- NLA means Natural Language Autoencoder reconstruction-fidelity discipline:
  Layer A / IR acts as verbalizer and reconstruction/reread must recover the
  operative noetic state. It is not generic linear algebra or Shannon theory.

## Package

The package is generated from tracked `atomics/skill/**` through ignored
local/CI `skill/**` runtime into the consolidated skill artifact. Raw atomics
are not distributed as the `.skill` asset.

- Intended corrected replacement asset: `daee-epistemics-v0.4.1.0.skill`
- SHA256: `184D370534DE9E6FFC10C736CDD5C96C15D28761C5ED86156228930F95B4C59E`
- Size: `557920` bytes
- Entries: `20`
- Archive root: `README.md`, `SKILL.md`, `build-manifest.json`,
  `compiled-module-map.json`, `references`

Publication status: local corrected replacement candidate only until the
GitHub Release asset and provenance are explicitly replaced.
