# v0.4.0.0 - Release Log

Release title:
`v0.4.0.0 - Register Grammar, Release-Boundary and Meta-Noetic Operator Hardening, Evidence-Gated Operator Runtime`

Subtitle:
`Summary: Evidence-gated noetic operator architecture over the compact DSL runtime.`

## Scope

v0.4.0.0 is the release-line promotion for the Stage 1.x through Stage 8.x
operator and register hardening campaign:

- evidence-gated child-mode pattern across M9, PM, AS, DW, DA/DS/HK, OQ, and MM;
- NS/register grammar hardening without hard schema migration;
- Architecture tab repair and source/runtime boundary cleanup;
- AGENTS child-mode evidence protocol;
- retained local dev-checker evidence for `.daee` child-mode samples;
- NLA-inspired diagnostic explanation faithfulness recorded as a release gate.

## Package Artifact

- Package zip path: `build/daee-epistemics-v0.4.0.0.skill.zip`
- Release asset path: `build/daee-epistemics-v0.4.0.0.skill`
- SHA256: `78C2BECE8818DEFF10C8CEF1A1578D9B54D0929950460681D04DC4F885576D0C`
- Size: `554776` bytes
- Entry count: `20`
- Top-level entries: `README.md`, `SKILL.md`, `build-manifest.json`,
  `compiled-module-map.json`, `references/`

## Current-Release Smoke Evidence

Current-release package-bound smokes are retained locally under:

```text
.daee/v0.4.0.0-current-release-smokes/
```

Smoke families:

- hard moral-protest / source-worldview canary;
- predication / attribute canary;
- naturalist / transmission canary;
- child-mode local evidence boundary canary.

Validation command:

```powershell
python tools/check_smoke_artifacts.py --root .daee/v0.4.0.0-current-release-smokes --require-current-release-smokes
```

Result: PASS.

## Verification Summary

The release gate includes:

- frontmatter contract check for `0.4.0.0`;
- generated runtime freshness;
- package shape and forbidden-entry checks;
- routing parity and strict parity;
- recursive traversal governance;
- Pipeline #2 bridge;
- Diagnostic IR instance and catalogue integrity;
- smoke artifact validation, including current-release smokes;
- encoding hygiene;
- local child-mode positive/negative checker suite;
- CI status before tag/release asset publication.

## NLA Faithfulness Gate

The release preserves diagnostic explanation faithfulness:

```text
diagnostic/noetic state -> natural-language explanation -> reconstructed diagnostic/noetic state
```

This gate is implemented through IR reconstruction discipline, compact Layer A /
state-reread requirements, child-mode checker contracts, local smoke evidence, and
trace/verdict provenance. It is not an activation-level interpretability claim.

## Caveats

- Static fixtures are not live execution proof.
- Retained `.daee` samples are local ignored evidence only.
- Dev-local checker PASS is not universal semantic grading.
- Current-release package smoke proof applies only to the v0.4.0.0 package SHA
  recorded above.
- The release does not claim all-host parity or deterministic transformer execution.
