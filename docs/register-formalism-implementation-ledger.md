# schema-light register bridge Implementation Ledger

This ledger adjudicates the schema-light register bridge repo plan recovered from `docs/index.html`.
`PROVEN IMPLEMENTED` means the item is present in canonical atomics/generated runtime, has
dedicated static fixture/checker evidence in `tests/register-formalism-bridge-fixtures/` plus
`tools/check_register_formalism_bridge.py`, and is backed by the installed-skill live smoke proof summarized
in `docs/audits/audit-history-pre-v0.4.1.md`. It does not expand the Diagnostic IR JSON schema with mandatory `heart` /
`xi` / `Omega` / `mu` / `kappa` fields. Hard schema migration remains a separate authorization
decision from the v0.4.0.0 package/release line.

Baseline wording rule: The schema-light register bridge is the current baseline register formalism. Row labels such as
`derived bridge` describe the schema-light control boundary only; they do not mean future parity,
compact DSL/IR runtime spine, or optional runtime theory.

| Register-formalism item | Source files touched | Runtime effect | Checker/fixture effect | Smoke implication | Status | Evidence |
|---|---|---|---|---|---|---|
| `𝓝` noetic-structure selection space | `nomenclature-normalization.md`; `diagnostic-ir.md`; `noetic-reading-checklist.md`; formalism spec; index; bridge fixtures | Governs design/read domain for possible noetic structures before a live frame is selected or held. | `check_register_formalism_bridge.py` requires bridge anchors and fixture coverage for `N_space`. | Hard smokes must not family-lock from thin input. | PROVEN IMPLEMENTED | Fixture `02-xi-warrant-owner-selection` proves the selection space remains open until warrant grammar distinguishes the operative frame. |
| `D₀` surface discourse / signal | `nomenclature-normalization.md`; `diagnostic-ir.md`; `noetic-reading-checklist.md`; formalism spec; index; bridge fixtures | Names surface discourse before diagnostic reduction. | Bridge checker requires `D0 -> PsiN` chain and D0 fixture coverage. | Smoke input remains source-local; no detached source-stack compression. | PROVEN IMPLEMENTED | Fixtures `01` through `05` project input as D0 before control effects are selected. |
| `Ψᴺ` encoded noetic signal-state | `nomenclature-normalization.md`; `diagnostic-ir.md`; `noetic-reading-checklist.md`; formalism spec; index; bridge fixtures | Names encoded noetic signal-state over current IR/case-state. | Bridge checker requires `PsiN` anchors and fixture coverage. | Register stress should preserve encoded-state reread rather than thin labels. | PROVEN IMPLEMENTED | Fixtures encode grief, warrant, predication, carrier, and collapse-radius cases as PsiN before owner/reread decisions. |
| `N∈𝓝` runtime-selected noetic frame | `nomenclature-normalization.md`; `diagnostic-ir.md`; `noetic-reading-checklist.md`; formalism spec; index; bridge fixtures | Makes selected `N` a runtime/held choice, not assumed by label. | Bridge checker requires selected-frame fixture coverage and no schema field. | Hard smokes must preserve source-status/noetic-frame non-equivalence. | PROVEN IMPLEMENTED | Fixtures `01` through `04` prove selected/held frame changes burden or owner choice rather than following a topic label. |
| `IR(N,m,τ,σ,♥,ξ,Ω,μ,κ)` derived bridge | `diagnostic-ir.md`; `nomenclature-normalization.md`; `framework-pipeline.yaml`; output/render docs; formalism spec; index; bridge fixtures | Registers are conditionally live analytic functions over existing IR; they affect owner eligibility, hold/release, collapse radius, reread, PARTIAL, or restoration when live. | Bridge checker enforces derived bridge, fixture behavior coverage, and absence of hard schema fields. | Installed-skill live hard smokes exercise the behavior; package proof belongs to release provenance, not this ledger. | PROVEN IMPLEMENTED | Fixtures `01` through `08` map the expanded bridge to existing IR projections without hard schema fields. |
| `♥` affective-discursive register / release-posture control | `diagnostic-ir.md`; `noetic-reading-checklist.md`; `output-release.md`; render contract; formalism spec; index; bridge fixtures | Governs hold, sequence, tone, directness, softness, closure posture when live. | Bridge checker requires `heart` fixture coverage with owner-choice and hold/release effects. | Hard moral-protest smoke should preserve grief/identity/performance holds where live. | PROVEN IMPLEMENTED | Fixture `01-heart-register-hold` routes through `M4-grief-register`/`P7-restoration-stops`, holds theodicy detail, and returns HOLD. |
| `ξ` epistemic/warrant grammar | `diagnostic-ir.md`; `noetic-reading-checklist.md`; `recursive-state-transitions.md`; formalism spec; index; bridge fixtures | Tracks warrant/testimony/proof-method/authority/defeater grammar when it changes routing or reread. | Bridge checker requires `xi` fixture coverage with owner-choice and burden-selection effects. | Tawatur/scientism smoke should still show correct first-/higher-order warrant handling. | PROVEN IMPLEMENTED | Fixture `02-xi-warrant-owner-selection` selects `reason-disambiguation`, FPD, V2, and `proof-method-audit` before evidence release. |
| `Ω` ontological/predication grammar | `diagnostic-ir.md`; `noetic-reading-checklist.md`; `recursive-state-transitions.md`; formalism spec; index; bridge fixtures | Tracks predication, modality, dependence, causality, and creator/creation grammar when live. | Bridge checker requires `Omega` fixture coverage with owner-choice and burden-selection effects. | Predication smoke should still show M9/V8 owner activation where live. | PROVEN IMPLEMENTED | Fixture `03-omega-predication-owner-selection` selects M9, attribute precision, perfection-criterion audit, and V8 before attribute content release. |
| `μ` meta-noetic memetic carrier/stabilizer | `diagnostic-ir.md`; `noetic-reading-checklist.md`; `output-release.md`; formalism spec; index; bridge fixtures | Tracks carrier, compression, stabilizer, defensive move, reproduction, or mutation pattern when it changes existing controls. | Bridge checker requires `mu` fixture coverage with burden-selection and hold/release effects. | Naturalist/scientistic smoke should not turn `μ` into rhetoric. | PROVEN IMPLEMENTED | Fixture `04-mu-carrier-burden-selection` makes the self-sealing carrier the bounded burden and holds evidence dump. |
| `κ` collapse radius / downstream dependency set | `diagnostic-ir.md`; `recursive-state-transitions.md`; `output-release.md`; formalism spec; index; bridge fixtures | Tracks downstream claims/routes to reread after a load-bearing node changes. | Bridge checker requires `kappa` fixture coverage with reread effect and schema guard. | Hard canary must preserve visible closure audit and no premature closure. | PROVEN IMPLEMENTED | Fixture `05-kappa-deltakappa-reread-partial` maps the downstream dependency set into `collapse_radius` and post-render reread. |
| `∇` route-gradient pressure | `recursive-state-transitions.md`; `diagnostic-ir.md`; output release; render contract; formalism spec; index; field-operator checker | Reads eligible route pressure over the live noetic/burden/dependency/register/route field without bypassing IR, V1/routing precedence, catalogue, or held-material gates. | `check_field_operator_architecture.py` requires route-gradient definition, gate constraint, negative controls, docs/index concepts, and fixture coverage. | Hard cases may explain release order without freezing the whole field into one deterministic route. | PROVEN IMPLEMENTED | Fixture `10-field-gradient-loop-closure-coupling` distinguishes `∇` from `Δ`, `∇·`, and `∇×`. |
| `Δκ` reread input after burden landing | `diagnostic-ir.md`; `recursive-state-transitions.md`; `output-release.md`; `framework-pipeline.yaml`; formalism spec; index; bridge fixtures | Forces dependency-radius delta into `R(H,Delta)` before STOP/HOLD/PARTIAL/RECURSE. | Bridge checker requires `Delta-kappa` fixture coverage with reread and PARTIAL evidence. | Register stress should show dependency-radius change affects closure. | PROVEN IMPLEMENTED | Fixture `05` returns PARTIAL because `Delta-kappa` leaves hiddenness/worship routes newly eligible after reread. |
| `ⁿB`, `ⁿBᵢ`, `ΔⁿB`, `ⁿ⁺¹B` | `recursive-state-transitions.md`; render contract; output release; formalism spec; index; bridge fixtures | Separates burden-cycle, submove, local landed delta, and next burden license. | Existing recursion/render checks plus bridge fixtures guard delta/next-burden distinction. | Hard smokes must preserve hard-case depth floor and same-burden discipline. | PROVEN IMPLEMENTED | Fixture `05` covers burden notation and prevents dependent submoves from being split into fake new burdens. |
| `ⁿBᵢ[OP] : target -> operation -> result -> ΔⁿB{♥,ξ,Ω,σ,μ} / Δκ` | `recursive-state-transitions.md`; render contract; output release; framework pipeline; formalism spec; index; bridge fixtures | Makes each operative submove a local owner-backed state change, not a route label. | Bridge checker requires operator-signature fixture coverage; render checks require target/operation/result shape generally. | Hard canary must show owner/TTP/source matrix pass and no route harness contamination. | PROVEN IMPLEMENTED | Fixture `05` covers operator signature; existing render/recursive checkers enforce the general target/operation/result rule. |
| `R(H,ΔⁿB{♥,ξ,Ω,σ,μ},Δκ)` | `recursive-state-transitions.md`; output release; framework pipeline; formalism spec; index; bridge fixtures | Expands reread to consume held material, register deltas, and dependency radius before decision. | Bridge checker requires expanded-R fixture coverage and all STOP/HOLD/RECURSE/PARTIAL decisions. | Hard smokes must preserve visible closure audit and P1/P7 closure requirement. | PROVEN IMPLEMENTED | Fixtures cover HOLD (`01`, `04`), RECURSE (`02`, `03`), PARTIAL (`05`), and STOP (`06`, `07`, `08`). |
| `LoopBreak(∇×T)` curl-resolution submove | `recursive-state-transitions.md`; output release; render contract; formalism spec; index; field-operator checker | Licenses owner-grounded loop-breaking only when nonzero curl remains and target, ground, Δ effect, and post-break reread are explicit. | `check_field_operator_architecture.py` requires positive/negative loop-breaker fixture coverage. | Hard circular-dependency cases must not defer forever or close over live curl. | PROVEN IMPLEMENTED | Fixture `10-field-gradient-loop-closure-coupling` requires target loop, grounding source, Δ effect, and post-break `∇×T` reread. |
| `𝒞(Ψᴺ)` positive closure-field condition | `recursive-state-transitions.md`; output release; render contract; formalism spec; index; bridge fixtures | Names terminal resolution only after the agent execution field reaches the target configuration, not checklist exhaustion. | Bridge and field-operator checkers require terminal fixture coverage, non-conversion boundary, and anti-symbol-theater policy. | Smoke must not expose terminal symbols as default decorative runtime or claim interlocutor uptake. | PROVEN IMPLEMENTED | Fixtures `06` and `10` permit terminal closure only after STOP and bounded residual field pressure. |
| `N_fiṭrī ∧ ʿaql ṣarīḥ` restorative terminal-state formalism | `recursive-state-transitions.md`; output release; render contract; formalism spec; index; bridge fixtures | Names final restorative orientation after governing misread loses control. | Bridge checker requires terminal restoration fixture coverage. | Hard smokes must preserve P1/P7 closure and restoration after live burdens are cleared/held/PARTIALed. | PROVEN IMPLEMENTED | Fixture `06` maps the terminal formalism to P1/P7 restoration after no live held route remains. |
| `Ψᴵ` and `T_lang: Ψᴺ ⇢ Ψᴵ` coupling boundary | `diagnostic-ir.md`; `recursive-state-transitions.md`; output release; formalism spec; index; field-operator checker | Separates agent execution field from diagnosed interlocutor field and bounds release to language-mediated public output without soul-access or uptake claims. | `check_field_operator_architecture.py` requires `Ψᴺ`/`Ψᴵ` distinction, coupling non-claims, docs/index concepts, and fixture coverage. | Restorative response can assess coupling conditions without claiming guidance control. | PROVEN IMPLEMENTED | Fixture `10-field-gradient-loop-closure-coupling` requires no guaranteed-uptake or access-to-soul claim. |
| Same-burden / next-burden algebra `Sameτ ∧ Sameξ ∧ SameΩ ∧ Sameσ ∧ Sameκ` | `recursive-state-transitions.md`; render contract; formalism spec; index; bridge fixtures | Prevents facet splitting into fake recursion when tribunal, warrant, ontology, source-status, and collapse radius are same. | Bridge checker requires `same_burden_test` coverage. | Hard smokes preserve no detached source-stack compression and no route/check contamination. | PROVEN IMPLEMENTED | Fixture `02` proves same `τ/ξ/Ω/σ/κ` stays inside the current burden cycle instead of splitting. |
| Shannon analogy boundary | formalism spec; index; bridge fixtures | Allows signal/encoding/noise/capacity/error-correction analogy only; forbids truth/meaning/warrant entropy claims. | Bridge checker requires negative Shannon fixture with forbidden claims and warrant blocker. | Register stress must not replace warrant with signal math. | PROVEN IMPLEMENTED | Fixture `07-shannon-boundary-negative` routes the issue to warrant grammar and forbids truth/meaning/warrant entropy claims. |
| Anti-symbol-theater guard | `diagnostic-ir.md`; render contract; output release; formalism spec; index; bridge fixtures | Notation may appear only when it changes routing, release, reread, owner selection, burden delta, or restoration. | Bridge checker requires visible-symbol policy or forbidden-symbol negative fixtures. | Targeted smoke/check assertion should keep unneeded symbols unprinted. | PROVEN IMPLEMENTED | Fixtures `04`, `06`, `07`, and `08` prove notation is permitted only with control effect and suppressed in default output otherwise. |
| Anti-schema-bloat guard | `diagnostic-ir.md`; `nomenclature-normalization.md`; render contract; formalism spec; `AGENTS.md`; checker; bridge fixtures | Preserves compact Diagnostic IR schema while accepting derived bridge semantics. | Bridge checker fails if hard schema fields appear in canonical schemas or fixture projections. | Full suite must pass with the current release-line schema contract. | PROVEN IMPLEMENTED | `check_register_formalism_bridge.py` rejects hard register fields in schemas and bridge fixture projections. |
| Optional `field_witness` sidecar | `diagnostic-ir.md`; `diagnostic-ir.schema.json`; `tools/check_ir_instance_integrity.py`; gap remediation audit | Provides optional schema-light traceability for route-gradient, burden delta, field diagnostics, loopbreak, reread, closure, and transfer boundary when an artifact elects to include it. | `check_ir_instance_integrity.py` validates the sidecar only when present and rejects empty/overclaiming shapes. | Future smoke/live artifacts may include it; ordinary outputs are not required to print it. | IMPLEMENTED OPTIONAL | It does not replace `post_render_gate`, does not force hard register fields, and does not prove truth, warrant, uptake, or full formal calculus. |
| Optional `field_witness.coverage_proof` shape | `diagnostic-ir.md`; `diagnostic-render-contract.md`; `tools/check_live_default_witness_contract.py`; `tools/visualize_closure_witness.py` | Documents schema-light collapse reconstruction: initial burden set, terminal states, dependency graph, divergence/curl status, and coverage completion. | Live witness checker validates rendered coverage structure; optional sidecar shape is for artifacts that elect to carry machine-readable coverage. | Future sidecar producers can mirror the rendered Closure/Reconstruction Witness without forcing ordinary outputs into a hard schema block. | DOCUMENTED OPTIONAL | Coverage proves rendered accounting, not live noetic competence or full formal calculus. |
| Hard mandatory schema fields for `heart`/`xi`/`Omega`/`mu`/`kappa` | `TODO.md`; `AGENTS.md`; formalism spec; checker | No runtime field migration in this slice; derived bridge maps to existing fields. | Bridge checker explicitly guards against accidental hard-field schema expansion. | Would require new schema migration and full hard smokes before release. | DEFERRED WITH BLOCKER | Blocker: breaks current Diagnostic IR schema and requires package/runtime version migration plus schema/checker/fixture/smoke migration. |
| v0.4.0.0 contract/release marker migration | `TODO.md`; release docs | Release-line migration is authorized only by the v0.4.0.0 release gate. | Frontmatter contract check must pass with `0.4.0.0` after migration. | Release-package canaries must be re-run from the authorized package artifact before release/package readiness is claimed. | RELEASE-GATED | Gate: requires package artifact rebake, package-bound smoke provenance, release docs, tag, and GitHub Release authorization. |

## Verification Plan

- Build generated framework/runtime from atomics when atomics change.
- Run the existing checker suite plus `tools/check_register_formalism_bridge.py`.
- Parse `docs/index.html` and visually sanity-check the restored interactive page.
- Preserve recovered v0.3.2.0 hard-canary behavior; installed-skill hard smokes are recorded as
  behavioral evidence, while package/release claims remain governed by release provenance.
- Treat the static bridge fixtures as proof of derived/conditional behavior against existing
  controls; treat the installed-skill smokes as live behavioral proof; do not treat either as proof
  of package artifact readiness or hard schema migration.
