# docs/index Design Refinement Implementation

Status: CURRENT-CANDIDATE

## Executive result

P1 docs/index design refinement implemented.

This pass did not replace the docs/index system and did not hand-edit generated
HTML. It refined source-owned sections, template behavior, generator output,
design documentation, visual QA documentation, and non-brittle checker coverage.

## Implemented P1 tasks

| Task | Result | Source owner | Regression prevention |
|---|---|---|---|
| Component role taxonomy | Added `data-surface-role` roles for focal, support, control, provenance, raw-source, disclosure, and generated-snapshot surfaces. Major tabs now declare one primary focal object. | `docs/index/DESIGN.md`, `docs/index/manifest.json`, source sections, template/generator | `tools/check_docs_index_interactions.py` checks roles and exactly one primary focal surface for Architecture, Owners, Theory, and Reference. |
| Reference source-browser | Reference Library now opens as summary counts, search/filter controls, document list, selected generated snapshot preview, then collapsed full source map. | `tools/build_docs_index.py`, `docs/index/sections/reference.html`, `docs/index/templates/index.html.tpl` | Checker validates `REF_SUMMARY`, source parity, list/detail before raw table, collapsed raw table, keyboard-accessible document buttons, and generated snapshots. |
| Owners selected-detail workspace | Owners/TTP now emphasizes source-derived summary counters, operator/family selector controls, selected operator detail, owner-family detail, and collapsed matrix/source tables. | `docs/index/sections/owners.html`, template JS, module catalogue data | Checker validates selected-detail workspace, collapsed raw tables, module-catalogue parity, source-path parity, and no hardcoded module-count claim. |
| Theory hierarchy | Theory now starts with notation map plus Highlighted notation context panel, with theory cards grouped by runtime phase segments while preserving source order and exact target mappings. | `docs/index/runtime-architecture.json`, `tools/build_docs_index.py`, `docs/index/sections/theory.html` | Checker validates card order/targets, metadata, phase classes, active card, button semantics, and critical notation survival. |
| Architecture support compression | Long runtime/source-boundary prose moved into a compact support disclosure below the primary Architecture runtime map and carousel. | `docs/index/sections/architecture.html` | Checker validates primary map first, paired vertical pipeline structure, no horizontal chip-row regression, and source-boundary/provenance as support. |
| Visual QA runbook | Added desktop and narrow/browser QA targets for Architecture, Owners, Theory, and Reference. | `docs/index/VISUAL_QA.md` | Checker requires the runbook and named QA targets. |
| Checker hardening | Added source-role, Reference hierarchy, `REF_SUMMARY`, Owners disclosure, critical notation, and runbook checks without pixel-perfect tests. | `tools/check_docs_index_interactions.py` | Structural/source checks prevent recurrence while leaving browser visual judgment in the runbook. |

## Notation preservation result

Exact daee-epistemics notation was preserved. The checker now requires critical
forms across generated output and source data, including `𝓝`, `D₀`, `Ψᴺ`,
`Ψᴵ`, `N∈𝓝`, `∇·T`, `∇×T`, `∇ route pressure`, `ⁿB`, `ⁿBᵢ[OPᵢ]`,
`Land(ⁿB)`, `ΔⁿB`, `Δκ`, `ΔⁿB{♥,ξ,Ω,σ,μ}/Δκ`,
`LoopBreak(∇×T)`, `R(H,Δ)`, `R(H, ΔⁿB{♥,ξ,Ω,σ,μ}, Δκ)`,
`𝒞(Ψᴺ)`, `T_lang`, `T_lang: Ψᴺ ⇢ Ψᴵ`,
`N_fiṭrī ∧ ʿaql ṣarīḥ`, `fiṭrah`, and `ʿaql ṣarīḥ`.

Plain-language labels were added around the notation where useful; formal
notation was not simplified, renamed, transliterated, or ASCII-normalized.

## Visual QA result

Desktop browser QA passed:

- Architecture paired pipelines rendered as two bounded vertical columns, not horizontal rows.
- Architecture carousel retained one selected primary card plus four scaled previews; cards 4 and 5 remained selectable.
- Owners/TTP opened as a selected-detail workspace; operator controls, family controls, summaries, and collapsed audit tables were usable.
- Theory card and notation selection updated the Highlighted notation panel with exact notation, meaning, runtime role, source owners, and related notation.
- Reference Library opened as source-browser summary/list/detail, not a table-first audit wall; filtering updated list and preview.
- No page-wide horizontal overflow or clipped notation/card content was observed.

Narrow browser QA passed:

- Architecture pipelines stacked into one bounded column and retained carousel controls.
- Owners controls and source-derived summaries wrapped without page-wide overflow.
- Theory notation wrapped and the Highlighted notation panel remained usable.
- Reference source-browser stacked list/detail flow and kept raw source map collapsed.

## Checks

Passed after implementation:

- `python tools\build_docs_index.py`
- `python tools\build_docs_index.py --check`
- `python tools\check_docs_index_interactions.py`
- `python tools\build_framework_pipeline.py`
- `python tools\build_compiled_runtime.py`
- `python tools\check_compiled_runtime_freshness.py`
- `python tools\check_field_operator_architecture.py`
- `git diff --check`

During validation, `python tools\build_compiled_runtime.py` initially found a
stale zero-byte ignored generated runtime file under `skill/`. That generated
runtime file was removed and the compiled runtime was rebuilt; freshness then
passed. No tracked runtime semantics changed.

## Remaining P2 work

- Consolidate older versioned CSS class families once the refined hierarchy has had more browser time.
- Add an optional ignored screenshot capture helper if repeatable visual evidence becomes useful.
- Add a small docs/index component example gallery only if future changes need it.
- Continue improving narrow top navigation ergonomics if it becomes a real usage pain.

## Release impact

This refinement does not block v0.4.2.0. It improves the generated public
navigation surface and checker discipline while leaving release, package, smoke,
tag, and runtime-semantics boundaries unchanged.
