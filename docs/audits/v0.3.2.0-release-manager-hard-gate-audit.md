# v0.3.2.0 Release-Manager Hard Gate Audit

**Date:** 2026-05-09
**Scope:** release-line coherence after pre-release faults were found in version markers, package shape, package docs, and smoke checker provenance handling.
**Release action status:** no `v0.3.2.0` tag, GitHub Release, or asset publication was created during this audit.

## Commands Run

Git/remote:

```text
git status --short
git branch --show-current
git rev-parse HEAD
git log --oneline --decorate -5
git status -sb
git remote -v
git ls-remote origin refs/heads/main
git tag --list v0.3.2.0
git ls-remote --tags origin refs/tags/v0.3.2.0
Invoke-WebRequest https://api.github.com/repos/theislampill/daee-epistemics/releases/tags/v0.3.2.0
Invoke-RestMethod https://api.github.com/repos/theislampill/daee-epistemics/actions/runs?per_page=10
Invoke-RestMethod https://api.github.com/repos/theislampill/daee-epistemics/actions/workflows?per_page=50
```

Version and contamination sweeps:

```text
rg 0.3.1.0 / 0.3.1.1 / 0.3.2.0 / RC00005 / current-release / package-bound / contract_version / compiler_version
rg stale contract/compiler markers across atomics, skill, tools, package docs, CI, and release docs
rg old package hashes and RC00005 labels across public and operative surfaces
rg \operatorname{Target} across docs, atomics, skill, and README
rg hard-smoke, local-path, source-audit, source-parade, and WSL/Hermes terms across operative/public surfaces
```

Build, package, and verification:

```text
python tools/build_framework_pipeline.py
python tools/build_compiled_runtime.py
python tools/check_compiled_runtime_freshness.py
python tools/check_compiled_module_boundaries.py
powershell -NoProfile -ExecutionPolicy Bypass -File .\package.ps1 build\daee-epistemics-v0.3.2.0.skill.zip
Copy-Item build\daee-epistemics-v0.3.2.0.skill.zip build\daee-epistemics-v0.3.2.0.skill
python skill/scripts/daee_level3.py --run-fixtures --simulate-output --repeat-stability 5
python tools/check_stub_integrity.py
python tools/check_consolidation_call_budget.py
python tools/check_routing_parity.py
python tools/check_routing_parity.py --strict
python tools/check_recursive_traversal_governance.py
python tools/check_render_modes.py
python tools/check_frontmatter.py
python tools/check_frontmatter.py --contract-version 0.3.2.0
python tools/check_coverage.py
python tools/check_framework_pipeline.py
python tools/check_recursion_collapse_noetic_frame.py
python tools/check_metacompliance_current_canon.py
python tools/check_smoke_artifacts.py
python tools/check_ir_instance_integrity.py
python tools/check_diagnostic_ir_catalogue_integrity.py
python tools/check_encoding_hygiene.py
git diff --check
```

`gh` CLI was unavailable in this workspace, so GitHub release and Actions checks used the public GitHub API.

## Git / Remote Coherence

- Branch: `main`.
- Local HEAD before hard-gate fix commit: `da23e4ec43f2b00a67303017669c55a96a970d91`.
- `origin/main` before hard-gate fix commit: `da23e4ec43f2b00a67303017669c55a96a970d91`.
- No local `v0.3.2.0` tag exists.
- No remote `refs/tags/v0.3.2.0` exists.
- GitHub Release `v0.3.2.0`: absent (`404`).
- GitHub Actions workflow: `.github/workflows/ci.yml`, active.
- Latest visible Actions runs before this fix were successful through `da23e4e`.

This audit found release-critical local fixes; they must be committed and pushed before tag/release work resumes.

## Findings And Patches

| Finding | Classification | Patch |
| --- | --- | --- |
| Operative atomics frontmatter retained `contract_version: "0.3.1.0"` in 103 source files and generated bundles. | release-blocking | Updated operative atomics to `0.3.2.0`, rebuilt generated runtime, and added explicit `check_frontmatter.py --contract-version 0.3.2.0`. |
| Generated runtime metadata retained `compiler_version: "0.3.1"`. | release-blocking | Updated `tools/compiled_runtime_lib.py` to compiler version `0.3.2` and rebuilt `skill/`. |
| CI ran frontmatter shape checks but did not enforce the current contract version. | must patch before tag/release | Added `python tools/check_frontmatter.py --contract-version 0.3.2.0` to `.github/workflows/ci.yml`. |
| `package.ps1` still rejected `tests/` even though v0.3.2.0 Level 3 packages include packaged fixtures. | release-blocking | Updated package validation to require/allow `data/`, `scripts/`, and `tests/` at archive root while still rejecting repo clutter. |
| Package artifact docs had stale current-hash/current-boundary wording. | release-blocking | Rebuilt the package and updated `docs/release-artifacts.md`, `docs/package-smoke-readiness.md`, `docs/v0.3.2.0-release-log.md`, and `docs/v0.3.2.0-release-notes.md`. |
| `tools/check_smoke_artifacts.py` re-bound historical v0.3.1.0 smoke evidence to the current release artifact. | release-blocking | Historical smokes now require a valid `current source package` shape but are not forced to match the current artifact hash. |
| Synthetic current-release bad samples in `tools/check_smoke_artifacts.py` were hard-coded to v0.3.1.0. | release-blocking | Added current v0.3.2.0 package constants and made synthetic current-release samples use the current release artifact dynamically. |
| README package boundary still foregrounded v0.3.1.0 as the current public asset. | must patch before tag/release | Reworded the package boundary so v0.3.1.0 is historical evidence and v0.3.2.0 package evidence is read from `docs/release-artifacts.md`. |
| Historical v0.3.1.0 release log mentioned `current RC00005` build evidence. | non-blocking but patch now | Reworded it as subsequent mainline package build evidence. |

## Version-Marker Classification

- `contract_version: "0.3.2.0"` is present across the current operative atomics frontmatter.
- `compiler_version: "0.3.2"` is present in generated runtime metadata.
- Search for stale operative `contract_version: "0.3.1.0"` / `compiler_version: "0.3.1"` / `COMPILER_VERSION = "0.3.1"` across `atomics`, `skill`, `tools`, docs, package script, and CI returned no release-blocking matches.
- Remaining `v0.3.1.0` references are historical changelog/release-log/release-note/smoke-checker sample references or historical package provenance.
- No stale `RC00005` or old v0.3.2.0 package-hash reference remains in public/current release docs or operative surfaces.

## Frontmatter / YAML Contract Coherence

- `python tools/check_frontmatter.py`: PASS.
- `python tools/check_frontmatter.py --contract-version 0.3.2.0`: PASS.
- Files checked: 105 atomics reference files.
- YAML/JSON data key hazard check over `atomics/skill/data` and `skill/data`: 0 non-ASCII keys.

## Generated-From-Atomics Coherence

- `python tools/build_framework_pipeline.py`: PASS.
- `python tools/build_compiled_runtime.py`: PASS.
- `python tools/check_compiled_runtime_freshness.py`: PASS.
- `python tools/check_compiled_module_boundaries.py`: PASS.
- Generated `skill/` contains packaged Level 3 `data/`, `scripts/`, and `tests/`.
- No hand-edited generated-only release changes were found; generated files were rebuilt from atomics/source tooling.

## Package Shape

Current local package artifact:

| Field | Value |
| --- | --- |
| Asset path | `build/daee-epistemics-v0.3.2.0.skill` |
| SHA256 | `EE1A90C7ABBCC06A7C6098903E73FFD69872D65E5FF84D0C84FD5AAAE46E7EE6` |
| Size | `522890` bytes |
| Entry count | `45` |
| Top-level entries | `README.md`, `SKILL.md`, `build-manifest.json`, `compiled-module-map.json`, `data/`, `references/`, `scripts/`, `tests/` |

Package forbidden-entry scan found no `smokes/`, `level3-runs/`, `.daee/`, `__pycache__`, `atomics/`, `tools/`, `docs/`, `build/`, `.git/`, `.pyc`, temporary owner lists, raw campaign artifacts, or local Hermes helpers in the package.

## Release-Artifact Docs Coherence

- `docs/release-artifacts.md` records the v0.3.2.0 package filename, SHA256, size, entry count, expected root shape, and no-current-release-smoke boundary.
- Historical v0.3.1.0 package evidence remains historical and is not described as current.
- `docs/package-smoke-readiness.md` records the v0.3.2.0 package hash and keeps `--require-current-release-smokes` as a stricter promotion gate.
- `docs/v0.3.2.0-release-log.md` and `docs/v0.3.2.0-release-notes.md` record the current package artifact and preserve the honest non-claims.

## Smoke / Evidence Boundary

- `python tools/check_smoke_artifacts.py`: PASS.
- Current-release strict mode was not run because no current-release package-bound smoke suite exists yet. Running `--require-current-release-smokes` would be expected to fail until such smokes are truthfully regenerated.
- Historical `runtime-grounding-v5` smoke artifacts remain historical regression evidence; they are no longer re-bound to the current v0.3.2.0 release artifact by the checker.
- Synthetic current-release bad samples now use the current package filename/SHA dynamically through checker constants.

## README / Changelog / TODO Public Coherence

- README describes Level 3 route-first execution under runtime use, not as an opening top-level feature block.
- README documents `/daee-epistemics [input]`, script-available Level 3 default behavior, scriptless Level 1/2 fallback, maintainer commands, and package/smoke boundaries.
- CHANGELOG records v0.3.2.0 Level 3 route-first work and the contract/compiler marker update.
- TODO keeps completed v0.3.2.0 work out of current blockers and moves future work under post-v0.3.2.0 candidates.
- No public docs claim pure-Hermes parity, deterministic feature extraction, all-runtime Level 3, codon implementation, or fixture-18 resolution.

## CI / GitHub Actions Coherence

- Workflow YAML parse: PASS.
- CI triggers on `push` and `pull_request`.
- CI calls local repo commands and does not require secrets or hosted LLM calls.
- CI is maintainer/release validation only and is not required for ordinary skill use.
- Added current contract-version enforcement to CI.

## Source-Neutrality / Contamination

- No `\operatorname{Target}` rendering bug remains in docs/atomics/skill/README search.
- No source-audit ledger, quotation-bank, scholar-parade, public-source-list, or source-to-TTP extraction surface was introduced in operative/public release surfaces.
- Named TST/Richard/Lael/Lillard strings remain in tests, fixture paths, generated fixture metadata, and smoke-checker contamination guards; they are not operative routing logic.
- WSL/Hermes strings remain in maintainer tooling (`package.ps1`, `tools/run_hermes_*.ps1`) and are not packaged runtime surfaces or ordinary user instructions.
- No `/home/agent` or `C:\workspace` local path remains in intended release surfaces.

## Full Verification Result

All mandatory hard-gate checks passed:

- Level 3 fixture runner: PASS, 7 fixtures, 5 stability repetitions.
- Build/freshness/module-boundary checks: PASS.
- Stub integrity, call budget, routing parity, strict routing parity: PASS.
- Recursive traversal, render governance, coverage, framework pipeline: PASS.
- Recursion/noetic-frame, metacompliance, smoke artifact validation: PASS.
- IR instance integrity, diagnostic IR catalogue integrity, encoding hygiene: PASS.
- `git diff --check`: PASS with Windows line-ending warnings only.

## Remaining Unstaged / Untracked Files

At audit time the hard-gate patches were local modifications and must be committed/pushed before release work resumes. Build artifacts remain untracked/ignored:

- `build/daee-epistemics-v0.3.2.0.skill`
- `build/daee-epistemics-v0.3.2.0.skill.zip`
- historical/local `build/` smoke helper outputs
- local `level3-runs/` if present

These build outputs are not source commits; GitHub Release asset upload is a separate explicit step after tag/release approval.

## Tag / Release Decision

No tag or GitHub Release was created. After the hard-gate fix commit is pushed and CI is green, the release manager may proceed to tag/release only with the evidence boundary intact:

- v0.3.2.0 package artifact recorded and reproducible.
- Historical smokes are historical regression evidence.
- Current package-bound smokes are absent unless regenerated later.
- Level 3 claims remain limited to deterministic routing given extracted features, not deterministic feature extraction or guaranteed transformer execution.

## Final Classification

**RELEASE HARD GATE CLEAN WITH NON-BLOCKING NOTES — TAG/RELEASE MAY PROCEED**

Non-blocking notes:

- Current-release package-bound smoke artifacts are absent and must not be claimed.
- The package archive is build output and is not committed; publish it only as an explicit GitHub Release asset.
- The hard-gate fix commit must be pushed and CI must remain green before tag/release commands are run.
