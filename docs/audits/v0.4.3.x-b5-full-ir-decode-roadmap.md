# B.5 Full-IR Decode Roadmap

Status: `B.5.1 CANONICAL_IR_PROJECTION CHECKER/FIXTURE SLICE SOURCE-BOUNDARY LIVE-CI PASS; FULL IR DECODE NOT CLAIMED`

This audit note records the post-A.13.2 B.5 architecture and the first
checker/fixture implementation slice. It is not release notes, not package
provenance, and not evidence of runtime/default hard-register emission.

Source-boundary evidence: commit
`960ff404309e749eb20823a91e404724b81d2bb5` passed GitHub CI run
`26731429838` and Pages run `26731429349`.

## Current B.5 State

B.5 previously proved bounded NLA semantic faithfulness: visible ACT records
can be parsed into checker-owned `CanonicalActivation` slots, dereferenced to
the matching Layer B body, compared against `field_witness.owner_activations`,
and reconstructed into verifier-passable Layer B submoves.

That was useful but intentionally not full IR decode. It did not prove that a
complete machine-readable IR projection could be recovered from output-level
evidence, especially when hard-register state was present.

## A.13.1 / A.13.2 Prerequisites

A.13.1 added the explicit hard-register schema marker
`diagnostic_ir_schema_version = "0.4.3-hard-registers-v1"` and canonical hard
register keys:

- `heart`
- `xi`
- `Omega`
- `mu`
- `kappa`

A.13.2 then added checker/fixture reconciliation between the hard-register v1
object, `field_witness.normalized_activation_record.live_registers`,
`field_witness.register_deltas`, and
`coverage_proof.diagnostic_completeness`.

These two slices make a B.5 projection gate possible without requiring ordinary
runtime governed output to emit hard registers by default.

## Target Model: Canonical IR Projection

`field_witness.canonical_ir_projection` is an optional B.5 reconstruction
object. When present, it states the IR-level slots that the checker can recover
from the visible output plus field witness:

- selected N-frame;
- live register set;
- burden floor;
- per-burden normalized activation rows;
- diagnostic-completeness coverage;
- optional hard-register v1 state when the hard-register marker is present.

The object is intentionally optional. Existing schema-light outputs remain
valid when it is absent.

## Projection Object

The current B.5.1 object is:

```json
{
  "schema": "b5-canonical-ir-projection-v1",
  "diagnostic_ir_schema_version": "0.4.3-hard-registers-v1",
  "hard_registers": {
    "heart": {"state": "non_live", "functions": [], "basis": [], "non_live_reason": "..."},
    "xi": {"state": "live", "functions": ["warrant-authority"], "basis": ["..."]},
    "Omega": {"state": "non_live", "functions": [], "basis": [], "non_live_reason": "..."},
    "mu": {"state": "non_live", "functions": [], "basis": [], "non_live_reason": "..."},
    "kappa": {"state": "live", "functions": ["closure-boundary"], "basis": ["..."]}
  },
  "n_frame": "...",
  "live_registers": ["xi", "kappa"],
  "burden_floor": ["B1"],
  "per_burden": [
    {
      "burden_id": "B1",
      "owner_id": "source-status-repair",
      "operation": "source-order",
      "delta_result": "science-source-bounded",
      "mrp_route_result_type": "held_burden_activation",
      "terminal_state": "landed",
      "generation_depth": 0
    }
  ],
  "diagnostic_completeness": {
    "live_registers": ["xi", "kappa"],
    "coverage": {"xi": ["B1"], "kappa": ["B1"]},
    "complete": true
  }
}
```

For schema-light projections, `diagnostic_ir_schema_version` and
`hard_registers` are omitted.

## What the Checker Proves Now

`tools/check_nla_decode_semantic_faithfulness.py` now validates
`field_witness.canonical_ir_projection` when an artifact elects to provide it.
The checker verifies:

- the projection schema marker is current;
- projection N-frame, live registers, burden floor, per-burden rows, and
  diagnostic-completeness object mirror `normalized_activation_record` and
  `coverage_proof`;
- projection per-burden rows decode to visible ACT rows by burden, owner family,
  operation, and delta result;
- route-result and terminal-state tokens are controlled;
- generation depth is a non-negative integer;
- hard-register projections require the A.13 v1 marker;
- hard-register v1 live/held state reconciles with projection live registers,
  NAR live registers, live `register_deltas`, and diagnostic completeness.

## What Remains Future / Protected

B.5.1 does not claim:

- default runtime emission of hard-register fields;
- full IR decode for arbitrary governed outputs;
- a standalone natural-language-to-IR parser;
- full register-level machine-checkable decode across all retained corpus cases;
- full IR decode implementation for package/provenance claims;
- guaranteed `T_lang` uptake;
- cross-host or paraphrase proof.

Those remain protected future choices and require explicit owner approval.

## First Implementation Slice

The first implementation slice is B.5.1:

- add optional `canonical_ir_projection` validation to the NLA decode checker;
- add schema-light positive projection fixture;
- add hard-register v1 positive projection fixture;
- add invalid canaries for missing hard-register live set, NAR live-set
  mismatch, `register_deltas` mismatch, diagnostic-completeness mismatch,
  hard-register object without version marker, and invalid owner/delta/MRP route
  projection.

## Risks / Stop Conditions

Stop if:

- schema-light NLA fixtures are forced to emit hard-register state;
- hard-register projection validation starts acting as runtime/default emission;
- checker green output is described as full IR decode;
- projection rows can pass without visible ACT/body evidence;
- projection hard-register live state can drift from NAR, register deltas, or
  diagnostic completeness;
- package, tag, upload, release, provenance, D.8 cleanup, model smokes, or
  runtime/default hard-register emission are accidentally opened.

## Fixtures / Canaries

Positive fixtures:

- `tests/nla-decode-semantic-faithfulness/valid/canonical-ir-projection-schema-light.md`
- `tests/nla-decode-semantic-faithfulness/valid/canonical-ir-projection-hard-registers.md`

Expected-invalid canaries:

- `tests/nla-decode-semantic-faithfulness/invalid/canonical-ir-projection-hard-register-missing-live-set.md`
- `tests/nla-decode-semantic-faithfulness/invalid/canonical-ir-projection-nar-live-register-mismatch.md`
- `tests/nla-decode-semantic-faithfulness/invalid/canonical-ir-projection-register-delta-mismatch.md`
- `tests/nla-decode-semantic-faithfulness/invalid/canonical-ir-projection-diagnostic-completeness-mismatch.md`
- `tests/nla-decode-semantic-faithfulness/invalid/canonical-ir-projection-hard-registers-without-version.md`
- `tests/nla-decode-semantic-faithfulness/invalid/canonical-ir-projection-invalid-owner-delta-route.md`
- `tests/nla-decode-semantic-faithfulness/invalid/canonical-ir-projection-schema-marker-mismatch.md`
- `tests/nla-decode-semantic-faithfulness/invalid/canonical-ir-projection-per-burden-row-count-mismatch.md`
- `tests/nla-decode-semantic-faithfulness/invalid/canonical-ir-projection-generation-depth-negative.md`
- `tests/nla-decode-semantic-faithfulness/invalid/canonical-ir-projection-terminal-state-invalid.md`
- `tests/nla-decode-semantic-faithfulness/invalid/canonical-ir-projection-hard-register-invalid-function.md`

Hardening addendum, 2026-06-01:

- Added focused no-model canaries for projection schema marker drift,
  projection-vs-NAR `per_burden` row-count drift, negative generation depth,
  uncontrolled terminal-state token, and hard-register controlled-vocabulary
  drift.
- Smoke A: before the hardening canaries,
  `python tools\check_nla_decode_semantic_faithfulness.py` PASSed with valid
  fixtures checked `4`, invalid fixtures checked `15`, and reconstructed
  submoves checked `6`.
- Smoke B: after the hardening canaries,
  `python tools\check_nla_decode_semantic_faithfulness.py` PASSed with valid
  fixtures checked `4`, invalid fixtures checked `20`, and reconstructed
  submoves checked `6`.

Register-composition projection addendum, 2026-06-01:

- Owner opened the combined B.5 / fixture 63 register-composition disposition
  lane after A.13.2. Gemba found an owner-free no-model slice: fixture 63
  already names register composition, owner eligibility, `R(H,Delta)/kappa`,
  and the rule that owner eligibility aids selection without becoming an
  automatic dispatch chain.
- `tools/check_nla_decode_semantic_faithfulness.py` now accepts an optional
  `field_witness.canonical_ir_projection.register_composition` object with
  schema `b5-register-composition-v1`.
- The checker binds that object to
  `tests/routing-fixtures/63-register-composition-owner-handoff.json`, requires
  component hard registers to match projection `live_registers`, keeps `sigma`
  outside the hard-register object, requires selected owners to be backed by
  projection rows, and rejects automatic dispatch chains.
- Positive fixture:
  `tests/nla-decode-semantic-faithfulness/valid/canonical-ir-projection-register-composition.md`.
- Invalid canaries:
  `tests/nla-decode-semantic-faithfulness/invalid/canonical-ir-projection-register-composition-auto-dispatch.md`
  and
  `tests/nla-decode-semantic-faithfulness/invalid/canonical-ir-projection-register-composition-component-mismatch.md`.
- Smoke A before this addendum:
  `python tools\check_nla_decode_semantic_faithfulness.py` PASSed with valid
  fixtures checked `4`, invalid fixtures checked `20`, and reconstructed
  submoves checked `6`; `python tools\check_routing_parity.py --strict`
  PASSed with fixtures checked `92`.
- Smoke B after this addendum:
  `python tools\check_nla_decode_semantic_faithfulness.py` PASSed with valid
  fixtures checked `5`, invalid fixtures checked `22`, and reconstructed
  submoves checked `11`.
- Source-boundary / live checks: commit
  `13ba5ef5b92a2df80fb28770ee839672c4b047eb` pushed the projection gate.
  GitHub CI run `26735634986` PASSed, and Pages run `26735634625` PASSed.

## Boundary

This roadmap and B.5.1 checker slice are schema/checker/fixture work only.
They do not change runtime/default governed output emission and do not open
package, tag, upload, release, provenance, D.8 cleanup, or model-smoke work.

Post-A.13.3 decision-prep note, 2026-06-01:

- The A.13.3 owner-decision packet is now
  `docs/audits/v0.4.3.x-a13-3-runtime-emission-decision-packet.md`.
- No additional owner-free B.5 implementation slice was found after B.5.1 and
  fixture 63. The remaining full-IR work depends on an owner decision to accept,
  revise, or defer A.13.3 runtime/projection emission policy, or on a separate
  owner-approved full-IR preflight.
- B.5 remains bounded to schema-light semantic faithfulness plus optional
  `canonical_ir_projection` checking until that future owner decision.

## CI Gate Addendum

2026-06-01 local patch:

- `.github/workflows/ci.yml` now runs
  `python tools/check_nla_decode_semantic_faithfulness.py` in the main runtime
  check sequence.
- Smoke A before the workflow patch:
  `python tools\check_nla_decode_semantic_faithfulness.py` PASSed locally with
  valid fixtures checked `4`, invalid fixtures checked `20`, and reconstructed
  submoves checked `6`.
- The patch promotes the B.5.1 fixture gate into CI. It does not broaden B.5
  into full runtime/default IR decode and does not make Graphify or ActiveGraph
  proof.
- Smoke B local: the full local CI-equivalent command set from
  `.github/workflows/ci.yml`, including the newly inserted B.5 checker, PASSed.
  `git diff --exit-code -- atomics/skill/references/diagnostics/framework-pipeline.md`
  and `git diff --check` also PASSed.
- Source-boundary / live checks: commit `f91c636` pushed the workflow gate.
  GitHub CI run `26731891310` PASSed, and Pages run `26731890984` PASSed.
