# v0.4.4.0 Post-Gate48 Four-Smoke Matrix

Status: shared-infrastructure launch Andon recorded; refreshed matrix pending.

Boundary:
This is diagnostic-matrix evidence only. It is not Stage 8, retained
promotion, package, tag, GitHub Release, release asset, public sample
publication, or provenance evidence.

## Preflight

- Source boundary: `3dcde51dcea52f9e4f55be7407a61a65d0e516f3`.
- Ahead/behind before launch: `0/0` versus `origin/main`.
- GitHub CI: `26981321278` PASS.
- GitHub Pages: `26981320604` PASS.
- Worktree: clean tracked worktree.

## Andon GATE49-MATRIX-SUBAGENT-CWD-INFRA

Symptom:
The first post-Gate48 four-smoke matrix launch did not reach the smoke harness.
All four subagents invoked relative `tools\run_staged_current_skill_smoke.py`
from `C:\workspace\ai\daee-epistemics` instead of the active worktree
`C:\workspace\ai\daee-epistemics\_tmp_stage07_v7_repair_20260603`.

Failed check:

```powershell
python tools\run_staged_current_skill_smoke.py ...
```

Representative failure:

```text
can't open file 'C:\workspace\ai\daee-epistemics\tools\run_staged_current_skill_smoke.py':
[Errno 2] No such file or directory
```

Owner file / owner surface:
Coordinator subagent-launch prompt, not a repository checker or case artifact.
The intended harness exists at
`C:\workspace\ai\daee-epistemics\_tmp_stage07_v7_repair_20260603\tools\run_staged_current_skill_smoke.py`.

5 Whys:

1. The matrix failed because the subagents invoked the runner from the parent
   workspace, where `tools\run_staged_current_skill_smoke.py` does not exist.
2. The wrong cwd existed because the coordinator prompt described the intended
   repo path but did not make `Set-Location` part of the executable command.
3. The launch was allowed to proceed because the preflight checked the main
   thread worktree, not each subagent's actual execution cwd.
4. The failure was not caught earlier because the launch prompt used relative
   paths for both the runner and run directories.
5. Recurrence is prevented by requiring every smoke subagent command to start
   with an explicit `Set-Location -LiteralPath <active worktree>`, verify the
   harness path exists, and verify the run directory absence under that exact
   worktree before invoking the runner.

Hansei:
Subagents do not inherit the main thread cwd as proof. For long, expensive
smokes, the launch command itself must bind the worktree, harness path, and run
directory before any model transport is touched.

Countermeasure:
Refresh the matrix from scratch with new run directories and commands that
begin with:

```powershell
Set-Location -LiteralPath 'C:\workspace\ai\daee-epistemics\_tmp_stage07_v7_repair_20260603'
if (-not (Test-Path -LiteralPath 'tools\run_staged_current_skill_smoke.py' -PathType Leaf)) { throw 'missing harness' }
if (Test-Path -LiteralPath '<assigned-run-dir>') { throw 'run dir already exists' }
```

Smoke B command/result:

```powershell
Test-Path -LiteralPath C:\workspace\ai\daee-epistemics\_tmp_stage07_v7_repair_20260603\tools\run_staged_current_skill_smoke.py -PathType Leaf
```

Result: PASS in the main thread. Refreshed subagents must repeat the same
preflight inside their own command before running.

Remaining blocker or closure:
Open until this documentation Andon is committed/pushed and CI/Pages are green,
then the four-smoke matrix may be relaunched with new run directories. The
failed launch produced no case output and no case-specific Stage 07 evidence.

## Invalidated Launch Attempts

| Case | Assigned run dir | Result |
|---|---|---|
| Trinitarian J17:3 | `.daee/release-regression-smokes/20260604-174457-matrix-trinitarian-j173` | Harness not found from parent cwd; no run dir/output in active worktree. |
| TST Lillard | `.daee/release-regression-smokes/20260604-174457-matrix-tst-lillard` | Harness not found from parent cwd; no run dir/output in active worktree. |
| Khaybar | `.daee/release-regression-smokes/20260604-174457-matrix-khaybar` | Harness not found from parent cwd; no run dir/output in active worktree. |
| Secularism | `.daee/release-regression-smokes/20260604-174457-matrix-secularism` | Harness not found from parent cwd; ignored raw input was created under the parent workspace, not the active worktree; no case output. |

## Non-Claims

- No model smoke reached the harness in this launch.
- No Stage 01-07 case result exists for the invalidated launch.
- No Stage 8 sidecars were built.
- No docs Output Grapher smoke ran.
- No retained promotion or retained corpus proof was attempted.
- No package, tag, GitHub Release, release asset, public sample publication, or
  provenance action opened.

## Refreshed Matrix 20260604-175402

Status:
Complete with case-specific failures. The Gate49 cwd/harness infrastructure
Andon did not recur. No package, tag, GitHub Release, release asset, public
sample publication, or provenance action opened.

| Case | Reached output.md? | Output size | SHA256 | B_LA | B_MRP | ACT/submove count | Stage 01-06 | Stage 07 | Stage 08 | First failed gate | Failure class | Predicted by canary audit? | Shared vs case-specific | Next smallest repair |
|---|---:|---:|---|---:|---:|---:|---|---|---|---|---|---:|---|---|
| Trinitarian J17:3 | yes | 143770 | `4D202787873898124D1F99F8824DEEB0112FCDD55CF32891DACA755360D87513` | 5 | 1 | 11 unique; 22 mirrored | pass except Stage 05 partial | PASS | FAIL | Stage 08 B.5 full-IR projection eligibility | truthful HOLD/PARTIAL generated burden blocks collapse-positive projection | yes | case-specific manifestation of proof-boundary/retained eligibility | Repair proof objective or route execution so generated `B6` is either actually executed/closed or remains explicit HOLD/PARTIAL without entering B.5 projection eligibility. |
| TST Lillard | no | n/a | n/a | 5 | n/a | 7 Stage 04 ACT rows in failed response | Stage 01-03 pass; Stage 04 fail | not reached | not reached | Stage 04 burden execution ACT | route label / callable owner and controlled delta vocabulary gap | yes, older TST route-label family | case-specific route/callable-owner defect with generic owner-contract implications | No-model repair for `do-second-loop` as non-ACT route label and `V2-reconstituting-reason` delta vocabulary or routing downgrade. |
| Khaybar | yes | 134018 | `51ED1A74EB207C6A07C4C2436B22F2DB83D5EE2DA1B8133E93ED66AD8EA3980A` | 3 | 0 | 9 structured ACT rows; 26 literal ACT markers | pass | post-output verifier FAIL | not reached | `check_mrp_generated_burden.py` | hard-case empty `B_MRP` / no generated_burden_instantiation | yes | shared hard-case generated-MRP invariant, Khaybar shape | No-model repair for hard-source cases that land all baseline burdens with no generated pressure: prove explicit no-recoil/non-load-bearing basis or produce generated `B_MRP`. |
| Secularism | yes | 158082 | `3B72C2B691F1B8E7F84AD1E96A8126128DC6464CEFE8A0826D2D487C07E4CE2A` | 4 | 0 | 12 structured ACT rows; 24 literal ACT markers | pass | post-output verifier FAIL | not reached | `check_mrp_generated_burden.py` | hard-case empty `B_MRP` / no generated_burden_instantiation | yes | shared hard-case generated-MRP invariant, Secularism shape | Same generic hard-case generated-MRP/no-recoil proof lane as Khaybar. |

## Andon GATE50-TRINITARIAN-B5-PROJECTION-ELIGIBILITY

Symptom:
Trinitarian produced a 143770-byte `output.md` and passed Stage 07, but failed
Stage 08 verifier-sidecar eligibility. The collapse certificate truthfully
reported `collapse_positive=false`, `coverage_complete=false`, and
`hold_partial_nodes=B6 carried-PARTIAL`.

Failed check:

```text
stage-08-verifier-sidecars: B.5 full-IR projection ineligible:
collapse_positive must be true before B.5 projection; coverage_complete must
be true before B.5 projection; hold_partial_nodes must be empty before B.5
projection
```

Owner file / owner surface:
`tools/run_staged_current_skill_smoke.py` Stage 08 verifier-sidecar eligibility
gate and Stage 07 generated-burden execution route. The failure artifact is
`.daee/release-regression-smokes/20260604-175402-matrix-trinitarian-j173/proof-sidecars/matrix-trinitarian-j173-gate49-refresh.b5-full-ir-projection-eligibility.json`.

5 Whys:

1. B.5 projection failed because the output was Stage 07-valid but not
   collapse-positive.
2. It was not collapse-positive because generated `B6` remained carried
   PARTIAL/HOLD rather than executed and closed.
3. `B6` remained open because the model surfaced genuine generated pressure
   but did not execute that burden to a landed terminal state.
4. The system allowed the run to reach Stage 08 because Stage 07 can honestly
   pass HOLD/PARTIAL outputs, while B.5 projection is stricter.
5. Recurrence is prevented by keeping the eligibility gate strict and repairing
   the upstream proof objective/route execution so generated burdens either
   receive ACT execution or remain explicitly non-eligible without projection.

Hansei:
This is a proof-boundary success, not a validator bug: Stage 07 can preserve an
honest partial result, but B.5 projection must not promote it.

Countermeasure:
Proposed, not yet applied: add a no-model Stage 08 eligibility fixture from
this shape if not already covered, and repair the Trinitarian route/execution
producer only if the release objective requires collapse-positive closure
rather than an honest generated HOLD/PARTIAL result.

Smoke B command/result:
No countermeasure has been applied in this lane. The failed matrix artifact is
preserved as negative evidence.

Remaining blocker or closure:
Open. Blocks Trinitarian retained promotion and release. Does not authorize a
validator weakening.

## Andon GATE50-TST-STAGE04-CALLABLE-OWNER-DELTA

Symptom:
TST stopped at Stage 04 before `output.md` because the model could not
honestly compile ACT rows from the supplied Stage 03 route set without
inventing a callable ACT owner or controlled `delta_result`.

Failed check:

```text
stage-04-burden-execution-act: model returned fail
```

Representative failure:

```text
B2/B3 require do-second-loop and V2-reconstituting-reason from the supplied
owner_routes; do-second-loop is a case-library/load-floor route label rather
than a valid ACT bracket owner, and V2 has no allowed Stage 04 delta_result
token in the provided controlled vocabulary.
```

Owner file / owner surface:
`tools/run_staged_current_skill_smoke.py` Stage 03 route-owner output and
Stage 04 ACT owner/delta controlled vocabulary contract; owner catalogue /
delta-vocabulary source surfaces for `do-second-loop` and
`V2-reconstituting-reason`.

5 Whys:

1. Stage 04 failed because routed owners could not be converted into valid ACT
   rows.
2. The conversion failed because `do-second-loop` was routed as if it were a
   callable ACT owner, while it behaves like a case-library/load-floor route
   label.
3. `V2-reconstituting-reason` also failed because Stage 04 lacked an allowed
   controlled `delta_result` for that selected route.
4. The previous Gate48 hardening emphasized Stage 07 owner/delta proof and did
   not close this older TST Stage 04 route-label family.
5. Recurrence is prevented by a no-model route-to-ACT canary that rejects
   non-ACT route labels as ACT owners and either supplies or downgrades V2
   delta vocabulary before Stage 04.

Hansei:
Callable availability is not activation, and route labels are not automatically
ACT owners. The Stage 03 route set must be executable by Stage 04 without
inventing owner brackets.

Countermeasure:
Proposed, not yet applied: add a Stage 03/04 no-model fixture for
`do-second-loop` and `V2-reconstituting-reason`; patch the owner-route or
delta-vocabulary surface so Stage 04 either receives a callable owner plus
controlled delta or routes HOLD/PARTIAL instead of pretending Land.

Smoke B command/result:
No countermeasure has been applied in this lane. The failed Stage 04 record is
preserved as negative evidence.

Remaining blocker or closure:
Open. Blocks TST output generation, downstream Stage 07, Stage 08, retained
promotion, and release.

## Andon GATE50-KHAYBAR-EMPTY-BMRP-HARDCASE

Symptom:
Khaybar produced a 134018-byte `output.md`, but the post-output Stage 07
verifier rejected it because the hard case closed with structured `B_MRP=0`
and no `generated_burden_instantiation`.

Failed check:

```powershell
python tools\check_mrp_generated_burden.py --outputs .daee\release-regression-smokes\20260604-175402-matrix-khaybar\output.md --show-advisories
```

Representative failure:

```text
required hard theological smoke must prove non-empty B_MRP with generated_burden_instantiation
```

Owner file / owner surface:
`tools/check_mrp_generated_burden.py` hard-case gate remains strict; producer
surface is `tools/run_staged_current_skill_smoke.py` Stage 05/07 MRP route and
formal-reread output for hard-source cases.

5 Whys:

1. The checker failed because Khaybar emitted no generated `B_MRP`.
2. It emitted no generated `B_MRP` because the model treated all visible
   source-stack pressure as resolved by baseline ACT rows.
3. The new Gate48 hard-case canary rejects that shape unless the output proves
   non-empty generated pressure or a source-owned no-recoil/non-load-bearing
   basis.
4. The producer does not yet force hard-source cases to distinguish
   no-recoil proof from absent generated-burden detection.
5. Recurrence is prevented by a generic hard-case no-recoil/generation canary:
   if no generated burden appears, the output must prove why MRP found no live
   generated pressure; otherwise it must instantiate and account for `B_MRP`.

Hansei:
The new checker is doing its job. A hard case cannot silently equate
baseline-landed burdens with global absence of generated pressure.

Countermeasure:
Proposed, not yet applied: add/extend a no-model fixture for hard-source
empty-`B_MRP` with explicit no-recoil proof requirements, then patch the MRP
producer to emit either generated `B_MRP` or parseable non-load-bearing
grounds.

Smoke B command/result:
No countermeasure has been applied in this lane. The failed output is preserved
as negative evidence.

Remaining blocker or closure:
Open. Blocks Khaybar Stage 07 proof, Stage 08, retained promotion, and release.

## Andon GATE50-SECULARISM-EMPTY-BMRP-HARDCASE

Symptom:
Secularism produced a 158082-byte `output.md`, but the post-output Stage 07
verifier rejected it because the hard case closed with structured `B_MRP=0`
and no `generated_burden_instantiation`.

Failed check:

```powershell
python tools\check_mrp_generated_burden.py --outputs .daee\release-regression-smokes\20260604-175402-matrix-secularism\output.md --show-advisories
```

Representative failure:

```text
required hard theological smoke must prove non-empty B_MRP with generated_burden_instantiation
```

Owner file / owner surface:
`tools/check_mrp_generated_burden.py` hard-case gate remains strict; producer
surface is `tools/run_staged_current_skill_smoke.py` Stage 05/07 MRP route and
formal-reread output for hard worldview/source cases.

5 Whys:

1. The checker failed because Secularism emitted no generated `B_MRP`.
2. It emitted no generated `B_MRP` because the model treated four baseline
   burdens as enough to close the hard worldview case.
3. Gate48 intentionally made Secularism path-aware for hard-case generation
   coverage, so the empty `B_MRP` shape is now rejected early.
4. The producer does not yet require an explicit no-recoil proof when a hard
   worldview case claims no generated pressure.
5. Recurrence is prevented by the same generic hard-case no-recoil/generation
   canary required for Khaybar, with Secularism as a negative example.

Hansei:
This is the expected result of the hardening lane. The checker caught exactly
the class it was designed to expose: hard case closure without generated-MRP
evidence.

Countermeasure:
Proposed, not yet applied: repair the generic hard-case MRP producer so
empty-`B_MRP` is allowed only with parseable, source-owned no-recoil /
non-load-bearing proof; otherwise instantiate `B_MRP`.

Smoke B command/result:
No countermeasure has been applied in this lane. The failed output is preserved
as negative evidence.

Remaining blocker or closure:
Open. Blocks Secularism Stage 07 proof, Stage 08, retained promotion, and
release.

## Matrix Decision

Release gates remain closed. Do not enter retained promotion, package, tag,
GitHub Release, release assets, public sample publication, or provenance.

Initial classification:

- TST is the earliest release-blocking failure by stage: Stage 04 callable
  owner / controlled delta contract.
- Khaybar and Secularism share a generic hard-case generated-MRP/no-recoil
  proof invariant and should be repaired together.
- Trinitarian reached the furthest point and exposed a proof-boundary
  eligibility issue: a truthful generated HOLD/PARTIAL output must not enter
  B.5 projection.

Required next gate:
Read-only saturation audit, then read-only adversarial falsification of the
coverage map, before any further model-smoke discovery.

## Saturation Audit

Read-only subagent saturation result:

- No patching, commit, push, model smoke, `.daee` mutation, retained promotion,
  package, tag, release, asset, public sample publication, or provenance action
  occurred.
- The refreshed `20260604-175402` matrix is not model transport and not shared
  infrastructure. The prior `20260604-174457` cwd/harness failure did not recur.
- Release gates remain closed.

Classification:

| Case | Saturation classification | Severity | Blocks further model-smoke discovery? | Smallest no-model repair / canary |
|---|---|---:|---:|---|
| Trinitarian J17:3 | Release-scope/proof-boundary mismatch; old B.5 generated-burden eligibility family, not a Stage 07 regression | HIGH for release | partly | Keep B.5 strict. Add/retain fixture where `collapse_positive=false`, `coverage_complete=false`, and `hold_partial_nodes=[B6]` emits non-claim ineligibility; only repair producer if release objective requires executing/closing `B6`. |
| TST Lillard | Old-family recurrence plus narrow TST route-contract defect | CRITICAL | yes | Canary where `do-second-loop` cannot enter ACT as a bracket owner and `V2-reconstituting-reason` must have an allowed `delta_result` or be downgraded/HOLD before Stage 04. |
| Khaybar | New shared hard-case invariant, Khaybar shape: empty `B_MRP` without `generated_burden_instantiation` | HIGH | yes | Generic hard-case no-recoil/generation canary: if hard-source case emits `B_MRP=0`, it must prove parseable source-owned no-recoil/non-load-bearing grounds; otherwise instantiate generated `B_MRP`. |
| Secularism | Same new shared hard-case invariant, Secularism shape | HIGH | yes | Same generic lane as Khaybar; use Secularism as paired negative fixture for empty `B_MRP` with no generated-pressure proof. |

Saturation recommendation:
Run one narrow TST Stage 03/04 executable-route repair first if the goal is a
full four-case discovery matrix, then one generic Khaybar/Secularism hard-case
generated-MRP/no-recoil proof lane, then Trinitarian B.5-positive closure only
if the release objective requires generated `B6` execution rather than honest
HOLD/PARTIAL evidence.

## Adversarial Falsification

Read-only adversarial result:

- No patching, commit, push, model smoke, `.daee` mutation, retained promotion,
  package, tag, release, asset, public sample publication, or provenance action
  occurred.
- The adversarial audit agrees that TST Stage 03/04 is the next first repair,
  because it is the earliest matrix-stopping failure and prevents full
  four-case discovery.
- The hard-case generated-MRP lane must follow before any release-positive
  matrix claim because the current hard-case canary is too filename/path-gated
  to trust alone.

Adversarial findings:

| Finding | Why current coverage / planned repair is insufficient | Owner surface | Severity | Blocks next matrix? | Smallest no-model assertion |
|---|---|---|---:|---:|---|
| Hard-case `B_MRP` coverage is filename-gated | Khaybar/Secularism empty `B_MRP` is caught because the path contains `khaybar` or `secularism`, not because the output is semantically recognized as a hard source/worldview case. A renamed hard case could pass with empty generated burdens. | `tools/check_mrp_generated_burden.py`; hard-case MRP fixtures | HIGH | blocks release-positive hard-case matrix; not necessarily first TST repair | Feed current Khaybar/Secularism outputs through a neutral filename and require failure unless non-empty `generated_burden_instantiation` or structured hard-case no-recoil proof exists. |
| TST Stage 03 can emit non-callable ACT owners | `do-second-loop` is accepted as `owner_route` even though it is a case-library/load-floor route label; `V2-reconstituting-reason` has no Stage 04 controlled `delta_result`. | `tools/run_staged_current_skill_smoke.py`; `tools/check_staged_runtime_handshake.py`; `atomics/skill/references/diagnostics/delta-result-vocabulary.json` | CRITICAL | yes | Stage 03 fixture with `do-second-loop` and `V2-reconstituting-reason`; checker must fail unless downgraded to route-context/HOLD/PARTIAL or backed by callable ACT owner plus allowed delta token. |
| Delta canonicalization can hide bad model output | Stage 04 raw near-synonym `delta_result` values can be rewritten into controlled tokens before downstream validation, masking producer failure as checker success. | `tools/run_staged_current_skill_smoke.py`; Stage 04 ACT records; delta vocabulary checker | HIGH | blocks claiming a repaired matrix clean | Model/release-mode Stage 04 records with `normalization.delta_result_canonicalizations` must fail or be ineligible; raw ACT rows must validate before canonicalization. |
| Empty no-new-resultant proof is too cheap | Khaybar/Secularism can assert `no_new_resultant_proof.proved=true` with empty generated burdens, but hard cases lack machine-readable escape-route closure proof. | Stage 05/06 MRP producer contract; `tools/check_mrp_generated_burden.py` | HIGH | blocks hard-case lane before release-positive claim | If hard-case `B_MRP` is empty, require structured `escape_routes_checked[]` covering closure-boundary, proof-carousel, authority-order, hidden-framework, and restoration recoil, each marked non-load-bearing with source-owned basis. |
| MRP route-result enum consistency is underchecked | Trinitarian Stage 05 contains escape-route disposition spelling drift such as `generated_burden-instantiation` while nearby fields use `generated_burden_instantiation`. | Stage 05 MRP result schema/checker | MEDIUM | no for next matrix; yes for promotion/release eligibility | Validate `escape_routes_checked[].disposition` against the same controlled enum as MRP resultants and require consistency with generated-burden records. |

Repair sequencing decision:

1. Open a no-model TST Stage 03/04 executable-route repair lane first.
2. Then open a generic hard-case generated-MRP/no-recoil proof lane for
   Khaybar/Secularism and neutral filenames.
3. Then address Trinitarian B.5-positive closure only if the release objective
   requires generated `B6` execution rather than honest HOLD/PARTIAL evidence.

No further model-smoke discovery is authorized until the first no-model repair
lane is patched, Smoke B passes, changes are committed/pushed if clean, and
CI/Pages are green.
