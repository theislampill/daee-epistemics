# v0.4.0.0 Release Manager Hard-Gate Audit

## Gate Summary

Release title:
`v0.4.0.0 - Register Grammar, Release-Boundary and Meta-Noetic Operator Hardening, Evidence-Gated Operator Runtime`

Subtitle:
`Summary: Evidence-gated noetic operator architecture over the compact DSL runtime.`

## Baseline

- Starting source commit: `b1d70b59351a7ae2b357eb6aefa936c82bf7a65a`
- Starting `HEAD...origin/main`: `0 0`
- Starting `v0.4.0.0` tag: absent
- Starting GitHub Release `v0.4.0.0`: absent
- Release package authorization: explicit for v0.4.0.0 only

## Version / Contract Migration

- Current release-line front matter migrated to `contract_version: "0.4.0.0"`.
- Historical v0.3.2.0 release docs remain historical.
- AGENTS, README, TODO, package examples, package-shape wording, and release
  artifact metadata were migrated only where they describe the current release line.

## Source / Runtime Freshness

The generated runtime is rebuilt from atomics by:

```powershell
python tools/build_framework_pipeline.py
python tools/build_compiled_runtime.py
python tools/check_compiled_runtime_freshness.py
```

The release process blocks if generated runtime is stale.

## Package Artifact

- Asset: `daee-epistemics-v0.4.0.0.skill`
- SHA256: `78C2BECE8818DEFF10C8CEF1A1578D9B54D0929950460681D04DC4F885576D0C`
- Size: `554776` bytes
- Entries: `20`
- Shape: canonical package root only; repo/dev harness roots excluded.

## Current-Release Smoke Gate

Current-release smokes were regenerated against the v0.4.0.0 package SHA and
validated with:

```powershell
python tools/check_smoke_artifacts.py --root .daee/v0.4.0.0-current-release-smokes --require-current-release-smokes
```

Result: PASS.

## NLA / Diagnostic Explanation Faithfulness Gate

Release audit checked the NLA-inspired invariant:

```text
state -> explanation -> reconstructed state
```

The repo preserves this through `diagnostic-ir.md`, `ir-reconstruction-pass.md`,
`diagnostic-render-contract.md`, `recursive-state-transitions.md`,
`check_ir_instance_integrity.py`, `check_recursive_traversal_governance.py`, and
the child-mode checker family. The release does not claim activation-level
interpretability or transformer-internal inspection.

## CI Gate

CI must pass on the release commit before tag creation and GitHub Release asset
publication. If CI fails, the release blocks until the failing check is fixed and
rerun.

## Go / No-Go

Go only if all local release gates, current-release smokes, commit/push coherence,
CI, tag push, and GitHub Release asset verification pass. No package, tag, or
release claim is valid before those gates complete.
