# v0.4.3.x B.5.4 Strict Projection Decision Packet

Date: 2026-06-01

Status: `OWNER_ACCEPTED_OPTION_1; STRICT_PROJECTION_SHAPE_GUIDANCE_CANARY_LANE_OPENED; NO_SMOKE_OPENED`

Canonical proof ledger:
`docs/audits/v0.4.3.0-implementaudit-orchestrator.md`.

Canonical row ledger:
`docs/audits/v0.4.3.0-open-work-ledger.md`.

This packet prepares the next protected choice after the focused B.5.4
proof-mode/full-IR attempt. It does not authorize another model/current-skill
smoke, retained-corpus promotion, package, tag, upload, GitHub Release
mutation, provenance publication, D.8 cleanup, broad governed/model matrix
work, or an A.13 follow-on slice.

Graphify remains a read-only navigation aid. ActiveGraph, if used for this
decision, remains ignored local sidecar metadata and is not proof.

## Verdict

`READY_FOR_OWNER_DECISION`

B.5.4 is closer than the prior "no proof-mode artifact exists" blocker, but it
is not closed. The retained-target semantics and runtime guidance are now
source-boundary/live-CI proven, while the attempted governed output still did
not produce strict `canonical_ir_projection`, `decoded_ir`, `full_ir_decode`,
`proof_mode`, `emission_policy`, and terminal STOP/formal-reread agreement.

Recommended next decision: accept a no-model strict projection-shape
guidance/canary lane before authorizing another focused current-skill smoke.

Owner decision update: Option 1 was accepted. The resulting lane is no-model
source guidance plus invalid canaries only; it does not run another current-skill
smoke, promote retained proof, package, tag, upload, publish provenance, or close
B.5.4.

## Current State

- B.5.1 validates optional `field_witness.canonical_ir_projection`.
- B.5.2 validates optional `canonical_ir_projection.decoded_ir`.
- B.5.3 validates optional `canonical_ir_projection.proof_mode` and requires a
  proof-mode marker whenever `full_ir_decode` appears.
- B.5.4 adds retained proof-mode/full-IR target infrastructure and strict-subset
  target semantics.
- B.5.5 validates optional `canonical_ir_projection.emission_policy`.
- The B.5 fixture checker runs in CI.
- The visible noetic-field opening/header remains required for projection-
  bearing outputs.
- No positive retained proof-mode/full-IR governed artifact exists.

Source-boundary evidence:

- B.5.4/B.5.5 scaffold commit `352768005448b4cca4cd39c1e4a2208413411cd8`;
  GitHub CI run `26775051405` PASS; Pages run `26775047293` PASS.
- B.5.4 retained-target exactness fix commit `f8d69e4`.
- B.5.4 strict projection guidance/status commit `95a3a5d`; GitHub CI run
  `26779258641` PASS; Pages run `26779257934` PASS.
- B.5.4 live-boundary docs commit `d559942`; GitHub CI run `26779437244` PASS;
  Pages run `26779435741` PASS.
- Latest row/queue synchronization commit `82b3a22`; GitHub CI run
  `26779848388` PASS; Pages run `26779847563` PASS.

## Focused Attempt Result

The owner authorized one focused current-skill smoke/promotion attempt to close
`PARTIAL_REQUIRES_FRESH_PROOF_MODE_GOVERNED_ARTIFACT`.

Ignored local artifacts:
`.daee/b5-4-proof-mode-smoke/20260601-source-order-full-ir/`.

Outcome:

- Runs 1-3 failed ACT, field-witness, graph-completeness, or terminal stop-
  proof validators.
- After a narrow runtime-guidance patch, run4 passed field-witness convergence.
- Run4 still failed NLA semantic faithfulness because `canonical_ir_projection`,
  `decoded_ir`, `full_ir_decode`, `proof_mode`, and `emission_policy` did not
  match strict B.5 schemas.
- Run4 also failed formal reread / terminal STOP proof.
- No retained case was promoted.
- Failed `.daee` outputs must not be hand-edited into retained proof.

Current residual:
`PARTIAL_REQUIRES_STRICT_PROOF_MODE_PROJECTION_EMISSION`.

## Required Shape For Closure

A B.5.4 closure artifact must be a fresh governed artifact or checker-owned
sidecar source accepted by the retained proof-corpus gate. It must include:

- raw input sidecar;
- governed output;
- checker-owned collapse certificate;
- warning-clean certificate-backed Output Grapher sidecar;
- retained manifest entry under the specialized
  `b5-4-retained-proof-mode-full-ir` target;
- `field_witness.canonical_ir_projection.proof_mode`;
- `field_witness.canonical_ir_projection.decoded_ir`;
- `field_witness.canonical_ir_projection.full_ir_decode`;
- if B.5.5 is claimed, `field_witness.canonical_ir_projection.emission_policy`;
- strict NLA semantic faithfulness PASS;
- graph completeness / collapse certificate PASS;
- formal reread terminal STOP/no-new-resultant proof PASS;
- retained proof corpus PASS.

Schema-light historical B.5 retained cases must remain valid when they are not
listed under `b5-4-retained-proof-mode-full-ir`.

## Candidate Options

### Option 1 - No-model strict projection-shape guidance/canary lane

Patch only source/checker/docs/fixtures needed to make the strict projection
shape more obvious and harder to miss before another smoke.

Allowed:

- clarify generated runtime guidance for the exact proof-mode/full-IR object
  shape;
- add a compact fixture/canary that mirrors the run4 failure class if the
  current canaries do not already cover it;
- keep schema-light behavior valid;
- run local checkers and CI;
- update ledgers/orchestrator.

Not allowed:

- model/current-skill smoke;
- retained proof artifact promotion;
- package/tag/upload/release/provenance;
- D.8 cleanup;
- full arbitrary IR parser claim.

Stop condition:

- Gemba finds the current checker/fixture suite already covers the run4 failure
  class and only a new governed artifact could advance B.5.4.

### Option 2 - Another focused current-skill proof-mode smoke

Run exactly one new focused current-skill smoke with a prompt designed around
the strict B.5.4 projection stack.

Required up-front owner choices:

- prompt family;
- harness boundary;
- whether installed-skill metadata failure is acceptable or must be fixed first;
- required validator stack;
- whether B.5.5 `emission_policy` is required in the artifact.

Stop condition:

- harness failure;
- NLA, graph/certificate, formal reread, retained-corpus, or Grapher failure;
- output is structurally green but expert/manual scope is thin;
- any pressure to hand-edit `.daee` output into retained proof.

### Option 3 - Defer B.5.4 and work another row

Leave B.5.4 as infrastructure-only until a later proof-mode artifact is needed
for a release, package/provenance, or broader full-IR milestone.

This is safe if the project prioritizes other broad residuals:

- B.1/B.2 broad sidecar adoption;
- C.5-C.7 broad matrix coverage;
- D.2/D.3 broad governed-output adoption;
- package-bound proof only after a separate release gate.

## Risks

| Risk | Why it matters | Guard |
|---|---|---|
| Laundering failed `.daee` output | Would fabricate retained proof | Promotion must use checker-owned sidecars and manifest hashes only |
| Treating proof-mode fixture as governed proof | Existing proof-mode positive is a fixture/canary, not a promoted artifact | Retained manifest must identify real source artifact and raw input |
| Replacing visible opening/header | A.13.3.1 requires human-facing field orientation | Projection-bearing outputs must preserve the opening read/header |
| Overclaiming full IR decode | B.5 still validates optional projection objects, not arbitrary NL-to-IR parsing | Keep non-claims in docs and release surfaces |
| Running a model campaign accidentally | B.5.4 needs one focused artifact, not broad matrix work | Prompt count and proof target must be owner-selected |

## Required Files / Owners If Option 1 Is Accepted

- `tools/build_compiled_runtime.py`
- `skill/SKILL.md` generated through `python tools/build_compiled_runtime.py`
- `tools/check_nla_decode_semantic_faithfulness.py` only if Gemba proves a
  checker false-negative remains
- `tests/nla-decode-semantic-faithfulness/valid/`
- `tests/nla-decode-semantic-faithfulness/invalid/`
- `tests/retained-proof-corpus/v0.4.3.0-schema-light/`
- `docs/audits/v0.4.3.x-b5-full-ir-decode-roadmap.md`
- `docs/audits/v0.4.3.x-target-row-status.md`
- `docs/audits/v0.4.3.x-owner-decision-queue.md`
- `docs/audits/v0.4.3.0-open-work-ledger.md`
- `docs/audits/v0.4.3.0-implementaudit-orchestrator.md`

## Required Checks If Option 1 Is Accepted

- `python tools/check_nla_decode_semantic_faithfulness.py`
- `python tools/check_retained_proof_corpus.py`
- `python tools/check_graph_completeness.py`
- `python tools/check_collapse_certificate_schema.py`
- `python tools/check_formal_reread_state_semantics.py`
- `python tools/check_compiled_runtime_freshness.py` if generated runtime
  guidance changes
- `python tools/build_docs_index.py --check`
- `git diff --check`
- full local CI-equivalent if checker/runtime guidance changes

## Owner Options

1. Accept Option 1: open no-model strict projection-shape guidance/canary lane.
2. Accept Option 2: authorize one focused current-skill proof-mode smoke.
3. Defer B.5.4 and select another open row.

## Recommendation

Choose option 1.

Reason: run4 already showed the model can move toward the surface, but it missed
the strict object schemas and terminal proof details. A no-model guidance/canary
pass is cheaper and safer than immediately spending another focused smoke. It
also preserves the retained proof boundary by making the next smoke's target
sharper without treating Graphify, ActiveGraph, fixtures, or failed `.daee`
artifacts as proof.

## Non-Claims

This packet does not claim:

- B.5.4 completion;
- full IR decode;
- runtime/default hard-register emission;
- model-smoke proof;
- package/provenance proof;
- cross-host/paraphrase proof;
- guaranteed `T_lang` uptake;
- Graphify or ActiveGraph proof;
- permission to close D.8 rows.
