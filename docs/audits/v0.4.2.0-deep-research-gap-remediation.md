# v0.4.2.0 Deep Research Gap Remediation

Boundary: this is a targeted remediation ledger before any RC archive packaging. It does not
publish, tag, create a GitHub Release, fabricate smokes, fabricate live outputs, or claim public
release proof. Decisions are repo-grounded and release-safe.

| Finding ID | Finding | Repo evidence | Decision | Files changed | Verification | Remaining follow-up |
| --- | --- | --- | --- | --- | --- | --- |
| F1 | Fourteen modules were under-fixtured or partial on fixture/checker coverage: `E3-cumulative-case`, `E4-cross-cultural-check`, `F3-practice-epistemic-access`, `M1P-performative-self-refutation`, `M6-excluded-middle`, `M7-definition-anchor`, `R1-internalist-criterion`, `R2-the-reminder`, `R3-warranted-basic-belief`, `inductive-fitri-method`, `P2-objection-mapping`, `P5-already-believing`, `V3-regress-dissolution`, `V6-convergence`. | `docs/audits/v0.4.1.0-ttp-end-to-end-operativity-audit.json` records each as `PARTIAL`; RC2 fixture-mining checked both candidate archives and the live fixture suite. | IMPLEMENTED_NOW | RC1/RC2 strengthened all fourteen owner files; this pass added direct positive and negative/misuse routing fixtures for all fourteen and a coverage checker. See `docs/audits/v0.4.2.0-underfixtured-module-coverage.md` and `docs/audits/v0.4.2.0-fixture-corpus-mining-audit.md`. | `check_routing_parity.py --strict`, `check_underfixtured_module_coverage.py`, and `check_ttp_operator_contracts.py --strict` pass. | Future live/paraphrase probes remain needed; direct fixtures are static route proof, not behavior proof. |
| F2 | `LoopBreak:` must be conditional, not required as decoration in every ordinary output. | `diagnostic-render-contract.md`, `output-release.md`, `recursive-state-transitions.md`, `tools/check_live_default_witness_contract.py`. | IMPLEMENTED_NOW | Render/output/state docs; live witness checker accepts `not applicable` / null statuses and docs require licensed target/ground/Δ/reread/result only when cyclic pressure is non-null. | `check_render_modes.py`, `check_field_operator_architecture.py`, and py_compile. | Future live fixtures should include positive and null loopbreak captures. |
| F3 | Bare `R(H,Δ)` is insufficient; reread must show held/live remainder/new release or block/next pass. | Render/output/state docs and live witness checker. | IMPLEMENTED_NOW | `diagnostic-render-contract.md`, `output-release.md`, `recursive-state-transitions.md`, `tools/check_live_default_witness_contract.py`. | `check_render_modes.py`, `check_recursive_traversal_governance.py`, py_compile. | Add live-output fixtures once real outputs exist. |
| F4 | `𝒞(Ψᴺ)` must require agent/runtime-side closure wording and bounded decision/status. | Render/output/state docs, field-operator checker, live witness checker. | IMPLEMENTED_NOW | `diagnostic-render-contract.md`, `output-release.md`, `recursive-state-transitions.md`, `tools/check_live_default_witness_contract.py`. | `check_field_operator_architecture.py`, `check_render_modes.py`, py_compile. | Future live outputs should test COMPLETE/STOP/HOLD/PARTIAL/RECURSE variants. |
| F5 | Add optional `field_witness` sidecar as schema-light trace bridge if safe. | `diagnostic-ir.schema.json` previously had no sidecar; `check_ir_instance_integrity.py` had no sidecar validator. | IMPLEMENTED_NOW | `diagnostic-ir.md`; `diagnostic-ir.schema.json`; `tools/check_ir_instance_integrity.py`; `docs/register-formalism-implementation-ledger.md`. | `check_ir_instance_integrity.py` validates positive/bad embedded sidecar samples; existing fixtures remain valid. | Use only for artifacts that elect to include it; do not require ordinary outputs or claim full formal calculus. |
| F6 | `noetic-profiles` is redirect/context surface, not bounded runtime operator proof. | `module-catalogue.json` lists it as `case-library`; owner file says redirect only; `check_ttp_operator_contracts.py` currently allows only five executable-compatible classes. | DEFERRED_BREAKING_CATALOGUE_CHANGE | This ledger; next handoff. | Strict TTP checker remains compatible. | Add non-executable metadata/context catalogue classes in a separate schema/checker migration. |
| F7 | `heuristics` is ambient governance, not route-eligible bounded operator in the same sense as tactics. | `module-catalogue.json` lists it as `technique`; owner says "throughout every engagement as background governance"; checker class set has no ambient class. | DEFERRED_BREAKING_CATALOGUE_CHANGE | This ledger; next handoff. | Strict TTP checker remains compatible. | Add ambient governance class only with catalogue/checker/doc migration. |
| F8 | Thin operators need stronger field target, Δ effect, and `R(H,Δ)` language. | Primary owner files inspected in RC1: `E4-cross-cultural-check`, `F3-practice-epistemic-access`, `P2-objection-mapping`, `V6-convergence`; remaining under-fixtured owners inspected in RC2. | IMPLEMENTED_NOW | RC1 strengthened four primary thin owners; RC2 strengthened `E3`, `M1P`, `M6`, `M7`, `R1`, `R2`, `R3`, `inductive-fitri-method`, `P5`, and `V3`; this pass adds direct route/misuse fixture coverage. | Strict TTP checker, routing parity, and under-fixtured coverage checker. | Live-output route correctness and behavioral generalization remain future proof. |
| F9 | Add C-class versus D-class distinction without public stale A/B/C/D tables. | Deep Research reports and operativity audit distinguish weak bounded operators from router/context surfaces; no maintained catalogue field exists. | IMPLEMENTED_NOW | This ledger; coverage proof; next handoff; release-candidate audit. | Docs checks. | Add structured catalogue metadata before public tables. |
| F10 | `Ψᴵ` diagnosis should carry uncertainty / alternative-diagnosis guidance. | `diagnostic-ir.md` had soul-access boundary; schema has `read_status`, `confidence`, `decisive_missing_differentiator`; profile owners remain candidate surfaces. | IMPLEMENTED_NOW | `diagnostic-ir.md`; `recursive-state-transitions.md`; `diagnostic-render-contract.md`; `algebraic-notation-and-noetic-formalism.md`. | Field-operator and render checks. | Future profile fixtures should test underdetermined/alternative read handling. |
| F11 | Add seven-layer behavioral evaluation framework. | Prior docs separated smoke/release proof but did not name the seven evaluation layers. | IMPLEMENTED_NOW | `docs/behavioral-evaluation-framework.md`; `docs/package-smoke-readiness.md`; this ledger; next handoff. | Docs build/check. | Use it to design future smoke/live evaluation without canned expected outputs. |

## RC2 Implementation-Completeness Addendum

| Finding | Decision | Evidence | Files changed | Remaining follow-up |
| --- | --- | --- | --- | --- |
| F1 direct fixture closure | IMPLEMENTED_NOW | RC2 fixture-mining reviewed all 37 markdown files in the candidate archives and added direct positive plus negative/misuse fixtures for all fourteen under-fixtured modules. | `tests/routing-fixtures/65-rc2-*.json` through `92-rc2-*.json`; `tools/check_underfixtured_module_coverage.py`; `docs/audits/v0.4.2.0-underfixtured-module-coverage.md`; `docs/audits/v0.4.2.0-fixture-corpus-mining-audit.md`. | Future live/paraphrase behavioral evaluation; static fixtures are not live-output proof. |
| Operative contract front matter | IMPLEMENTED_ALREADY | All fourteen target owners plus `V1-diagnostic`, `M1-self-refutation`, `M9-predication-mode`, `P7-restoration-stops`, and `definition-discipline` already expose required operative frontmatter. | `tools/check_operative_contracts.py` verifies required keys, catalogue parity, companion IDs, and absence of unsupported `fixture_refs` / `blocked_moves`. | Optional fields remain limited to evidence already present in owners. |
| Pending current-release smoke skeleton | IMPLEMENTED_NOW | `tests/smokes/current-release/v0.4.2.0/manifest.json` lists `CR-01` through `CR-10` as `pending-live-output`; outputs/IR/verdict dirs contain only `.gitkeep`. | `tests/smokes/current-release/v0.4.2.0/`; `tools/check_smoke_artifacts.py`; `docs/package-smoke-readiness.md`. | Strict current-release smoke remains blocked until real package-bound outputs, IR, verdicts, and provenance exist. |
| Initial burden-set enumeration gate | IMPLEMENTED_NOW | Closure witness checker requires a pre-release Layer A / Diagnostic IR `Initial burden set` matching the closure witness, and rejects obvious retroactive initial-set wording. | `diagnostic-ir.md`; `recursive-state-transitions.md`; `diagnostic-render-contract.md`; `output-release.md`; `tools/check_live_default_witness_contract.py`; live-witness fixtures. | Future real smokes should show initial burden enumeration before terminal accounting. |
| Closure witness visualization | IMPLEMENTED_NOW | `tools/visualize_closure_witness.py` emits Mermaid Markdown and a JSON sidecar summary with nodes, dependency edges, coverage, collapse, `∇·B`, and `∇×κ`. | `tools/visualize_closure_witness.py`; `docs/package-smoke-readiness.md`. | Interactive colored-node graph remains a future artifact built on the JSON summary. |
| Catalogue reclassification | DEFERRED_BREAKING_CATALOGUE_CHANGE | `noetic-profiles` remains redirect/context; `heuristics` remains ambient governance; current catalogue/checkers do not yet support non-executable metadata/context classes. | This ledger; next handoff; release-candidate audit. | Design a checked catalogue-schema migration before changing module classes. |

## Proof Boundary

This pass improves traceability, contracts, fixtures, smoke-skeleton honesty, and evaluation framing.
It does not close release evidence blockers: public v0.4.2.0 provenance, current-release
package-bound PASS smokes, and live-output behavioral proof remain absent.
