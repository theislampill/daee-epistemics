# v0.4.3.x Staged Runtime A/B/C/D Closure Plan

Status: `ONE_CASE_STAGED_PROOF_BUNDLE_RETAINED; STAGED_LONG_OUTPUT_COMPILER_NO_MODEL_PASS; COMPILED_STAGE07_SURFACE_REPAIR_NO_MODEL_PASS; PILOT_RETRY_STAGE05_ACT_BURDEN_TERMINAL_MISMATCH; STAGE04_STAGE05_HANDOFF_REPAIR_NO_MODEL_PASS; REAL_V2_REPAIR_VERIFICATION_CHECKER_PASS; B1_B2_PILOT_V3_NEGATIVE_PRE_STAGE_FAILURE; STAGE06_NFRAME_REPAIR_NO_MODEL_LIVE_CI_PASS; B1_B2_PILOT_V4_NEGATIVE_STAGE07_VALIDATOR; STAGE07_ACT_PARTITION_MIRROR_REPAIR_LIVE_CI_PASS; B1_B2_PILOT_V5_NEGATIVE_STAGE07_ASSEMBLY; NO_BROAD_ROW_CLOSURE_CLAIM`.

Date: 2026-06-02

This plan explains how staged execution can later close remaining A/B/C/D row
families. It is not a completion claim. One staged smoke, even if it passes,
proves one-case staged execution only.

## Closure Rule

A row can move from partial to closed only when a staged row-family campaign is
owner-selected, validator-clean, retained or otherwise proof-owned, and mapped
back to the canonical open-work ledger and orchestrator. Failed staged runs are
negative evidence and do not close rows.

## Row-Family Requirements

| Row | What staged harness must prove before closure |
|---|---|
| A.13 | Staged hard-register/projection handoff in a proof-class run, plus retained sidecar proof. |
| B.1 | Staged graph/terminal handoff plus graph-completeness sidecar over selected cases. |
| B.2 | Staged certificate/sidecar generation by default for selected proof-class runs. |
| C.5 | Staged restoration-recoil route/owner coverage over the selected subtype matrix. |
| C.6 | Staged primary/restoration two-track handoff over selected cases. |
| C.7 | Staged divergence/curl/reread handoff over selected cases. |
| D.2 | Staged release/Closing Formulation binding to closure state. |
| D.3 | Staged mixed-concealment intake/routing/owner handoff over selected variants. |

## Current Closure State

The retained replay bridge creates the replay target shape. The staged
current-skill harness prototype creates the first model-stage runner. The first
run localized a Stage 03 route-target shape mismatch; the follow-up repair has
one Stage-03-scoped current-skill PASS with canonical `route_targets` and
optional `route_target_details`. A later Stage 04 repair adds canonical
ACT/body-ref handoff checks and has one Stage-04-scoped current-skill PASS with
canonical `⟦ACT ...⟧` rows and zero replay mismatches through
`stage-04-burden-execution-act`. The later Stage 05 repair adds canonical
terminal-state/dependency-graph/no-new-resultant checks and has one
Stage-05-scoped current-skill PASS with zero replay mismatches through
`stage-05-mrp-reread-terminal-state`. The later Stage 06 repair adds
canonical field_witness body-ref, NAR burden, owner-activation, structured NAR,
and register-delta checks and has one Stage-06-scoped current-skill local PASS
with zero replay mismatches through `stage-06-field-witness-nar`. The Stage 07
release-output contract/checker/harness path is source-boundary/live-CI green
as no-model source maintenance, but the single authorized Stage-07-scoped
current-skill retry failed before Stage 07 because rich Stage 02 diagnostic
objects exposed a harness normalization gap. That gap is patched, but no final
governed `output.md` has been produced by Stage 07 yet.

This does not close broad A/B/C/D rows because no final governed `output.md`,
proof sidecars, retained-corpus promotion, Stage 07 release-output handoff, or
row-family campaign was produced.

Proof-bundle update, 2026-06-02: a later owner-opened single proof-bundle retry
passed Stage 07, built Stage 08 sidecars, and retained case
`staged-a9-science-source-proofbundle` with rows `B.1` through `B.5` plus a
`b5_full_ir_projection_sidecar`. This creates one proof-owned staged
current-skill execution artifact for the `a9-science-source` source-order case.
It updates the closure map only at one-case evidence level. It still does not
close broad A/B/C/D rows because no staged row-family campaign has been
owner-selected, matrixed, validator-clean across a family, and mapped back to
the row ledger.

Long-output assembly update, 2026-06-02: the next B.1/B.2 row-family lane now
has a no-model Brandolini-safe assembly gate. `tools/build_staged_governed_output.py`
assembles final `output.md` from hash-checked sections under manifest schema
`staged-governed-output-assembly-v1`, rejects path escape, role-order errors,
hash mismatch, harness commentary, package/provenance claims, guaranteed-uptake
claims, and premature sidecar-proof claims, and writes an assembly hash record.
`python tools/build_staged_governed_output.py --self-test` passes with a
compiled output over 200KB. `python tools/run_staged_current_skill_smoke.py
--self-test` also covers compiled Stage 07 mode without a model and validates
the compiled output against the existing Stage 07 checker stack. This is a
compiler/harness readiness gate only; it is not retained row-family proof.

Pilot update, 2026-06-02: the single authorized secularism pilot ran in
compiled mode and failed Stage 07 visible-output validation after assembly.
The compiled output was 25,632 bytes and failed because it lacked the exact
validator-visible noetic-field opening/header, parser-stable `field_witness`,
and `normalized_activation_record`/`NAR` surfaces. Stage 08 sidecars were not
built, retained promotion did not run, and release-regression cases were
skipped by stop rule. The next narrow action is a no-model compiled Stage 07
prompt/fixture exactness repair, not a regression matrix run.

Repair/retry update, 2026-06-02: the compiled Stage 07 surface repair now
passes no-model checks. The assembler enforces the stricter compiled roles,
required public surfaces, forbidden claims, and `target_output_kb` floor; its
self-test passes with a `386,053` byte large fixture. The single authorized
secularism v2 retry then failed before Stage 07 at Stage 05 because Stage 04
`act_burdens` used descriptive strings while Stage 05 terminal states used
canonical `B1`-`B5` identifiers. A later no-model repair normalizes the Stage
04/Stage 05 burden-id handoff, and Gate 2 repair verification passes against
the real v2 failed-prefix artifact after the checker learned to classify
explicit stopped failure records as non-proof prefix-verification evidence.
That checker lane was pushed and GitHub-audited at
`1e47307ef6097a965fd217eeb2a539b7020ee645` with CI run `26836402281` PASS and
Pages run `26836395093` PASS. The exactly-one authorized v3 secularism
compiled pilot then failed before Stage 07 at Stage 06 because
`normalized_activation_record.n_frame` was object-shaped rather than a
non-empty string. No final compiled output, Stage 08 sidecars, retained
promotion, release-regression cases, or row-family closure were produced. The
next owner-gated action was a no-model Stage 06 prompt/checker contract
repair. That repair keeps canonical `normalized_activation_record.n_frame`
scalar, moves selected/held detail to `n_frame_details`, rejects missing
selected tokens, and adds checker canaries. The repair was committed/pushed at
`21616e7aa7af27eedf7a7efc72152c1ef1f64bf1`; GitHub CI run
`26840561357` PASSed and Pages run `26840556953` PASSed. No second v3 retry
remains authorized. The exactly-one v4 secularism compiled pilot then ran,
reached Stage 07, and failed NLA semantic-faithfulness validation as
`B1_B2_PILOT_V4_NEGATIVE_STAGE07_VALIDATOR`. No Stage 08 sidecars, retained
promotion, release-regression cases, broad row closure, package/release, or
provenance action followed, and no second v4 retry is authorized in this lane.
The follow-up no-model Stage 07 repair adds manifest-enforced ACT/body-ref
partitioning and field_witness mirror prompt/canary coverage. Smoke A passed
before mutation, v4 Gemba preserved the `36` ACT rows over `14` unique body
refs failure shape, and Smoke B passed `19/19` no-model checks. The repair was
pushed/GitHub-audited, then the exactly-one v5 secularism pilot failed Stage 07
assembly on exact witness-token and target-size checks. This still does not open
Stage 08, retained promotion, release-regression cases, or broad row closure.

## Recommended Order

1. Treat Stage 03 routing-shape repair as complete for one scoped retry.
2. Treat Stage 04 ACT/body-ref handoff repair as complete for one scoped retry.
3. Treat Stage 05 MRP/reread/terminal-state handoff repair as complete for one
   scoped retry after its source-boundary checks and live CI pass.
4. Treat Stage 06 field_witness/NAR handoff repair as complete for one scoped
   retry after its source-boundary checks and live CI pass.
5. Treat the current Stage 07/08 proof-bundle path as one retained staged
   current-skill proof case only.
6. Use compiled-output mode for Brandolini-scale staged cases before attempting
   more proof-bundle smokes.
7. Choose a single row family deliberately; do not rerun the whole staged
   system as a broad smoke.
8. Treat the no-model Stage 06 `n_frame` prompt/checker repair as pushed and
   GitHub-audited at `21616e7aa7af27eedf7a7efc72152c1ef1f64bf1`. Treat the
   exactly-one v4 secularism compiled pilot as consumed and negative at Stage 07
   NLA validation; do not run a second v4 retry in this lane.
9. Treat the no-model Stage 07 ACT partition / field_witness mirror repair as
   pushed and GitHub-audited at
   `485cd260afe68048d92ea06ae40467f3a4d33fa2` with CI `26857597844` PASS and
   Pages `26857597394` PASS. The exactly-one v5 secularism compiled pilot then
   failed Stage 07 assembly on the exact witness token and target-size floor; do
   not enter Stage 8, retained promotion, or release-regression from v5.
10. Only after retained proof evidence accumulates across an owner-selected
   row family should row-family closure be considered.

## Boundaries

This plan does not open package, tag, upload, GitHub Release, provenance, D.8
cleanup, a broad model matrix, arbitrary natural-language-to-IR parsing, or a
guaranteed `T_lang` uptake claim.
