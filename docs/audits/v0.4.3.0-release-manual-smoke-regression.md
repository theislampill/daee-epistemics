# v0.4.3.0 Release / Manual Smoke Regression Audit

## Executive verdict

RELEASE ASSET STALE.

The current generated repo skill now restores the manual/default Trinitarian smoke contract in
Codex-hosted exact-file execution: readable governed output, canonical burden notation, non-empty
generated MRP ledger, `R(H,Delta)` / MRP resultants, closure witness, `T_lang`, Restorative
Response, and Closing Formulation all pass their live-output gates.

Earlier local candidate packages were useful diagnostic evidence, but they are no longer current
proof: the current stricter manual-smoke checker rejects the older 2026-05-21 generated/candidate
outputs. Current package-bound behavior remains unproven until a new package is built from the
committed source and rerun through the current Trinitarian smoke/checker set.

The downloaded GitHub Release v0.4.3.0 `.skill` asset remains stale and behaviorally fails the
same contract. It is structurally a valid skill archive, but its extracted `SKILL.md` is not the
current generated runtime surface and its smoke output regresses to ASCII burden notation,
route-only MRP, missing ledgers, missing generated-by provenance, and missing formal MRP route
fields.

## Goal

Audit the v0.4.3.0 manual smoke regression without treating static checker success as behavioral
proof. Required behavior is readable governed output plus formal reconstructibility: Noetic Field
execution banner, compact Layer A header, canonical notation, `B_LA` / `B_MRP` / `B_total`, MRP
inside `R(H,Delta)`, typed MRP resultants, generated-by burden provenance, closure witness with
MRP ledger, Restorative Response, and Closing Formulation.

## Skill/package surfaces tested

- Current generated repo skill: `skill/SKILL.md`; SHA256
  `B9D2B4177A358DCC67CCE182C852013815ED30E3FA6D81E2382EA604C0691FFA`; size
  `148167` bytes.
- Local candidate package currently present: `.release/candidate/daee-epistemics-v0.4.3.0.skill`;
  SHA256 `AB30B7D372BC9E9B87C7DF64B9B770FE92C1D41770282D98D84DBC7C21EBD2CE`; size
  `709662` bytes. This artifact is local/dirty and is not current behavior proof; no current
  package-bound Trinitarian smoke has passed the stricter 2026-05-23 checker set.
- Local candidate provenance: `.release/candidate/daee-epistemics-v0.4.3.0.provenance.json`;
  SHA256 `27C16192F71BB6D1BFCED41EAACAD20912E9583EEDCC8867DC11ED8E7CFF44F8`; validates with
  `tools/check_release_provenance.py`. It explicitly records `dirty_worktree: true`.
- Downloaded GitHub Release asset: `.release/verify/daee-epistemics-v0.4.3.0.skill`;
  SHA256 `A04CB35F696A1CBF5C6A2177B84DFC06A94A628E3172B3FB8E9C0E30E1176F94`; size
  `637876` bytes.
- Extracted release `SKILL.md`: `.release/verify/extracted-v0.4.3.0/SKILL.md`; SHA256
  `37EECDA99FDDA6F3BF1CB270A63CF6F227C923843B7A4BA9F150E1122B3FE191`; size `134878`
  bytes.
- Release archive structure: root contains `SKILL.md`; no `atomics/`, `.daee/`, `.git/`, or raw
  smoke outputs were found.

The release asset lacks current generated-skill contract strings such as
`NON-DROPPABLE DEFAULT MANUAL CONTRACT`, the latest `B_MRP` ledger requirements, the
`PsiA`/`PsiN` closure-symbol guard, and current route/closure syntax hardening.

## Smoke prompts

Prompt files were created under `tests/smokes/v0.4.3.0-release-regression/prompts/`:

- `01-trinitarian-j173.md`
- `02-tst-lillard.md`
- `03-khaybar.md`
- `04-doubt-churn-loopbreak.md`
- `05-hujjah-hold.md`

The Trinitarian prompt was run through isolated Codex-hosted runner subagents against the generated
repo skill, older rebuilt candidate packages, and the extracted release asset. Those older
candidate-package runs are now historical diagnostic evidence only; the current restored behavior
proof is the fresh current-skill lane under `.daee/progressive-disclosure-smokes/20260522-034000/`.
The release-asset run remains the stale-package proof.

## Raw output locations

Raw ignored outputs were captured under `.daee/release-regression-smokes/`, including:

- `20260521-144350/trinitarian-release-asset-exactfile.md`; SHA256
  `096811C15E5B27E29B92CA85A8837CC390D0494AED29819E4C30C8414CB16CA9`; size `27287`
  bytes; release asset behavior failure.
- `20260521-153214/trinitarian-generated-exactfile-psiN-final.md`; SHA256
  `37070B4F139506D3059B05EFDA1073AC20D7F4A3910ADA46D089F76B1C89F7F9`; size `38896`
  bytes; historical generated-skill diagnostic output, superseded by the 20260522-034000 proof lane
  and no longer a current checker pass.
- `20260521-bareguard-candidate/trinitarian-candidate-package-bareguard.md`; SHA256
  `690F593E29E2A57D2EAA87960E66E51CE0E495F61053652B175B08954DCDF635`; size `60456`
  bytes; historical candidate-package diagnostic output, now rejected by the stricter current
  manual-smoke checker.

Raw outputs remain local under `.daee/` and were not committed.

## Render contract results

Generated skill:

- Current proof is `.daee/progressive-disclosure-smokes/20260522-034000/trinitarian-current-skill.md`.
- It passes `check_manual_smoke_render_contract.py --outputs`, contains the execution banner,
  compact Layer A header, governed Layer B heading, canonical burden notation,
  Closure/Reconstruction Witness, Final Restorative Response, and Closing Formulation.
- It preserves the prior fixes for the `Layer B - Owner Submoves` heading regression and the
  `PsiA` closure-symbol substitution regression.

Release asset:

- `20260521-144350/trinitarian-release-asset-exactfile.md` fails the manual render checker.
- Missing or regressed: compact DSL/IR header shape, canonical burden notation, `B_LA`,
  `B_MRP`, `B_total`, `Route-gradient`, `MRP route result type`, target-explicit field
  diagnostics, LoopBreak status, held/generated distinction, generated-by marker, and generated
  non-baseline burden.

Candidate package:

- No current package-bound output passes the stricter 2026-05-23 manual-smoke checker set.
- Older candidate-package outputs remain useful for root-cause history, but they must not be used
  as release proof. Rebuild from committed source and rerun package-bound Trinitarian smoke before
  claiming package-bound behavior restored.

## MRP/formalism results

Generated skill:

- `B_LA = {1B..5B}` remains baseline input-present burden inventory.
- `B_MRP = {6B}` is generated after `Land(5B)` / `R(H,Delta)` through
  `generated_burden_instantiation`.
- Baseline transitions are typed as `held_burden_activation`.
- The generated heading appears as `6B [generated-by: MRP(5B)]`.
- Generated `6B` receives owner-backed Layer B submoves, lands, and then routes to STOP through
  a final MRP reread.
- `check_live_default_witness_contract.py`, `check_trinitarian_mrp_hotfix.py --outputs`,
  `check_mrp_generated_burden.py --outputs`, and `check_output_grapher.py --skill-output
  --expect-reconstructible` all pass against the fresh 20260522-034000 generated-skill output.

Release asset:

- The release-asset output can be parsed by Output Grapher as a legacy reconstructible shape,
  but it fails the v0.4.3.0 manual/default contract.
- It has only four baseline burdens, no generated `B_MRP`, no generated-by burden, and no
  route-gradient/result-type fields.

Candidate package:

- Current package-bound MRP/formalism proof is missing. Older package-bound outputs no longer pass
  the current stricter checker and are retained only as historical diagnostics.

## Meta-noetic viability

The generated exact-file output is viable/strong meta-noetic behavior: it tracks pressure
migration through grammar, distributive exclusivity, proof-text stack, eternal-life inference,
doctrine-immunity closure, and then generates the broader-system/mystery-immunity recoil as a
non-baseline burden.

The release asset output remains weaker: it contains disciplined refutation and some reread
language, but does not prove the current generated-burden contract.

## Adversarial falsification

The restored generated-skill smoke cannot be reduced to ordinary refutation with ornamental MRP:
it has a non-empty generated ledger, a generated-by chain, Layer B treatment for the generated
burden, terminal accounting, and closure-field witness.

The release surface is falsified as stale by both content/hash comparison and behavior: its smoke
output reproduces the manual-smoke regression class.

## Output Grapher acceptance

- Static `check_output_grapher.py` and `check_output_grapher_layout.py` pass.
- Generated-skill output `.daee/progressive-disclosure-smokes/20260522-034000/trinitarian-current-skill.md`
  parses as reconstructible with 6 burdens, 20 submoves, 6 MRP resultants, and 6 terminals.
- Candidate-package Output Grapher acceptance is not current proof; older candidate-package outputs
  are superseded by the stricter manual-smoke checker.
- Release-asset output parses as a legacy reconstructible shape, but fails the stricter
  v0.4.3.0 manual/default contract.
- No live browser/PNG section export was performed in this audit.

## Release asset status

RELEASE ASSET STALE.

No release asset was updated in this audit. The current worktree is dirty with local source,
checker, docs, and fixture changes; updating a GitHub Release asset from this dirty state would
not provide clean release provenance tied to a committed source revision.

A local candidate package and provenance file exist under `.release/candidate/`, but they are not
release assets and are not current behavior proof until the source changes are committed,
CI/provenance are green from that committed source, and a fresh package-bound Trinitarian smoke
passes the current checker set.

Local release/download/candidate artifacts are quarantined under ignored `.release/` storage so
they cannot be accidentally staged with source/checker/docs changes. This preserves the release
boundary: local packages are not public v0.4.3.0 assets and must be rebuilt from a committed source
revision before they can be used as release-correction evidence.

Fresh continuation validation on 2026-05-21 reran the full local runtime/render/formalism/docs
checker set after rebuilding the compiled runtime. The generated runtime, checker fixtures,
manual-smoke recurrence guard, MRP/despecialization checks, docs index checks, formalism bridge
checks, Output Grapher checks, candidate package provenance, and candidate package artifact shape
all passed at that time. This is now superseded by the stricter 2026-05-23 checker: the older
candidate-package Trinitarian outputs no longer pass the manual-smoke render contract. The current
line status is narrower and cleaner: current generated-skill behavior restored; current
package-bound behavior still needs fresh proof; public release asset still stale.

Commit-readiness hygiene check on 2026-05-21 found no tracked local release/smoke/package scratch
paths under `.release/`, `.daee/`, generated `skill/`, `smokes/`, `level3-runs/`, `tests/tmp/`,
`tests/output/`, or `tests/smoke-output/`. Untracked paths are limited to source-facing audit,
fixture, prompt, and checker additions. The new manual-smoke render checker is also wired into
both `.github/workflows/ci.yml` and `.github/workflows/release-skill.yml`, so the broken-output
shape is guarded in normal CI and release-skill validation.

Follow-up owner cleanup aligned stale MRP field-summary prose in `output-release.md`,
`diagnostic-render-contract.md`, and `TTP-MRP-mid-reread-pressure.md` with the current default
manual render shape: route-bearing `R(H,Δ): ...`, target-explicit
`Field diagnostics: ∇·B ...; ∇×κ ...`, literal `Route-gradient:`, typed MRP resultant, and graph
delta. The remaining `Reread: R(H,Delta)` owner hits are negative guards that forbid using that
legacy alias as the primary public/default surface.

After that owner cleanup, the local candidate package was rebuilt from the refreshed generated
`skill/` tree. A follow-up behavioral smoke exposed one more route-render weakness: the model
printed a bare `R(H,Δ):` heading before the actual route-bearing MRP reread line. The source
contract was hardened to forbid bare `R(H,Δ):` / `Reread: R(H,Δ)` headings as public/default
section labels, `tools/check_render_modes.py` now checks for the old field-list wording, and
`tests/manual-smoke-render/invalid/bare-reread-heading.md` guards the exact failure shape.

Historical note, superseded by the 2026-05-23 stricter checker: at this point in the audit, the
candidate archive was
`FC5C579A1A968862F3F553232F3DD71E4F61595D46B592DD9BBCC32DB060E90F` / `649296` bytes, with 20
entries and root `SKILL.md`. Local provenance was regenerated and passes
`tools/check_release_provenance.py`; it still records `dirty_worktree: true`, so it remains
candidate evidence only and cannot serve as final public release provenance.

An exact-file package-bound runner smoke was then run against the extracted bare-guard
candidate `SKILL.md`. The raw output
`.daee/release-regression-smokes/20260521-bareguard-candidate/trinitarian-candidate-package-bareguard.md`
is `690F593E29E2A57D2EAA87960E66E51CE0E495F61053652B175B08954DCDF635` / `60456` bytes. It passed
the then-current manual-smoke render contract, live default witness contract, closure-witness graph
check, Trinitarian MRP hotfix output check, and Output Grapher reconstructibility. Under the
current 2026-05-23 checker, it fails and is no longer release proof. Output Grapher parsed
6 burdens, 37 submoves, 6 MRP resultants, and 6 terminal states at the time. No bare `R(H,Δ):` or
`Reread: R(H,Δ)` heading lines remain. Line evidence includes the execution banner at line 1,
`𝔅_LA` at line 5, `𝔅_MRP` at lines 7 and 474, `𝔅_total` at lines 9 and 476, generated
instantiation at line 465, generated burden heading at line 478, closure witness at line 567,
closure ledgers at lines 571/573/575, generated MRP resultant at line 599, Restorative Response
at line 617, and Closing Formulation at line 670. This line evidence is historical only; the
current proof lane is the 20260522-034000 current-skill output.

Follow-up render-reference and baseline-ledger control:

- Old render/baseline control package:
  `C:\Users\theis\Downloads\daee-epistemics-v0.4.2.0(3).skill`; SHA256
  `21B25FF08AD36E26A57BACEC785C635F49DEC06E84A6EE191F4F8A61870913A7`; size `594097`
  bytes. Extracted `SKILL.md` SHA256:
  `C2459E2FCD0A70620051AF1819AD3F1B20F370C71380F86F272CC763DECF54A7`; size `78834`
  bytes.
- v0.4.2.0 control smoke output:
  `.daee/release-regression-smokes/20260521-closure-canonical-candidate/trinitarian-v0.4.2.0-render-control.md`;
  SHA256 `C22CEFCDE53473133A79292BE9714C9A4395C4402CDC3CD17705377E6E19C83B`;
  size `34698` bytes.
- Historical v0.4.3.0 local candidate package:
  `.release/candidate/daee-epistemics-v0.4.3.0.skill`; SHA256
  `72E904BCF6F95699F296486678728BA1173A90C365E21B64F8D75D5F219625FD`; size
  `649561` bytes. Archive validation passes with 20 entries, root `SKILL.md`, no
  `atomics/`, no `.daee/`, no `.git/`, and no raw smoke outputs.
- Historical v0.4.3.0 package-bound control smoke:
  `.daee/release-regression-smokes/20260521-closure-canonical-candidate/trinitarian-candidate-package-closure-canonical.md`;
  SHA256 `9B6D234525C1A82992F8AF23CE8A13B6D01D63C8448F5F0E7B10C68CDDA3AC5C`.

The v0.4.2.0 control output preserves the old public render quality: prefixed noetic-field banner
at line 2, compact Layer A header at line 10, burden sequence at lines 37/108/185/262, initial
baseline set at lines 309-313, and Closing Formulation at line 366. Because v0.4.2.0 is pre-MRP,
it has no `𝔅_MRP`, `generated_burden_instantiation`, or generated-by burden ledger.

Baseline ledger comparison against the same Trinitarian prompt:

| Burden role | v0.4.2.0 burden | v0.4.3.0 `𝔅_LA` burden | Same baseline? |
|---|---|---|---|
| word-order / only-true-God | `B1`: John 17:3 restrictive predicate and referent (lines 37, 310) | `¹B`: "only" placement / logical subject (line 27; `𝔅_LA` line 15) | yes |
| math analogy / analogy failure | within v0.4.2.0 `B1` submoves (lines 50-57) | `¹B` submoves preserve it, while `²B` carries the sender/sent grammar released after the initial predication landing (lines 45-53, 140) | yes, not generated |
| 1 John 5:20 / antecedent prooftext | `B2`: later prooftexts and attempted override (lines 108, 311) | `⁴B`: 1 John 5:20 cannot cancel John 17:3 (line 366) | yes |
| eternal-life inference | inside v0.4.2.0 `B2` prooftext override (lines 131-136) | `³B`: eternal-life/knowing-Jesus inference (line 253) | yes |
| John 1:1 / predication backstop | inside v0.4.2.0 `B2` prooftext override (lines 121-128) | `⁵B`: John 1:1 predication backstop (line 479) | yes |
| person/nature / doctrine model pressure | `B3`: Trinitarian model-switching (lines 185, 312) | `⁶B`: sacred-doctrine authority-order / model protection (line 582) | yes |

This comparison shows that v0.4.3.0 did not shrink Layer A to force generation. It preserved the
same baseline burden reading at finer granularity:

- v0.4.2.0: `𝔅_total = 𝔅_LA` conceptually, with baseline burdens B1-B4 and no MRP ledger.
- v0.4.3.0: `𝔅_LA = {¹B, ²B, ³B, ⁴B, ⁵B, ⁶B}` at lines 15 and 796;
  `𝔅_MRP = {⁷B [generated-by: MRP(⁶B)]}` at lines 17 and 798;
  `𝔅_total = 𝔅_LA ∪ 𝔅_MRP` at lines 19 and 800.
- v0.4.3.0 `⁷B` is absent from `𝔅_LA`, generated by `MRP(⁶B)` at lines 677 and 685,
  worked in Layer B with canonical submoves at lines 697-757, landed and reread before closure,
  and recorded in terminal/MRP ledgers at lines 824 and 852.
- Baseline burdens route through `held_burden_activation`; none of the v0.4.2.0/v0.4.3.0
  baseline burden roles are relabeled as generated.

After a closure-witness parser repair for blank-separated graph rows, the historical v0.4.3.0
package-bound output passed the then-current checks:

- `tools/check_manual_smoke_render_contract.py --outputs <latest-output>`
- `tools/check_live_default_witness_contract.py <latest-output>`
- `tools/check_closure_witness_graph.py --input <latest-output>`
- `tools/check_trinitarian_mrp_hotfix.py --outputs <latest-output>`
- `tools/check_output_grapher.py --skill-output <latest-output> --expect-reconstructible`

Output Grapher parsed that historical v0.4.3.0 output as reconstructible with 7 burdens,
53 submoves, 7 MRP resultants, and 7 terminal states. The remaining `B1_1`-style mentions were
canonical-first parenthetical parser aliases such as `¹B₁[M9] (B1_1[M9])`; there are zero
ASCII-primary submove headings. This is not current proof under the stricter 2026-05-23
manual-smoke checker.

Remaining gate before a public v0.4.3.0 asset correction:

1. Commit the source/checker/docs/fixture changes from this dirty worktree.
2. Push and require CI/release-skill validation to pass from the committed source.
3. Rebuild the `.skill` asset from generated `skill/` at that committed revision.
4. Produce clean provenance that names the committed source revision, tag state, package SHA256,
   checks run, and confirms no `.daee/`, `.release/`, `atomics/`, raw smoke outputs, or repo root
   wrapper were packaged.
5. Only then replace the stale GitHub Release v0.4.3.0 asset and update release body/provenance.

## Broken-output recurrence guard

Added/strengthened:

- `tools/check_manual_smoke_render_contract.py`
- invalid fixture for route-only Trinity regression
- invalid fixture for question-mark notation regression
- invalid fixture for empty `B_MRP` hard-smoke closure
- invalid fixture for under-inventory forced generation
- invalid fixture for `Layer B - Owner Submoves` heading regression
- invalid fixture for `PsiA` / agent-symbol closure substitution
- invalid fixture for wrong formal glyphs (`𝓑`/`𝓒` instead of `𝔅`/`𝒞`)
- integration through `tools/check_reconstructibility_and_mrp.py`
- CI/release workflow entries for the new checker

Source-owner hardening touched the MRP/render/closure contracts and Output Grapher parser support.

## Failure analysis

- stale package: confirmed for GitHub Release asset
- wrong skill surface: release asset differs from current generated repo skill
- banner/header regression: release asset fails; generated repo skill restored
- canonical notation regression: release asset fails; generated repo skill restored
- MRP ornamental/traversal confusion: release asset fails
- missing `B_LA/B_MRP/B_total`: release asset fails
- missing route-gradient/result type/generated-by: release asset fails
- generated behavior: current generated skill produces non-empty `B_MRP`
- Output Grapher parse/render: current generated and candidate-package outputs parse; live PNG
  export not tested

## Evidence boundary

This audit proves Codex-hosted behavior for the exact generated repo `skill/SKILL.md` and the
exact extracted rebuilt candidate package `SKILL.md`, and falsifies the exact extracted downloaded
release `SKILL.md`. It does not prove user-local model behavior, live GitHub Pages rendering, or
sectioned PNG export.

## 2026-05-22 continuation: current-smoke / Output Grapher / sectioned PNG gate

This continuation keeps the public release asset blocked. No package, release upload, commit, tag,
or asset replacement was performed.

The prior "live PNG export not tested" evidence boundary is superseded for local browser execution:
the Output Grapher sectioned PNG path was exercised against fresh current-skill outputs and the
resulting ZIP manifests/PNGs were inspected. This does not make the public release asset current;
it proves the local docs/Grapher path after the source-owned parser/export fixes below.

### Source-owned fixes added in this continuation

- `docs/index/output-grapher.js`
  - Corrected generated-burden provenance parsing so a standalone
    `generated-by: MRP(ⁿB)` line marks the current generated burden, not the source burden.
  - Prevented `Land(...)` text inside `Contribution-to-Land(...)` from terminating submove body
    capture early.
  - Expanded the browser-side mass gate from a closed FPD/M8/M9/P7-style family into catalogue-wide
    owner-specific operation recognition plus a source-owned fallback requiring mechanism/action,
    state change, and contribution.
  - Added adaptive section-PNG target width for very tall formal sections, so sectioned exports stay
    under browser canvas safety limits without clipping.
- `tools/check_output_grapher.py`
  - `--skill-output` now also runs the browser-side JS parser and fails on JS-only parser errors.
    This guards against Python/JS parser drift where a smoke looked reconstructible in Python but
    the actual export path rejected the generated burden as mass-insufficient.

### Fresh current-skill smoke evidence

Fresh outputs were produced under:

`.daee/progressive-disclosure-smokes/20260522-014500/`

Cases:

- `trinitarian-current-skill.md`
- `tst-lillard-current-skill.md`
- `khaybar-current-skill.md`
- `doubt-churn-current-skill.md`
- `hujjah-hold-current-skill.md`
- `secular-moral-realism-current-skill.md`

All six passed the required current-smoke gates after the browser parser recurrence guard was added:

- `python tools\check_manual_smoke_render_contract.py --outputs <six fresh outputs>` — PASS
- `python tools\check_mrp_generated_burden.py --outputs <six fresh outputs>` — PASS
- `python tools\check_live_default_witness_contract.py <six fresh outputs>` — PASS
- `python tools\check_output_grapher.py --skill-output <each fresh output> --expect-reconstructible` — PASS

The Output Grapher gate now reports browser-parser reconstructibility, not only Python-side
reconstructibility. Non-failing warnings remain for some legacy aliases in smoke prose, for example
`R(H,Delta)` and `B6`; these are warnings, not hard contract failures.

### Sectioned PNG export evidence

Section ZIPs were exported from the real browser path under:

`.daee/output-grapher-graphs/20260522-014500-sectioned-v3/`

Files:

- `trinitarian-sectioned.zip`
- `tst-lillard-sectioned.zip`
- `khaybar-sectioned.zip`
- `doubt-churn-sectioned.zip`
- `hujjah-hold-sectioned.zip`
- `secular-moral-realism-sectioned.zip`

Manifest/PNG inspection found:

| Case | PNG sections | Required section types | Parser errors | Foreign overlap | Unsafe canvas | Max PNG height |
|---|---:|---|---:|---:|---:|---:|
| Trinitarian | 9 | intro, burden, restoration, formal | 0 | 0 | 0 | 12152 |
| TST/Lillard | 8 | intro, burden, restoration, formal | 0 | 0 | 0 | 27984 |
| Khaybar | 9 | intro, burden, restoration, formal | 0 | 0 | 0 | 27985 |
| doubt-churn | 8 | intro, burden, restoration, formal | 0 | 0 | 0 | 14932 |
| hujjah-hold | 8 | intro, burden, restoration, formal | 0 | 0 | 0 | 27998 |
| secular moral realism | 9 | intro, burden, restoration, formal | 0 | 0 | 0 | 10811 |

Inspection also confirmed for all six exports:

- PNG count equals manifest section count.
- Required semantic section types are present.
- `parserVerdict.reconstructible=true` and manifest parser errors are empty.
- `hasRestorativeResponse=true`.
- `hasClosingFormulation=true`.
- `hasFormalCaseFill=true`.
- `hasLegend=true`.
- `hasFinalBurden=true`.
- No foreign-section overlap.
- No exported PNG exceeds the canvas safety limits.
- The restoration section title preserves Restorative Response and Closing Formulation.

### Steer consolidation status update

| Intent / steer | Status | Evidence | If incomplete, required next action |
|---|---|---|---|
| Public/default manual render contract | DONE for current local generated behavior | Six fresh current-skill outputs pass `check_manual_smoke_render_contract.py --outputs`; Trinitarian output includes banner, Layer A, canonical burdens, ledgers, MRP, Closure Witness, Restorative Response, and Closing Formulation | Keep release blocked until committed-source/package proof is rerun |
| MRP formalism and generated burden provenance | DONE for current local generated behavior | Six fresh outputs pass `check_mrp_generated_burden.py --outputs`; Output Grapher parses generated B_MRP provenance without baseline backfill | Commit and rerun CI/release-skill gates before release asset update |
| Catalogue-wide owner activation / code lookup is not activation | DONE for current Grapher/checker path | Browser parser now checks owner-specific operation activation and is wired into `check_output_grapher.py --skill-output` | Extend further only if source catalogue adds new owner families with new operation shapes |
| Brandolini non-terseness for baseline and generated burdens | DONE for fresh current-smoke behavior | Manual smoke checker and browser Output Grapher mass gate pass all six current outputs; generated Trinitarian/TST/Khaybar/secular burdens contain paragraph-level operation bodies | Keep invalid fixtures/checker gates in CI |
| Closure Witness vs `field_witness` agreement | DONE for current local generated behavior | `check_live_default_witness_contract.py` passes all six fresh outputs | Commit and rerun from clean source |
| Output Grapher B_LA/B_MRP/body preservation | DONE for local docs/Grapher path | Static Grapher checks pass; `--skill-output` browser parser passes all six; sectioned exports preserve burden/restoration/formal sections | Rebuild package only when packaging is allowed |
| Sectioned PNG export proof | DONE for local browser path | Six section ZIPs exported and manifest/PNG inspection passed with no parser errors, no overlap, no unsafe canvas | None for local browser path |
| Downloaded public v0.4.3.0 release asset | BLOCKED / STALE | Existing audit still falsifies downloaded asset as stale; no replacement performed | Commit source, pass CI/release-skill validation, rebuild package from committed source, produce clean provenance, then update asset only when explicitly authorized |
| Current package-bound proof after latest Grapher/doc changes | NEEDS BEHAVIOR PROOF | Older 2026-05-21 generated/candidate-package outputs no longer pass the current stricter manual-smoke checker; no new package was built after the latest Grapher/checker/export fixes because package/release work remains blocked | When packaging is authorized, rebuild candidate from committed source and rerun package-bound Trinitarian smoke/checkers |

### Current release blockers

1. Source/checker/docs/fixture worktree is still dirty and uncommitted.
2. Public v0.4.3.0 release asset remains stale.
3. The latest browser/Grapher fixes have not been packaged into a new candidate package because this
   continuation intentionally did not package or release.
4. Clean provenance tied to a committed source revision is still missing.
5. CI/release-skill validation from committed source has not yet been run.

### Next smallest safe action

Prepare the dirty worktree for review/commit by summarizing the source-owned diffs and running the
full local CI-equivalent checker set from the current tree. Do not rebuild or upload a public
release asset until the source is committed and the release correction gate is explicitly opened.

## 2026-05-23 continuation: restart recovery / current gate refresh

This continuation followed a local Windows restart/account-lockout recovery. The launcher failure
was treated as credential/account state, not as repository evidence. No reset, patch discard,
package, release upload, commit, or tag was performed.

### Local identity/runtime check

```powershell
whoami            # 3701media\theis
python --version  # Python 3.11.9
node --version    # v24.14.0
git status --short
```

The worktree remained dirty with the expected source/checker/docs/fixture changes.

### Source-owned encoding fix

The full local gate initially exposed one fresh source issue:

- `python tools\check_encoding_hygiene.py` failed on `docs/index/output-grapher.js` line 686
  because a submove-heading regression regex contained mojibaked superscript/subscript character
  classes.

Fix:

- `docs/index/output-grapher.js` now uses explicit Unicode escapes for the same regex class:
  superscript digits, subscript digits, and `ᵢ` are matched without mojibake bytes.
- `python tools\build_docs_index.py` regenerated `docs/index.html` and
  `docs/daee-epistemics-pipeline.html`.

### Full local source/checker gate after the fix

The current tree then passed the local CI-equivalent source/checker set:

```powershell
python tools\build_framework_pipeline.py
python tools\build_compiled_runtime.py
python tools\build_docs_index.py --check
python tools\check_compiled_runtime_freshness.py
python tools\check_level3_data_shapes.py --include-generated
python tools\check_package_shape.py
python tools\check_compiled_module_boundaries.py
python tools\check_stub_integrity.py
python tools\check_consolidation_call_budget.py
python tools\check_routing_parity.py
python tools\check_routing_parity.py --strict
python tools\check_recursive_traversal_governance.py
python tools\check_render_modes.py
python tools\check_frontmatter.py
python tools\check_frontmatter.py --contract-version 0.4.0.0
python tools\check_coverage.py
python tools\check_framework_pipeline.py
python tools\check_recursion_collapse_noetic_frame.py
python tools\check_metacompliance_current_canon.py
python tools\check_register_formalism_bridge.py
python tools\check_reconstructibility_and_mrp.py
python tools\check_manual_smoke_render_contract.py
python tools\check_closure_witness_graph.py --input tests\live-witness-fixtures\valid\closure-witness-dependency-graph.md --field-witness tests\live-witness-fixtures\valid\closure-witness-dependency-graph.field_witness.json
python tools\check_mid_reread_pressure.py
python tools\check_mrp_generated_burden.py
python tools\check_mrp_route_invariants.py
python tools\check_concealment_mode.py
python tools\check_trinitarian_mrp_hotfix.py
python tools\check_ttp_operator_contracts.py --strict
python tools\check_spec_authoring_pack.py
python tools\check_docs_index_interactions.py
python tools\check_field_operator_architecture.py
python tools\check_live_default_witness_contract.py tests\live-witness-fixtures\valid\closure-witness-dependency-graph.md
python tools\check_smoke_artifacts.py
python tools\check_ir_instance_integrity.py
python tools\check_diagnostic_ir_catalogue_integrity.py
python tools\check_encoding_hygiene.py
python tools\check_mojibake.py
python tools\check_output_grapher.py
python tools\check_output_grapher_layout.py
node --check docs\index\output-grapher.js
python -m py_compile tools/*.py
git diff --exit-code -- atomics/skill/references/diagnostics/framework-pipeline.md
git diff --check
```

Only line-ending warnings were reported by Git during `git diff --check`; the command exited `0`.

### Fresh 20260522-034000 current-skill proof

Fresh current-skill outputs under `.daee/progressive-disclosure-smokes/20260522-034000/` are now
the current behavioral proof lane. The exact Trinitarian output:

`.daee/progressive-disclosure-smokes/20260522-034000/trinitarian-current-skill.md`

passes:

- `python tools\check_manual_smoke_render_contract.py --outputs <trinitarian-current-skill>`
- `python tools\check_mrp_generated_burden.py --outputs <trinitarian-current-skill>`
- `python tools\check_live_default_witness_contract.py <trinitarian-current-skill>`
- `python tools\check_trinitarian_mrp_hotfix.py --outputs <trinitarian-current-skill>`
- `python tools\check_output_grapher.py --skill-output <trinitarian-current-skill> --expect-reconstructible`

Output Grapher reconstructs the Trinitarian output with `6` burdens, `20` submoves, `6` MRP
resultants, and `6` terminal states. The remaining legacy-alias warnings are non-failing parser
normalization warnings, not public-contract failures.

All six fresh `20260522-034000` current-skill outputs also pass the manual-smoke, generated-MRP,
live default witness, and Output Grapher reconstructibility gates.

### Fresh 20260522-034000 sectioned PNG export proof

Section ZIPs were exported from the real browser path and inspected under:

`.daee/output-grapher-graphs/20260522-034000-sectioned-proof/`

| Case | PNG sections | Required section types | Parser errors | Foreign overlap | Unsafe canvas | Max PNG height |
|---|---:|---|---:|---:|---:|---:|
| Trinitarian | 9 | intro, burden, restoration, formal | 0 | 0 | 0 | 16333 |
| TST/Lillard | 9 | intro, burden, restoration, formal | 0 | 0 | 0 | 19107 |
| Khaybar | 9 | intro, burden, restoration, formal | 0 | 0 | 0 | 27985 |
| doubt-churn | 6 | intro, burden, restoration, formal | 0 | 0 | 0 | 27994 |
| hujjah-hold | 7 | intro, burden, restoration, formal | 0 | 0 | 0 | 19752 |
| secular moral realism | 7 | intro, burden, restoration, formal | 0 | 0 | 0 | 27996 |

Inspection confirmed matching manifest/PNG counts, required semantic section types,
`parserVerdict.reconstructible=true`, no parser errors, no foreign-section overlap, and no unsafe
canvas sections.

### Stale release asset still falsified

The exact downloaded-release output:

`.daee/release-regression-smokes/20260521-144350/trinitarian-release-asset-exactfile.md`

still fails the current manual-smoke render contract. Representative failures include missing
compact Layer A / DSL-IR header, missing canonical burden notation, missing `𝔅_LA`, `𝔅_MRP`, and
`𝔅_total`, missing `Route-gradient:`, missing `MRP route result type:`, missing generated-by
burden provenance, and missing normal `field_witness` / graphable reconstruction payload.

### Stale candidate-output boundary

Older 2026-05-21 outputs that this audit previously described as passing, including:

- `.daee/release-regression-smokes/20260521-153214/trinitarian-generated-exactfile-psiN-final.md`
- `.daee/release-regression-smokes/20260521-bareguard-candidate/trinitarian-candidate-package-bareguard.md`
- `.daee/release-regression-smokes/20260521-closure-canonical-candidate/trinitarian-candidate-package-closure-canonical.md`

do **not** pass the current stricter manual-smoke checker. They remain historical diagnostic
artifacts only and must not be used as current proof. Current package-bound behavior proof remains
open until a new package is built from committed source and a package-bound Trinitarian smoke is
rerun under the current checker set.

### 2026-05-23 completion-audit matrix

| Requirement from objective | Current evidence | Status |
|---|---|---|
| Diagnose exact bad manual output and stale packaged skill asset | Downloaded release asset `.release/verify/daee-epistemics-v0.4.3.0.skill` is unchanged; release-output smoke `.daee/release-regression-smokes/20260521-144350/trinitarian-release-asset-exactfile.md` still fails the current manual-smoke checker | Proven stale |
| Patch source-owned runtime/render/checker owners, not tests only | Source/runtime owners, Output Grapher parser/export code, closure/witness libraries, and checker/workflow gates are dirty in the worktree; full local gate passes after rebuild | Proven for current tree |
| Real controlled Trinitarian smoke proves public/default manual contract | `.daee/progressive-disclosure-smokes/20260522-034000/trinitarian-current-skill.md` passes manual render, MRP generated-burden, live default witness, Trinitarian MRP hotfix, and Output Grapher checks | Proven for current generated skill |
| Manual output proves canonical header, notation, ledgers, generated burden, closure witness, Restorative Response, and Closing Formulation | Covered by the current manual-smoke checker and fresh Trinitarian pass; Output Grapher reconstructs 6 burdens, 20 submoves, 6 MRP resultants, and 6 terminals | Proven for current generated skill |
| Six-output current-smoke regression coverage | Six fresh `20260522-034000` outputs pass manual-smoke, generated-MRP, live witness, and Output Grapher reconstruction gates | Proven for current generated skill |
| Sectioned PNG export path does not drop body/restoration/formal material | Six real browser-exported ZIPs under `.daee/output-grapher-graphs/20260522-034000-sectioned-proof/` pass manifest/PNG inspection | Proven for local docs/Grapher path |
| Current package-bound behavior proof | Older 2026-05-21 candidate-package outputs fail the stricter current checker; no new package-bound Trinitarian smoke has been generated from committed source | Missing |
| Release update stays blocked until proof/provenance is clean | No package, release upload, tag, commit, or public asset update was performed in the 2026-05-22/23 continuations | Block preserved |

Completion judgment: do not mark the objective complete yet. The current generated-skill behavior
is repaired and strongly verified, but current package-bound proof and clean committed-source
provenance are still missing.
