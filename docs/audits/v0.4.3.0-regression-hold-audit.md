# v0.4.3.0 Regression Hold Audit

## Executive Verdict

v0.4.3.0 WITHDRAWN; WORK PRESERVED ON PR BRANCH

## Reason For Hold

Smoke comparison showed v0.4.3.0 regressed against v0.4.2.0 in:

- first-visible governed-execution surface / box art;
- Mid-Reread Pressure triggering;
- submove use.

The regression is release-blocking. v0.4.3.0 is not live/ship-ready until the behavioral gap is diagnosed and corrected with fresh smoke evidence.

## Evidence

Compared local smoke files:

- `C:\Users\theis\Downloads\v.4.2.0 smoke.md`
- `C:\Users\theis\Downloads\v.4.3.0 smoke.md`

v0.4.2.0:

- recognizable `NOETIC FIELD EXECUTION` first-visible governed-execution box;
- stronger Layer A / Layer B governed structure;
- visible submove/burden execution;
- active noetic re-read progression.

v0.4.3.0:

- plain `field:` first surface;
- flatter burden rendering;
- Mid-Reread Pressure not visibly triggering in the comparison;
- submove use appears lost/flattened.

## Preservation Branch

Branch:

```text
pr/v0.4.3.0-regression-hold
```

Preserved commits include:

```text
ab1d33f runtime: prepare v0.4.3.0 SKILL entrypoint cleanup
75cce53 v0.4.3.0 RC1: closure reconstructibility and reread pressure
9bb67f5 v0.4.3.0: package-bound release prep
8af69f2 ci: stabilize docs index provenance digest
c0952f8 fix: clean v0.4.3.0 release mojibake
fffa550 docs: refresh v0.4.3.0 release metadata
```

The branch preserves the closure witness graph, `field_witness`, visualizer/checker/fixture, and Mid-Reread Pressure work for diagnosis. It is not intended for merge as-is.

## Main / Live State

`main` is being restored by normal revert, not by destructive history rewrite. The intended live baseline after this rollback is the v0.4.2.0-safe runtime line before the v0.4.3.0 entrypoint cleanup and closure/MRP bundle.

## Release / Tag State

- v0.4.3.0 GitHub Release exists but is marked prerelease and retitled `v0.4.3.0 - WITHDRAWN / REGRESSION HOLD`.
- v0.4.3.0 release notes now begin with a withdrawal/regression-hold warning.
- v0.4.3.0 assets remain attached as quarantine evidence; no fixed asset was uploaded.
- v0.4.3.0 tag is retained for provenance but is not a ship-ready release signal.
- v0.4.2.0 tag/release remains the safe public baseline.

## Checks Run

Final rollback validation in this commit:

```text
python tools\build_framework_pipeline.py -> PASS
python tools\build_compiled_runtime.py -> PASS
python tools\build_docs_index.py -> PASS
python tools\check_compiled_runtime_freshness.py -> PASS
python tools\build_docs_index.py --check -> PASS
python tools\check_docs_index_interactions.py -> PASS
python tools\check_ttp_operator_contracts.py --strict -> PASS
python tools\check_field_operator_architecture.py -> PASS
python tools\check_ir_instance_integrity.py -> PASS
python tools\check_spec_authoring_pack.py -> PASS
python tools\check_render_modes.py -> PASS after restoring the NOETIC FIELD EXECUTION banner invariant
python tools\check_smoke_artifacts.py -> PASS
git diff --check -> PASS with line-ending warnings only for generated docs HTML
```

`check_render_modes.py` initially failed after the broad v0.4.3.0 revert because the source still
demoted the first-visible governed surface to a naked `field:` line. The rollback commit restores
`NOETIC FIELD EXECUTION` as the normal governed surface and keeps plain `field:` as a transport
fallback only.

## Diagnosis Follow-Up

Future diagnosis should determine:

1. which v0.4.3.0 change flattened the first-visible output surface;
2. why Mid-Reread Pressure was registered/checker-passing but not behaviorally triggering;
3. why submove surfacing disappeared;
4. whether closure graph / `field_witness` machinery over-compressed default render;
5. whether checkers proved structure without proving live behavioral activation;
6. how to preserve reconstructibility without sacrificing governed execution surface.

## Plan Closure

| Item | Status | Evidence | Follow-up |
|---|---|---|---|
| v0.4.3.0 work preserved | done | `pr/v0.4.3.0-regression-hold` pushed to origin | Diagnose there before any replacement release. |
| main restored | done | Normal revert staged from `fffa550` back through the v0.4.3.0 bundle, plus rollback-scoped banner restoration | Commit and push after checks pass. |
| release blocked/withdrawn | done | GitHub Release retitled and marked prerelease/withdrawn | Do not upload a fixed asset until diagnosis passes. |
| tag state resolved | changed | v0.4.3.0 tag retained for provenance, release marked withdrawn | Delete tag only if maintainer later decides provenance should be removed. |
| deterministic checks | done | Final rollback validation commands above passed | Re-run on CI after push. |
| smoke comparison recorded | done | Local compared smoke files named above | Keep raw smoke files outside repo. |
| PR/draft branch ready | done | Branch pushed; no PR opened | Open draft PR only if owner requests it. |
| future diagnosis task | deferred | Diagnosis follow-up listed above | Promote from audit into an explicit implementation task later. |
