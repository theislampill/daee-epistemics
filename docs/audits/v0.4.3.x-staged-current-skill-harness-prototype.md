# v0.4.3.x Staged Current-Skill Harness Prototype

Status: `STAGED_CURRENT_SKILL_HARNESS_LIVE_CI_PASS; STAGE_03_ROUTING_HANDOFF_REPAIRED_ONE_CASE_PASS; STAGE_04_ACT_HANDOFF_REPAIRED_ONE_CASE_PASS; STAGE_05_MRP_TERMINAL_HANDOFF_LIVE_CI_PASS; STAGE_06_FIELD_WITNESS_NAR_HANDOFF_LIVE_CI_PASS; STAGE_07_RELEASE_OUTPUT_CONTRACT_LOCAL_PASS_WITH_NEGATIVE_RETRY_EVIDENCE; STAGE_07_RETRY_V2_NEGATIVE_FIELD_WITNESS_CONVERGENCE_EVIDENCE; STAGE_07_FIELD_DIAGNOSTIC_CONVERGENCE_CONTRACT_LOCAL_PASS; STAGE_07_CONVERGENCE_RETRY_V1_NEGATIVE_STAGE06_NAR_SHAPE_EVIDENCE; STAGED_ONE_CASE_RETAINED_PROOF_BUNDLE_PASS; STAGED_LONG_OUTPUT_COMPILER_NO_MODEL_PASS; COMPILED_STAGE07_SURFACE_REPAIR_NO_MODEL_PASS; COMPILED_PILOT_RETRY_STAGE05_ANDON_RECORDED; STAGE04_STAGE05_HANDOFF_REPAIR_NO_MODEL_PASS; REAL_V2_REPAIR_VERIFICATION_CHECKER_PASS; B1_B2_PILOT_V3_NEGATIVE_PRE_STAGE_FAILURE; STAGE06_NFRAME_REPAIR_NO_MODEL_LIVE_CI_PASS; B1_B2_PILOT_V4_NEGATIVE_STAGE07_VALIDATOR`.

Date: 2026-06-02

This packet records the first bounded staged current-skill harness prototype
after the retained replay bridge. It is repo/dev tooling only. It does not add a
new public user mode and does not replace the normal `/daee-epistemics` answer.

## Boundary

The harness boundary is:

```text
input.md
-> stage prompts
-> stage artifacts
-> handoff checker
-> composed output.md
-> graph/certificate/Grapher sidecars
-> optional B.5.4.1 checker-owned full-IR sidecar
-> retained replay comparison
```

Three layers are kept distinct:

- retained replay = no-model target shape from existing retained proof;
- staged current-skill harness = model fills bounded stages;
- final verifier sidecar = proof gate after composed output.

The public surfaces remain:

```text
/daee-epistemics
/daee-epistemics:dsl
/daee-epistemics < input.md > output.md
```

The staged harness writes scratch artifacts under ignored `.daee/`. Failed
staged runs are negative evidence, not proof. `.daee` staged artifacts remain
ignored unless later promoted through the normal retained proof-corpus gates.

## Implemented Tooling

Added:

- `tools/run_staged_current_skill_smoke.py`
- `tools/compare_staged_runtime_replay.py`

Live source-boundary evidence:

- Source commit `9f5f4d64c2e76537d62ad2cbe8e7b478ff162c78`.
- GitHub CI run `26799711315`: PASS.
- GitHub Pages run `26799710840`: PASS.

The runner has two modes:

- `python tools/run_staged_current_skill_smoke.py --self-test`
- `python tools/run_staged_current_skill_smoke.py --case-name ...`

Stage 07 now has two release-output production modes:

- `--release-output-mode single-output` or `single`: current behavior; the
  model writes final `output.md` in one release-output invocation.
- `--release-output-mode compiled` or `compiled-output`: the model writes
  bounded Stage 07 section artifacts and
  `tools/build_staged_governed_output.py` assembles hash-checked sections into
  final `output.md` before the existing Stage 07 validators run.

The compiled mode accepts `--target-output-kb <n>` for Brandolini-scale cases.
The assembler now treats that value as a floor when it is written into the
assembly manifest. It also enforces compiled Stage 07 roles:
`visible_opening`, `layer_a_diagnostic_ir`, `layer_b_act`,
`mrp_reread_terminal`, `field_witness_nar`, `restorative_response`, and
`closing_formulation`. It does not prove output quality by assembly alone. The
compiled `output.md` must still pass visible-output, NLA, field-witness
convergence, formal reread, graph-completeness, manual render, and
owner-activation checks before Stage 08 sidecars can run.

The self-test is no-model and deterministic. It validates that the harness can
create a run directory, write stage artifacts, bind to the retained replay
record, validate the staged handoff record with
`tools/check_staged_runtime_handshake.py`, and write a hash record without
claiming model behavior. It also covers compiled Stage 07 mode without a model
by splitting the retained replay output into hash-checked sections, assembling
the compiled `output.md`, and running the existing Stage 07 validators against
that compiled result.

Compiled pilot negative evidence, 2026-06-02:

```text
python tools/run_staged_current_skill_smoke.py --case-name staged-secularism-proofbundle-pilot --raw-input-path .daee\staged-row-family-smokes\20260602-secularism-pilot\raw-input.md --replay-record tests\staged-runtime-handshake\valid\retained-a9-science-source.json --run-dir .daee\staged-row-family-smokes\20260602-secularism-pilot --model gpt-5.5 --release-output-mode compiled --target-output-kb 70
```

Result:

```text
staged current-skill smoke: FAIL
failure: stage-07-release-output: visible governed output validation failed:
- visible noetic-field opening/header
- parser-stable field_witness
- normalized_activation_record / NAR evidence
```

The compiler assembled all eight section artifacts and wrote a 25,632-byte
`output.md`, but Stage 07 rejected the public surface before sidecars. No Stage
08 sidecars were built, no retained promotion ran, and no rows are claimed from
the pilot. The next narrow harness repair is to make compiled Stage 07 section
prompts/fixtures preserve exact validator-visible public-surface tokens.

Compiled Stage 07 repair and v2 retry, 2026-06-02:

- `tools/build_staged_governed_output.py --self-test`: PASS with a
  `386,053` byte large fixture, a 100KB+ fixture, public-surface canaries,
  duplicate-role canary, and under-target canary.
- `tools/run_staged_current_skill_smoke.py --self-test`: PASS with compiled
  Stage 07 coverage against the retained replay output.
- The single authorized retry
  `staged-secularism-proofbundle-pilot-v2` failed before Stage 07. Stage 04
  emitted descriptive `act_burdens` strings while Stage 05 terminal states used
  canonical `B1`-`B5`, producing
  `stage-05 terminal_states missing ACT burden(s)`.
- No final compiled `output.md`, Stage 07 validators, Stage 08 sidecars,
  retained promotion, or release-regression matrix cases followed from v2.
- Next narrow harness repair is Stage 04/Stage 05 canonical burden-id handoff
  normalization before any future pilot authorization.

The live run mode invokes `codex exec` with read-only sandboxing for stages
01-07, then uses checker-owned builders for stage 08 when a final output exists.
It stops on the first mandatory handoff failure and does not compose a final
proof output after a failed stage.

The comparator reports mismatch against the retained
`a9-science-source` replay shape:

```text
tests/staged-runtime-handshake/valid/retained-a9-science-source.json
```

## One Focused Staged Smoke

Run:

```text
python tools/run_staged_current_skill_smoke.py --case-name staged-a9-science-source --raw-input-path tests/retained-proof-corpus/v0.4.3.0-schema-light/valid/sidecar-backed/cases/a9-science-source/input.txt --replay-record tests/staged-runtime-handshake/valid/retained-a9-science-source.json --run-dir .daee/staged-current-skill-smokes/20260602-a9-science-source-v1 --model gpt-5.5
```

Result:

```text
STAGED_MODEL_HARNESS_NEGATIVE_EVIDENCE_STAGE_03
```

The run completed stages 01 and 02, then stopped at stage 03 because the model
returned `route_targets` as richer route objects rather than the staged contract
target list. The retained replay target expects:

```json
["B1"]
```

The model returned a list containing an object with `burden_id`, route target,
register types, pressure classes, and route basis. That is useful diagnostic
content, but it does not satisfy the current staged handoff contract because
stage 03 `route_targets` must match the stage 02 `burden_floor` exactly.

No final `output.md` was composed, no proof sidecars were built, and no retained
corpus promotion occurred.

Ignored artifact references:

- `.daee/staged-current-skill-smokes/20260602-a9-science-source-v1/records/staged-handoff-failure.json`
- `.daee/staged-current-skill-smokes/20260602-a9-science-source-v1/staged-smoke.hashes.json`
- `.daee/staged-current-skill-smokes/20260602-a9-science-source-v1/replay-comparison.md`

Replay comparison recorded eight mismatches: stage order stopped after stage
03, stage 03 route target shape differed, stage 03 owner-route shape differed,
and stages 04-08 were absent because the harness stopped honestly.

## Stage 03 Repair Follow-Up

The follow-up repair keeps the canonical handoff field strict while preserving
useful routing prose as machine-facing metadata:

- `route_targets` remains a burden-id list used by the handoff checker;
- optional `route_target_details` carries richer route metadata;
- object-shaped model `route_targets` are normalized only when every object has
  a clear string `burden_id`;
- model-mode staged records are scoped with `model_scope` and model-specific
  non-claims instead of pretending to be no-model fixtures;
- `--stop-after-stage` supports a stage-local retry without composing final
  output or verifier sidecars.

New no-model contract evidence:

- `python tools/check_staged_runtime_handshake.py`: PASS with `3` valid
  fixtures and `28` invalid fixtures.
- `python tools/check_staged_runtime_handshake.py --records tests\staged-runtime-handshake\valid\retained-a9-science-source.json`: PASS.
- `python tools/run_staged_current_skill_smoke.py --self-test`: PASS, including
  route-target normalization and Stage 03 model-scope record validation.
- `python tools/compare_staged_runtime_replay.py --self-test`: PASS.

One Stage-03-scoped current-skill retry was then run:

```text
python tools/run_staged_current_skill_smoke.py --case-name staged-a9-science-source-stage03-retry --raw-input-path tests\retained-proof-corpus\v0.4.3.0-schema-light\valid\sidecar-backed\cases\a9-science-source\input.txt --replay-record tests\staged-runtime-handshake\valid\retained-a9-science-source.json --run-dir .daee\staged-current-skill-smokes\20260602-a9-science-source-stage03-retry-v1 --model gpt-5.5 --stop-after-stage stage-03-routing-owner-gate
```

Result:

```text
STAGED_CURRENT_SKILL_STAGE_LOCAL_PASS: stopped after stage-03-routing-owner-gate
```

The resulting handoff record validates:

```text
python tools/check_staged_runtime_handshake.py --records .daee\staged-current-skill-smokes\20260602-a9-science-source-stage03-retry-v1\records\staged-handoff-stage-local-record.json
```

Important artifact references:

- `.daee/staged-current-skill-smokes/20260602-a9-science-source-stage03-retry-v1/records/staged-handoff-stage-local-record.json`
- handoff record SHA256:
  `73AF40ADD169AAECCF1AFAD752E136BAD1308240BBEB24014B165A315F378F27`
- `.daee/staged-current-skill-smokes/20260602-a9-science-source-stage03-retry-v1/staged-smoke.hashes.json`
- generated runtime hash in the retry:
  `EE9F8B52133EFE881463952EFCBAB5FC078FC72AB90C267B3A1453F663E42862`

The retry stopped exactly after stage 03. It produced no final `output.md`, no
proof sidecars, no retained-corpus promotion, and no package/release/provenance
evidence. It is one-case Stage 03 handoff evidence only, not broad model
behavior and not A/B/C/D closure.

Live source-boundary evidence:

- Source commit `1cffc7610b7f3a6e9370ef9b72e859d88a4800c7`.
- GitHub CI run `26801891869`: PASS.
- GitHub Pages run `26801891431`: PASS.

## Claims

This lane may claim:

- a staged current-skill harness prototype exists;
- the harness has a deterministic no-model self-test;
- one focused staged model run was attempted;
- the run localized a mismatch at stage 03;
- the Stage 03 route-target shape mismatch is repaired for one scoped retry;
- model-scoped Stage 03 records can preserve richer route metadata without
  weakening the canonical `route_targets` burden-id list;
- failed `.daee` artifacts were not promoted or hand-edited.

## Stage 04 Repair Follow-Up

The follow-up Stage 04 repair tightens the ACT/body-ref handoff without opening
Stage 05+ or composing a final governed output:

- `act_targets`, `act_burdens`, `act_body_refs`, and `act_rows` are canonical
  string lists.
- Every ACT row must begin with `⟦ACT`, contain `body_ref=`, `Δ=`, and `Land(`,
  and close with `⟧`.
- ACT row body refs must agree with `act_body_refs`.
- Optional `act_row_details` may carry richer metadata, but must agree with the
  canonical burden/body/row fields.
- Object-shaped `act_rows` are normalized only when each object contains an
  explicit string `act_row`; objects without `act_row` are rejected.
- Stage 04 ACT owners, when detectable from rows/details, must be backed by
  Stage 03 `owner_routes`.
- The replay comparator can now compare only a stage prefix with
  `--through-stage`; Stage 03 owner routes compare by route identity so richer
  model metadata does not cause a false Stage 04 mismatch.

New no-model contract evidence:

- `python tools/check_staged_runtime_handshake.py`: PASS with `4` valid
  fixtures and `35` invalid fixtures.
- `python tools/check_staged_runtime_handshake.py --records tests\staged-runtime-handshake\valid\retained-a9-science-source.json`: PASS.
- `python tools/run_staged_current_skill_smoke.py --self-test`: PASS, including
  Stage 04 object-row normalization, object-without-`act_row` rejection, and
  Stage 04 model-scope record validation.
- `python tools/compare_staged_runtime_replay.py --self-test`: PASS, including
  Stage 04 prefix comparison and route-identity owner-route comparison.

One Stage-04-scoped current-skill retry was then run:

```text
python tools/run_staged_current_skill_smoke.py --case-name staged-a9-science-source-stage04-retry --raw-input-path tests\retained-proof-corpus\v0.4.3.0-schema-light\valid\sidecar-backed\cases\a9-science-source\input.txt --replay-record tests\staged-runtime-handshake\valid\retained-a9-science-source.json --run-dir .daee\staged-current-skill-smokes\20260602-a9-science-source-stage04-retry-v1 --model gpt-5.5 --stop-after-stage stage-04-burden-execution-act
```

Result:

```text
STAGED_CURRENT_SKILL_STAGE_LOCAL_PASS: stopped after stage-04-burden-execution-act
```

Important artifact references:

- `.daee/staged-current-skill-smokes/20260602-a9-science-source-stage04-retry-v1/records/staged-handoff-stage-local-record.json`
- handoff record SHA256:
  `4D925B6ECB210B3D24D3A0EDA1AF3B529D10D6DF5D53BE73ED1C7FFC1DF381B8`
- `.daee/staged-current-skill-smokes/20260602-a9-science-source-stage04-retry-v1/staged-smoke.hashes.json`
- hash record SHA256:
  `5889A53172605F115702C9C6A09D32AC29341A75A808E3DFF31C98B1E23BB017`
- `.daee/staged-current-skill-smokes/20260602-a9-science-source-stage04-retry-v1/replay-comparison-stage04.md`
- replay comparison SHA256:
  `98815FF44B74C219C2E1856D44C64FC531D2872D9A90717EC71B24FFCA077AC1`

The Stage 04 replay comparison passed with zero mismatches through
`stage-04-burden-execution-act`. The retry stopped exactly after Stage 04. It
produced no final `output.md`, no proof sidecars, no retained-corpus promotion,
and no package/release/provenance evidence. It is one-case Stage 04 handoff
evidence only, not broad model behavior and not A/B/C/D closure.

Live source-boundary evidence:

- Source commit `80d68414bdd6d42ef4851f4c0144f0ce45aa0d0f`.
- GitHub CI run `26804061169`: PASS.
- GitHub Pages run `26804060558`: PASS.

## Stage 05 Repair Follow-Up

The follow-up Stage 05 repair tightens the MRP/reread/terminal-state handoff
without composing a final governed output or opening Stage 06+:

- `terminal_states` must be a non-empty burden-id object and must account for
  every Stage 04 ACT burden.
- terminal-state values are controlled: `landed`, `held`,
  `held-with-reason`, `hold_partial`, `partial`, `carried-PARTIAL`, or
  `discharged-as-derivative`.
- optional `generated_burdens` must be accounted in `terminal_states`.
- `dependency_graph_edges` must be a list, and every edge endpoint must appear
  in the known routed/ACT/generated burden set.
- `no_new_resultant_proof` is required. Boolean `true` is valid only when no
  unresolved burden remains; object form requires `proved`, non-empty `basis`
  when proved, and empty `unresolved_burdens` when `proved=true`.
- optional `reread_state` uses controlled state/route tokens and cannot claim
  `STOP` while unresolved burdens remain.
- Stage 05 cannot emit final/release/verifier fields such as `release_output`,
  `field_witness`, `closure_claim`, `verifier_sidecars`, Grapher, or collapse
  certificate evidence.
- The replay comparator now supports a Stage 05 prefix and detects
  terminal-state mismatches.

New no-model contract evidence:

- `python tools/check_staged_runtime_handshake.py`: PASS with `4` valid
  fixtures and `44` invalid fixtures.
- `python tools/check_staged_runtime_handshake.py --records tests\staged-runtime-handshake\valid\retained-a9-science-source.json`: PASS.
- `python tools/run_staged_current_skill_smoke.py --self-test`: PASS, including
  Stage 05 model-scope record validation, generated-burden terminal coverage
  rejection, and no-new-resultant/unresolved-burden rejection.
- `python tools/compare_staged_runtime_replay.py --self-test`: PASS, including
  Stage 05 prefix comparison and terminal-state mismatch detection.

One Stage-05-scoped current-skill retry was then run:

```text
python tools/run_staged_current_skill_smoke.py --case-name staged-a9-science-source-stage05-retry --raw-input-path tests\retained-proof-corpus\v0.4.3.0-schema-light\valid\sidecar-backed\cases\a9-science-source\input.txt --replay-record tests\staged-runtime-handshake\valid\retained-a9-science-source.json --run-dir .daee\staged-current-skill-smokes\20260602-a9-science-source-stage05-retry-v1 --model gpt-5.5 --stop-after-stage stage-05-mrp-reread-terminal-state
```

Result:

```text
STAGED_CURRENT_SKILL_STAGE_LOCAL_PASS: stopped after stage-05-mrp-reread-terminal-state
```

Important artifact references:

- `.daee/staged-current-skill-smokes/20260602-a9-science-source-stage05-retry-v1/records/staged-handoff-stage-local-record.json`
- handoff record SHA256:
  `A311B64DDEC05CBE0A2243AA91E4F1DDFC7F86DA324631036E29356781C4DF95`
- `.daee/staged-current-skill-smokes/20260602-a9-science-source-stage05-retry-v1/staged-smoke.hashes.json`
- hash record SHA256:
  `28A4EE450CE0B42B44F2FCF5A8470F34BEC2F1B0315BCF4D71D4F18E6E717C77`
- `.daee/staged-current-skill-smokes/20260602-a9-science-source-stage05-retry-v1/replay-comparison-stage05.md`
- replay comparison SHA256:
  `073244E309BD55629F4F742AECF9C0D03A282D04641D5D23A2D917EFDEA4AA97`
- generated runtime hash in the retry:
  `EE9F8B52133EFE881463952EFCBAB5FC078FC72AB90C267B3A1453F663E42862`

The Stage 05 replay comparison passed with zero mismatches through
`stage-05-mrp-reread-terminal-state`. The retry stopped exactly after Stage
05. It produced no final `output.md`, no field witness/NAR stage, no verifier
sidecars, no retained-corpus promotion, and no package/release/provenance
evidence. It is one-case Stage 05 handoff evidence only, not broad model
behavior and not A/B/C/D closure.

Live source-boundary evidence:

- Source commit `10be2366fe8be58b59cfed8a1670c4da5b9b18a7`.
- GitHub CI run `26806293349`: PASS.
- GitHub Pages run `26806292856`: PASS.

## Stage 06 Repair Follow-Up

The follow-up Stage 06 repair tightens the field_witness/NAR handoff without
composing a final governed output or opening Stage 07+:

- `field_witness_body_refs` must be a non-empty string list matching Stage 04
  `act_body_refs`.
- `nar_burdens` must include Stage 04 ACT burdens and Stage 05 terminal-state
  burdens.
- `owner_activations` may be body-ref strings or object rows with explicit
  `body_ref`; object rows are normalized while preserved under
  `owner_activation_details`.
- model-scope Stage 06 records must carry structured NAR evidence, either in
  `normalized_activation_record` or `normalized_activation_record_details`.
- `register_deltas` must be parser-stable object/list evidence.
- Stage 06 cannot emit final/release/verifier fields such as `release_output`,
  `closure_claim`, `final_output`, Grapher, collapse-certificate, sidecar, or
  Closing Formulation evidence.
- The replay comparator now supports Stage 06 prefix comparison, equivalent
  structured/legacy no-new-resultant proof shapes, visible-output
  owner/source/target detail aliases, structured NAR fallback, and directional
  tolerance for fresh candidate register deltas when the historical retained
  replay target has no register-delta evidence.

New no-model contract evidence:

- `python tools/check_staged_runtime_handshake.py`: PASS with `5` valid
  fixtures and `56` invalid fixtures.
- `python tools/check_staged_runtime_handshake.py --records tests\staged-runtime-handshake\valid\retained-a9-science-source.json`:
  PASS.
- `python tools/build_staged_runtime_replay_record.py --case-id a9-science-source --check`:
  PASS.
- `python tools/run_staged_current_skill_smoke.py --self-test`: PASS.
- `python tools/compare_staged_runtime_replay.py --self-test`: PASS.

One Stage-06-scoped current-skill retry was then run:

```text
python tools/run_staged_current_skill_smoke.py --case-name staged-a9-science-source-stage06-retry --raw-input-path tests\retained-proof-corpus\v0.4.3.0-schema-light\valid\sidecar-backed\cases\a9-science-source\input.txt --replay-record tests\staged-runtime-handshake\valid\retained-a9-science-source.json --run-dir .daee\staged-current-skill-smokes\20260602-a9-science-source-stage06-retry-v1 --model gpt-5.5 --stop-after-stage stage-06-field-witness-nar
```

Result:

```text
STAGED_CURRENT_SKILL_STAGE_LOCAL_PASS: stopped after stage-06-field-witness-nar
```

Important artifact references:

- `.daee/staged-current-skill-smokes/20260602-a9-science-source-stage06-retry-v1/records/staged-handoff-stage-local-record.json`
- handoff record SHA256:
  `CD02137A432FF1D32D86F86885FE43F2D6AD8C44C90E28CA5D7808F05073482E`
- `.daee/staged-current-skill-smokes/20260602-a9-science-source-stage06-retry-v1/staged-smoke.hashes.json`
- hash record SHA256:
  `9D57A49306F935F81A9F9DF95C885F8D940E38ACBF52D490AA7DF61E22E434DF`
- `.daee/staged-current-skill-smokes/20260602-a9-science-source-stage06-retry-v1/replay-comparison-stage06.md`
- replay comparison SHA256:
  `2AE7445BDC330364D133ACF053B35AA73B2DC90C232E6A4ABD72A268665FF793`
- generated runtime hash in the retry:
  `EE9F8B52133EFE881463952EFCBAB5FC078FC72AB90C267B3A1453F663E42862`

The Stage 06 replay comparison passed with zero mismatches through
`stage-06-field-witness-nar`. The retry stopped exactly after Stage 06. It
produced no final `output.md`, no release output, no proof sidecars, no
retained-corpus promotion, and no package/release/provenance evidence. It is
one-case Stage 06 field_witness/NAR handoff evidence only, not broad model
behavior and not A/B/C/D closure.

Source-boundary/live-CI evidence:

- Source commit `b85818ee5b0946ba6bc424e6c8b2a7e32d022226`.
- GitHub CI run `26809590027`: PASS.
- GitHub Pages run `26809587484`: PASS.

This lane does not claim:

- broad model behavior;
- broad A/B/C/D closure;
- runtime/default inline proof-mode emission for all outputs;
- arbitrary natural-language-to-IR parsing;
- guaranteed `T_lang` uptake;
- package/release/provenance readiness;
- broad proof-mode corpus adoption;
- Graphify or ActiveGraph proof.

## Next Narrow Repair

Stage 03, Stage 04, Stage 05, and Stage 06 have one-case staged handoff
coverage against the retained `a9-science-source` replay shape, and the
B.1/B.2 compiled-output lane has now repaired the live v3 Stage 06
`normalized_activation_record.n_frame` blocker. The Stage 06 repair keeps
canonical `n_frame` scalar, preserves selected/held detail under
`n_frame_details`, and is pushed/GitHub-audited at
`21616e7aa7af27eedf7a7efc72152c1ef1f64bf1` with CI run `26840561357` and
Pages run `26840556953` PASS. The next staged step is exactly one secularism
compiled pilot v4; that pilot has now been consumed and failed Stage 07 NLA
semantic-faithfulness validation. Do not rerun the whole staged system as a
broad smoke campaign, and do not treat these stage-local, no-model, or negative
pilot records as proof-output, sidecar, retained-corpus, release/provenance, or
broad A/B/C/D closure events.

## Stage 07 Release-Output Contract Follow-Up

The Stage 07 repair lane tightens the final governed `output.md` handoff while
preserving Stage 08 as a separate verifier-sidecar boundary:

- Stage-local records that stop before Stage 07 still carry
  `stage_scope.not_release_output=true`.
- Stage-local records that stop at Stage 07 instead carry
  `stage_scope.release_output=true` and `stage_scope.not_verifier_sidecars=true`.
- Stage 07 must name a `release_output` path/SHA pair whose file exists and
  matches its hash.
- The visible governed output must preserve the noetic-field opening/header,
  compact Layer A/Diagnostic IR surface, Layer B/ACT rows, `body_ref=`, `Land(`,
  MRP/reread/terminal-state surface, `field_witness`, NAR evidence,
  Restorative Response, and Closing Formulation.
- Stage 07 model-scope records require checker-owned `release_validation`
  evidence for visible output, NLA semantic faithfulness, field-witness
  convergence, formal reread-state semantics, graph completeness JSON, manual
  render contract, and owner activation ordering.
- Stage 07 cannot claim verifier sidecars, collapse certificate sidecars,
  Grapher sidecars, retained promotion, package/provenance, guaranteed uptake,
  Graphify proof, or ActiveGraph proof.
- The replay comparator permits a fresh Stage 07 candidate to carry
  checker-owned `release_validation` while comparing against a historical
  retained replay target that did not previously store that validator object.

The Stage 07 repair also patches a no-model harness normalization gap exposed
by the single authorized Stage-07-scoped retry: Stage 02 can now normalize
object-shaped `selected_n_frame`, `live_registers`, and `burden_floor` into the
canonical string/string-list handoff fields while preserving rich details under
detail fields. Stage 03 owner routes can likewise normalize a `target` plus
`required` owner sequence into canonical `burden_id`/`owner_id` route rows
while preserving the richer route evidence.

Local no-model contract evidence:

- `python tools/check_staged_runtime_handshake.py`: PASS with `5` valid
  fixtures and `56` invalid fixtures.
- `python tools/run_staged_current_skill_smoke.py --self-test`: PASS,
  including rich Stage 02 diagnostic normalization, rich Stage 03 owner-route
  normalization, Stage 07 validation requirements, and Stage 07 sidecar
  rejection.
- `python tools/compare_staged_runtime_replay.py --self-test`: PASS.
- `python tools/compare_staged_runtime_replay.py --record tests\staged-runtime-handshake\valid\retained-a9-science-source.json --replay-record tests\staged-runtime-handshake\valid\retained-a9-science-source.json --through-stage stage-07-release-output --out .daee\validation\retained-stage07-replay-comparison.md`:
  PASS.
- Full local CI-equivalent command set from `.github/workflows/ci.yml`: PASS.
  On Windows, the `python -m py_compile tools/*.py` line was rerun with
  PowerShell-expanded file paths because GitHub's bash shell expands the
  wildcard but PowerShell does not.

One Stage-07-scoped current-skill retry was run exactly once:

```text
python tools/run_staged_current_skill_smoke.py --case-name staged-a9-science-source-stage07-retry --raw-input-path tests\retained-proof-corpus\v0.4.3.0-schema-light\valid\sidecar-backed\cases\a9-science-source\input.txt --replay-record tests\staged-runtime-handshake\valid\retained-a9-science-source.json --run-dir .daee\staged-current-skill-smokes\20260602-a9-science-source-stage07-retry-v1 --model gpt-5.5 --stop-after-stage stage-07-release-output
```

Result:

```text
staged current-skill smoke: FAIL
failure: stage-03 route_targets must match stage-02 burden_floor
```

The failure happened before Stage 07 output generation. It exposed the Stage
02 rich-shape normalization gap described above; it did not produce a final
`output.md`, did not build Stage 08 sidecars, did not run a retained-corpus
promotion, and did not mutate retained outputs.

Negative artifact references:

- `.daee/staged-current-skill-smokes/20260602-a9-science-source-stage07-retry-v1/records/staged-handoff-failure.json`
- failure record SHA256:
  `EBA4621543055DAA69CE18CF0F5682A6CCF4A1D5B85443C5E0FBD36D136FD162`
- `.daee/staged-current-skill-smokes/20260602-a9-science-source-stage07-retry-v1/staged-smoke.hashes.json`
- hash record SHA256:
  `8A2000DF8A07C316F235FD60DE940A04FA3F9B55ABCB65297F674CBF2765C1A7`

This lane currently claims no final Stage 07 governed output. It claims only
the no-model Stage 07 release-output contract/checker/harness repair plus
truthful negative evidence from the single consumed Stage-07-scoped retry.

Source-boundary/live-CI evidence:

- Source commit `dc2940a793ecaf9cb30a0e0384ea514fc6783ab9`.
- GitHub CI run `26812346314`: PASS.
- GitHub Pages run `26812344544`: PASS.

Owner-authorized fresh Stage-07 retry after the normalization repair, 2026-06-02:

```text
python tools/run_staged_current_skill_smoke.py --case-name staged-a9-science-source-stage07-retry-v2 --raw-input-path tests\retained-proof-corpus\v0.4.3.0-schema-light\valid\sidecar-backed\cases\a9-science-source\input.txt --replay-record tests\staged-runtime-handshake\valid\retained-a9-science-source.json --run-dir .daee\staged-current-skill-smokes\20260602-a9-science-source-stage07-retry-v2 --model gpt-5.5 --stop-after-stage stage-07-release-output
```

Result:

```text
staged current-skill smoke: FAIL
failure: field_witness convergence check: FAIL
- visible ∇·B status does not match field_witness.coverage_proof.divergence_check
- visible ∇×κ status does not match field_witness.coverage_proof.curl_check
```

The retry reached final output composition but failed the Stage 07
release-output validation stack. It produced a governed `output.md`, but the
artifact is negative evidence, not a passing Stage 07 handoff.

Negative artifact references:

- `.daee/staged-current-skill-smokes/20260602-a9-science-source-stage07-retry-v2/output.md`
- output SHA256:
  `1EF5C3CDF8B967E8CF131EA91F0BB8945623B813FA8EE412386FA9E3D97D7A8A`
- `.daee/staged-current-skill-smokes/20260602-a9-science-source-stage07-retry-v2/records/staged-handoff-failure.json`
- failure record SHA256:
  `209CC0B9C69BD38A33A874464F52466059102AD794B0C4C7EFA09B16272F3F78`
- `.daee/staged-current-skill-smokes/20260602-a9-science-source-stage07-retry-v2/staged-smoke.hashes.json`
- hash record SHA256:
  `247B091B067875EFCA767A4913713FFB81983EF062A2F05E529C2038CA7C172B`

Direct validator classification for the v2 output:

- `python tools/check_nla_decode_semantic_faithfulness.py --outputs ...\output.md`: PASS.
- `python tools/check_formal_reread_state_semantics.py --outputs ...\output.md`: PASS.
- `python tools/check_owner_activation_ordering.py --outputs ...\output.md`: PASS.
- `python tools/check_field_witness_convergence.py --outputs ...\output.md`: FAIL on the visible `∇·B`/`∇×κ` versus
  `field_witness.coverage_proof.divergence_check`/`curl_check` mismatch.
- `python tools/check_manual_smoke_render_contract.py --outputs ...\output.md`: FAIL on the same mismatch.
- `python tools/check_graph_completeness.py --outputs ...\output.md --json`: FAIL with
  `mrp_exhaustion`, `curl_loopbreak_handling`, `witness_agreement`, and
  `model_claim_consistency`.

Replay boundary:

- `python tools/compare_staged_runtime_replay.py --record .daee\staged-current-skill-smokes\20260602-a9-science-source-stage07-retry-v2\records\staged-handoff-failure.json --replay-record tests\staged-runtime-handshake\valid\retained-a9-science-source.json --through-stage stage-06-field-witness-nar --out .daee\staged-current-skill-smokes\20260602-a9-science-source-stage07-retry-v2\replay-comparison-stage06.md`: PASS.
- No passing `staged-handoff-stage-local-record.json` was produced, so a Stage
  07 replay comparison cannot honestly be claimed for v2.

Boundary:

- The owner-authorized fresh Stage-07 retry was consumed.
- No Stage 08 verifier sidecars were built.
- No retained output was mutated.
- No retained case or failed `.daee` artifact was promoted.
- No package, tag, upload, GitHub Release, release asset, or provenance lane
  was opened.
- No D.8 cleanup was opened.
- No broad model matrix was opened.
- No arbitrary NL-to-IR parser, guaranteed `T_lang` uptake, broad A/B/C/D
  closure, Graphify proof, or ActiveGraph proof was claimed.

Next narrow repair if opened later:

- Strengthen the Stage 07 release-output handoff so the final governed output
  includes `field_witness.coverage_proof.divergence_check` and
  `field_witness.coverage_proof.curl_check` values that agree with the visible
  field diagnostics and closure/reconstruction witness. Do not rerun another
  Stage-07 current-skill attempt until that narrow owner/source repair is
  selected.

## Stage 07 Field-Diagnostic Convergence Contract Follow-Up

The owner opened the narrow convergence repair identified by the v2 failure.
The no-model repair keeps Stage 07 as the release-output boundary, but adds a
checker-owned `release_field_diagnostics` object so a Stage 07 model-scope
record cannot pass by merely claiming that diagnostics match.

Implemented local slice:

- The Stage 07 prompt now requires the visible Closure/Reconstruction Witness
  `∇·B` and `∇×κ` statuses to agree with
  `field_witness.coverage_proof.divergence_check` and
  `field_witness.coverage_proof.curl_check`.
- `tools/run_staged_current_skill_smoke.py` computes
  `release_field_diagnostics` from the rendered output using the existing
  closure-witness and field-witness parsers.
- `tools/check_staged_runtime_handshake.py` validates the diagnostic object,
  allowed status vocabulary, `matches=true`, visible/coverage agreement, and
  recomputation from the referenced `release_output` text.
- `tools/compare_staged_runtime_replay.py` carries
  `release_field_diagnostics` for Stage 07 and permits a fresh candidate to
  have richer checker-owned diagnostics than historical replay only when the
  diagnostics themselves pass.
- Self-test canaries reject missing diagnostics and model-authored false-pass
  diagnostics.
- The same lane patched an upstream Stage 06 shape weakness exposed by the
  single fresh retry: Stage 06 now explicitly asks for `per_burden` as a JSON
  array/list of objects, and the harness normalizes a non-empty burden-keyed
  `per_burden` map into the canonical list shape before writing a record.

Smoke B local evidence:

- `python tools/check_staged_runtime_handshake.py`: PASS with `5` valid
  fixtures and `56` invalid fixtures.
- `python tools/check_staged_runtime_handshake.py --records tests\staged-runtime-handshake\valid\retained-a9-science-source.json`:
  PASS.
- `python tools/run_staged_current_skill_smoke.py --self-test`: PASS.
- `python tools/compare_staged_runtime_replay.py --self-test`: PASS.
- Retained proof corpus, B.5 full-IR projection sidecar self-test, NLA decode
  semantic faithfulness, IR integrity, field-witness convergence, formal
  reread-state semantics, graph completeness fixture JSON, collapse
  certificate schema, smoke-artifact samples, retained promotion self-test,
  docs freshness, and diff checks all PASS locally.
- Full local CI-equivalent command set from `.github/workflows/ci.yml`: PASS.
  On Windows, the `python -m py_compile tools/*.py` line was rerun with
  PowerShell-expanded file paths because GitHub's bash shell expands the
  wildcard and PowerShell does not.

One fresh Stage-07-scoped current-skill convergence retry was run exactly once:

```text
python tools/run_staged_current_skill_smoke.py --case-name staged-a9-science-source-stage07-convergence-retry --raw-input-path tests\retained-proof-corpus\v0.4.3.0-schema-light\valid\sidecar-backed\cases\a9-science-source\input.txt --replay-record tests\staged-runtime-handshake\valid\retained-a9-science-source.json --run-dir .daee\staged-current-skill-smokes\20260602-a9-science-source-stage07-convergence-retry-v1 --model gpt-5.5 --stop-after-stage stage-07-release-output
```

Result:

```text
staged current-skill smoke: FAIL
failure: stage-06 normalized_activation_record.per_burden must be a non-empty object list
```

The retry failed before Stage 07 output generation. It did not produce
`output.md`, did not run Stage 07 validators, and did not produce a Stage 07
replay comparison. The failure exposed the Stage 06 NAR shape weakness now
covered by the local prompt/normalization repair above.

Negative artifact references:

- `.daee/staged-current-skill-smokes/20260602-a9-science-source-stage07-convergence-retry-v1/records/staged-handoff-failure.json`
- failure record SHA256:
  `3C90C5B5B541518E990A3F7B04B6F3F950D0E2A636803C662C2C7DDE55864382`
- `.daee/staged-current-skill-smokes/20260602-a9-science-source-stage07-convergence-retry-v1/staged-smoke.hashes.json`
- hash record SHA256:
  `82DC43E4D7EE66377F7E5D39D97A8B1D66B4F46EC2893F8D12B5B2FB34E7EA4B`

Source-boundary/live-CI evidence:

- Source commit `2c42416faab6282186374f49e7a89d1969b21d24`.
- GitHub CI run `26817186249`: PASS.
- GitHub Pages run `26817185300`: PASS.

Boundary:

- The owner-authorized fresh Stage-07 convergence retry was consumed.
- No Stage 07 governed output passed.
- No Stage 08 verifier sidecars were built.
- No retained output was mutated.
- No retained case or failed `.daee` artifact was promoted.
- No package, tag, upload, GitHub Release, release asset, or provenance lane
  was opened.
- No D.8 cleanup was opened.
- No broad model matrix was opened.
- No arbitrary NL-to-IR parser, guaranteed `T_lang` uptake, broad A/B/C/D
  closure, Graphify proof, or ActiveGraph proof was claimed.

Next narrow action if owner-opened later:

- Do not rerun another Stage-07 current-skill attempt from the old source
  boundary. First commit/push the convergence-contract and Stage 06 NAR-shape
  repair, verify live CI/Pages, and only then decide whether a new fresh
  Stage-07 retry is worth authorizing.

## Stage 07 Proof-Bundle Retry / Stage 08 Retained Binding

The owner opened one fresh proof-bundle retry from source commit
`975570ce83713a350bd5be713a7d91a174ebdd57` after the Stage 06 NAR map/list
repair was live. The retry ran exactly once:

```text
python tools/run_staged_current_skill_smoke.py --case-name staged-a9-science-source-stage07-proofbundle-retry --raw-input-path tests\retained-proof-corpus\v0.4.3.0-schema-light\valid\sidecar-backed\cases\a9-science-source\input.txt --replay-record tests\staged-runtime-handshake\valid\retained-a9-science-source.json --run-dir .daee\staged-current-skill-smokes\20260602-a9-science-source-stage07-proofbundle-retry-v1 --model gpt-5.5 --stop-after-stage stage-07-release-output
```

Result:

```text
STAGED_CURRENT_SKILL_STAGE_LOCAL_PASS: stopped after stage-07-release-output
```

Stage 07 evidence:

- `output.md` SHA256:
  `0AEA1318E54C01B8CC3E4B0CC98BC80C03191F9E799831697544FA2BB713A2AA`.
- Handoff record:
  `.daee/staged-current-skill-smokes/20260602-a9-science-source-stage07-proofbundle-retry-v1/records/staged-handoff-stage-local-record.json`
  SHA256 `3B92E578F1C317EF3F31CC1500F9B2D17E8FB434913C3190128E1E568FA70E2B`.
- Direct Stage 07 validators PASSed: staged handshake, NLA semantic
  faithfulness, field-witness convergence, formal reread-state semantics,
  graph completeness JSON, manual render contract, and owner activation
  ordering.
- Replay comparison through `stage-07-release-output` PASSed with zero
  mismatches; comparison SHA256:
  `E915C8F10893A774A6C2C28F3E8B4D34AD534E6CF2C29FE0B7850359A426B217`.

Stage 08 evidence:

- Proof sidecars built under
  `.daee/staged-current-skill-smokes/20260602-a9-science-source-stage07-proofbundle-retry-v1/proof-sidecars/`.
- Collapse certificate schema PASSed.
- Strict wrapper proof-sidecar hash-record validation PASSed after adapting
  the builder hash record into the live `proof_sidecars` wrapper shape.
- B.5 full-IR projection sidecar build PASSed.

Retained binding:

- Case id: `staged-a9-science-source-proofbundle`.
- Rows: `B.1`, `B.2`, `B.3`, `B.4`, `B.5`.
- Retained manifest entry includes
  `b5_full_ir_projection_sidecar` with SHA256
  `07C64F07770746F210117F15FB17B16A7623C1DF460D57959C5C6E50235B63F9`.
- `python tools/check_retained_proof_corpus.py`: PASS with `2` valid
  manifests, `27` invalid manifests, and `14` coverage targets.

This is the first retained/proof-owned staged current-skill proof bundle for
one source-order case. It does not claim broad model behavior, broad A/B/C/D
closure, runtime/default inline proof-mode emission for all outputs, arbitrary
NL-to-IR parsing, guaranteed `T_lang` uptake, package/release/provenance
readiness, Graphify proof, or ActiveGraph proof.

## B.1/B.2 v3 Compiled Pilot Negative Evidence

After the B.1/B.2 Stage 04/Stage 05 handoff repair and real-v2
failed-prefix checker verification were pushed and GitHub-audited at
`1e47307ef6097a965fd217eeb2a539b7020ee645`, the owner-authorized single v3
secularism compiled pilot ran exactly once. It failed before Stage 07 at Stage
06:

```text
stage-06 normalized_activation_record.n_frame must be a non-empty string
```

The Stage 06 response emitted `normalized_activation_record.n_frame` as an
object with `selected` and `held` members. The checker contract requires a
non-empty string. This is negative pre-Stage07 model-compliance evidence only:
no final compiled `output.md`, Stage 08 sidecars, retained promotion,
release-regression matrix cases, broad row closure, package/release/provenance,
Graphify proof, or ActiveGraph proof is claimed. The next owner-gated repair,
if opened, is a no-model Stage 06 prompt/checker contract canary; no second v3
retry remains authorized in this lane.

## B.1/B.2 v4 Compiled Pilot Negative Stage 07 Evidence

After the Stage 06 `n_frame` repair was pushed and GitHub-audited at
`21616e7aa7af27eedf7a7efc72152c1ef1f64bf1`, the exactly-one authorized v4
secularism compiled pilot ran:

```text
python tools/run_staged_current_skill_smoke.py --case-name staged-secularism-proofbundle-pilot-v4 --raw-input-path .daee\staged-row-family-smokes\20260602-secularism-pilot-v4\raw-input.md --replay-record tests\staged-runtime-handshake\valid\retained-a9-science-source.json --run-dir .daee\staged-row-family-smokes\20260602-secularism-pilot-v4 --model gpt-5.5 --release-output-mode compiled --target-output-kb 70
```

The run reached Stage 07 and assembled final `output.md`, then failed the NLA
semantic-faithfulness validator. The failure boundary is
`B1_B2_PILOT_V4_NEGATIVE_STAGE07_VALIDATOR`.

Evidence:

- `output.md` SHA256 `B8C1CD2E2667D107BEE2BF62A13C23A512D634950855B182119B33FA9FB040A9`.
- `records/staged-handoff-failure.json` SHA256
  `B59AD074FF5339E1E37F14016F03DFF630B4E4BD4F4E222FDA547A4B2EAAF910`.
- `staged-smoke.hashes.json` SHA256
  `D8BC7CD236A725CEA2089FE2D632441F1EC86CEDE2EC22DD766797A4CCDB4EED`.
- `stage-07-output-assembly.manifest.json` SHA256
  `4A9A954438AF8DB44462A23A81F3E1F259240D48D38281FE7EEB2C4E7DD3FE10`.

Gemba found duplicate visible ACT body refs across sectioned ACT bodies and
checker-incomplete `field_witness` owner-activation mirrors. This is negative
Stage 07 evidence only: no Stage 08 sidecars, retained promotion,
release-regression matrix cases, broad row closure, package/release/provenance,
Graphify proof, or ActiveGraph proof is claimed. No second v4 retry is
authorized in this lane.
