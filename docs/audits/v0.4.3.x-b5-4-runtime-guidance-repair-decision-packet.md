# v0.4.3.x B.5.4 Runtime Guidance / Template / Harness Repair Decision Packet

Date: 2026-06-02

Status: `OWNER_ACCEPTED_CHECKER_OWNED_SIDECAR_LANE; SUPERSEDED_BY_B5_4_1_IMPLEMENTATION`

Canonical proof ledger:
`docs/audits/v0.4.3.0-implementaudit-orchestrator.md`.

Canonical row ledger:
`docs/audits/v0.4.3.0-open-work-ledger.md`.

This packet originally prepared the next protected B.5.4 choice after the 2026-06-02
fresh current-skill proof-mode attempt produced no promotable retained
artifact. The owner subsequently accepted a narrower checker-owned sidecar
implementation lane: derive proof-mode/full-IR sidecars from existing
validator-clean governed retained artifacts instead of rerunning a blind
proof-mode smoke or requiring inline runtime/default emission. This document is
therefore historical decision-prep plus boundary context; the current
implementation evidence lives in the B.5 roadmap, target status, open ledger,
and orchestrator.

Graphify remains a read-only navigation aid. ActiveGraph, if used, remains
ignored local sidecar metadata and is not proof.

## Verdict

`OWNER_ACCEPTED_CHECKER_OWNED_SIDECAR_LANE`

B.5.4 is not blocked on retained-target exactness or missing strict-shape
canaries. Those source boundaries are already implemented and live-CI recorded.
The current blocker is that the governed output path has not emitted one
proof-class closure artifact satisfying all of these surfaces together:

- public ACT / manual render / MRP notation;
- field-witness agreement;
- strict NLA semantic faithfulness for `canonical_ir_projection`,
  `decoded_ir`, `full_ir_decode`, `proof_mode`, and `emission_policy`;
- graph completeness and checker-owned collapse certificate sidecar creation;
- warning-clean certificate-backed Output Grapher reconstruction;
- retained proof-corpus promotion under `b5-4-retained-proof-mode-full-ir`.

The owner chose a bounded no-model checker-owned sidecar lane. That lane does
not run a model smoke, does not patch broad runtime/default emission, and does
not hand-edit governed output.

Accepted outcome:

- Add `tools/build_b5_full_ir_projection_sidecar.py`.
- Teach `tools/check_retained_proof_corpus.py` to accept B.5.4 adoption through
  manifest-referenced `b5_full_ir_projection_sidecar` evidence as well as the
  older embedded projection path.
- Promote one real retained sidecar for `a9-science-source` if the builder and
  validators pass.
- Preserve the boundary that the sidecar is checker-owned projection evidence,
  not arbitrary natural-language IR parsing and not default runtime emission.

## Current B.5.4 State

- B.5.1 validates optional `field_witness.canonical_ir_projection`.
- B.5.2 validates optional `canonical_ir_projection.decoded_ir`.
- B.5.3 validates optional proof-mode adoption and requires proof mode when
  `full_ir_decode` appears.
- B.5.4 adds retained proof-mode/full-IR target infrastructure and strict
  subset target semantics.
- B.5.5 validates optional `canonical_ir_projection.emission_policy`.
- Projection-bearing outputs must preserve the visible noetic-field banner and
  compact `Layer A` / Diagnostic IR opening header.
- Ordinary schema-light B.5 retained cases remain valid when not listed under
  the specialized proof-mode target.

Live evidence:

- B.5.4/B.5.5 scaffold commit
  `352768005448b4cca4cd39c1e4a2208413411cd8`; CI run `26775051405` PASS;
  Pages run `26775047293` PASS.
- Retained-target semantics fix commit `f8d69e4`.
- Runtime-guidance/status commit `95a3a5d`; CI run `26779258641` PASS; Pages
  run `26779257934` PASS.
- Strict projection-shape source commit
  `429a8a79680392435a264357fc9415ad53b29494`; CI run `26792000775` PASS;
  Pages run `26792000370` PASS.
- Strict-shape docs/live-boundary commit
  `d3a891594e685b4f17adedaaa86a3756af4eae66`; CI run `26792100735` PASS;
  Pages run `26792100240` PASS.
- Fresh current-skill no-promotion boundary commit
  `5628fdb9bac779dc1d8a21b1029b9e842293b784`; CI run `26793842182` PASS;
  Pages run `26793841805` PASS.
- Queue/status sync commit `624c14a8b5f859fa883f755fab286c043516dace`; CI run
  `26794162108` PASS; Pages run `26794161459` PASS.

## Why The Fresh Attempt Did Not Close B.5.4

The 2026-06-02 focused attempt used the direct file-retained current-runtime
harness because the installed user skill still failed the metadata-description
loader guard. Attempts v1-v3 were ignored local artifacts under:

`.daee/b5-4-proof-mode-smoke/20260602-current-skill-b5-4-proof-mode-v*/`

No attempt was promotable:

- v1 passed ACT syntax only among major proof surfaces.
- v2 passed field-witness convergence and NLA semantic faithfulness, but failed
  public ACT/manual/MRP/owner-order/graph/Grapher surfaces.
- v3 passed ACT syntax, owner activation ordering, and `T_lang`, but failed
  strict MRP, manual render, field-witness convergence, formal reread,
  mid-reread, NLA full-IR schema, graph completeness, and Output Grapher.

No smoke hash record, proof sidecar, retained case, or manifest mutation was
produced. Failed `.daee` outputs must not be hand-edited into retained proof.

## Candidate Repair Owners

The next repair lane should identify one actual owner before patching:

| Candidate owner | What it may fix | Risk |
|---|---|---|
| `atomics/skill/references/diagnostics/diagnostic-ir.md` | Exact machine-facing projection object shape and non-claims | More prose may still not force public ACT/MRP/graph surfaces |
| `atomics/skill/references/rubrics/diagnostic-render-contract.md` | Visible proof-mode closure template and opening/header preservation | Could bloat default output or confuse human-facing read with metadata |
| `atomics/skill/references/tactics/TTP-MRP-mid-reread-pressure.md` | MRP route, terminal no-new-resultant, and graph/field-witness agreement | Could overfit B.5.4 if phrased as global recursion policy |
| `tools/run_current_skill_smoke.ps1` and prompt wrapper conventions | Harness-side enforcement, proof-sidecar build ordering, and failure classification | Harness repair is not runtime proof and must not fake model compliance |
| `tools/build_retained_proof_sidecars.py` / graph checker surfaces | Sidecar failure diagnostics if a valid output is blocked by tooling | Must not weaken graph completeness or certificate rules |

## Owner Options

1. Accept a no-model runtime guidance/template/harness repair decision.

   The implementation lane must first Gemba-classify the owner/source among the
   candidates above. It may patch one narrow owner family, add canaries if a
   checker or harness false-pass/false-fail is found, regenerate generated
   runtime/docs through normal generators, and run local/CI checks. It must not
   run a model smoke unless the owner separately authorizes that smoke.

2. Authorize exactly one focused proof-mode current-skill smoke without prior
   no-model repair.

   This is higher risk. It requires the owner to name prompt family, harness
   boundary, validator stack, whether installed-skill metadata failure is
   acceptable, and whether B.5.5 `emission_policy` is required. Any validator
   failure stops without promotion.

3. Defer B.5.4.

   Keep B.5.4 as infrastructure-only and work another open row or protected
   decision. This is safe if no release/package/provenance or full-IR milestone
   currently needs a proof-mode retained governed artifact.

## Recommended Option

Choose option 1.

Reason: the latest failed attempts show multiple surfaces failing at once. A
bounded no-model repair lane can localize whether the root cause is runtime
guidance, visible template contract, MRP terminal accounting, harness
sidecar/failure ordering, or checker diagnostics. That is safer than another
blind smoke and more useful than deferring without a named owner/source.

## Required Smoke A If Option 1 Is Accepted

- `git status --short --branch`
- `python tools/check_nla_decode_semantic_faithfulness.py`
- `python tools/check_retained_proof_corpus.py`
- `python tools/check_graph_completeness.py --outputs tests/graph-completeness/valid/generated-burden-collapse-positive.md --json`
- inspect v1-v3 ignored artifacts only as negative evidence;
- inspect the owning candidate files before mutation.

## Required Smoke B If Option 1 Is Accepted

Run the smallest owner-relevant checks, and full local CI-equivalent if runtime,
checker, workflow, or generated docs/runtime behavior changes:

- `python tools/check_nla_decode_semantic_faithfulness.py`
- `python tools/check_retained_proof_corpus.py`
- `python tools/check_graph_completeness.py`
- `python tools/check_collapse_certificate_schema.py`
- `python tools/check_formal_reread_state_semantics.py`
- `python tools/check_compiled_runtime_freshness.py` if generated runtime
  guidance changes;
- `python tools/build_docs_index.py --check`
- `git diff --check`

## Stop Conditions

Stop and report instead of patching if:

- no single owner/source can be identified;
- the only possible advance is another model/current-skill smoke;
- the repair would require broad default hard-register emission;
- the repair would weaken strict NLA, graph completeness, collapse certificate,
  or Output Grapher validators;
- pressure appears to edit failed `.daee` outputs into proof;
- the lane crosses package, tag, upload, release/provenance, D.8 cleanup,
  broad model matrix, or A.13 follow-on boundaries.

## Non-Claims

This packet does not claim:

- B.5.4 completion;
- a retained proof-mode/full-IR artifact;
- full IR decode across arbitrary governed output;
- runtime/default hard-register emission;
- model-smoke proof;
- package/provenance proof;
- guaranteed `T_lang` uptake;
- Graphify or ActiveGraph proof;
- permission to close D.8 rows.
