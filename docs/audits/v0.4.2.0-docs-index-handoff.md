# docs/index Handoff Mirror

Status: CURRENT-CANDIDATE

This tracked note mirrors the ignored root `HANDOFF.md` state for GitHub visibility. Root
`HANDOFF.md` remains local/ignored by policy; it was not force-added.

## Mirror choice

The owner asked to push handoff content to GitHub somehow. This pass chose the tracked mirror
route: copy the current handoff state into this audit note and leave root `HANDOFF.md` ignored.

## Current Reference Library local experiment and remote rollback

- Current local task in progress: fixing Theory runtime notation controls so
  formal trace stays ordered, phase/color labels appear once as a thin left-rail
  legend, and the card bank renders as one flat runtime-ordered flow with compact
  runtime step breaks. Also tightening the Reference source-map rule so
  generated layer/role/source breakdowns are collapsed provenance, not the
  default reading surface.
- Latest owner correction: the Reference Library `Source map` section is the
  visible regression when it appears as a huge audit/statistics wall. The
  source-browser path must remain compact summary, filters, document list, and
  selected preview before any source-map material.
- Local Reference route correction: entering `#reference` now closes the
  `Source map / generated provenance` disclosure, renders the source browser,
  and scrolls the Reference tab to its source-browser path so stale scroll state
  cannot make generated provenance look like the default task.
- Local Source map hierarchy correction: opening Source map now shows compact
  provenance summary first and keeps layer, coarse family/type, fine-grained
  role/source, and raw generated source rows in nested closed disclosures. Long
  nested sections use local filtering, row-limited show-all controls, and normal
  page flow instead of local vertical scroll wrappers or one giant audit-table
  wall. Layer, coarse family/type, and fine-grained role/source breakdowns now
  preserve exact generated labels and render as compact key/value count lists,
  not tables and not `NAME` / `COUNT` header blocks. Fine-grained role/source
  remains auditor/provenance material with row limits and Show all. Raw
  generated source rows remain a real table only inside the deepest collapsed
  raw-source disclosure.
- Commit-readiness status: Reference Source map count-summary renderer is being
  finalized for commit/push. Reference Library default path remains
  source-browser first, `Source map / generated provenance` remains collapsed
  and secondary, no release/smoke artifacts were touched, no tag/release was
  performed, and changes are ready to commit/push if checks pass.
- Latest DRY/ACID/SSOT cleanup before push: `docs/index/DESIGN.md` is the
  detailed docs/index visual/design SSOT; `AGENTS.md` now keeps durable workflow
  gates and Reference invariants without duplicating the whole design spec.
- Latest Theory correction: the side legend must act as a narrow key, while the
  right-side Runtime notation controls remain the main reading path. The current
  local model uses six non-repeating runtime step breaks: Selection /
  signal-state, Runtime registers, Route and burden, Landing and diagnostics,
  Closure and restoration, and Public release boundary.
- Reference Library/source-browser fix is the current commit candidate for owner
  review and should be inspected at `http://127.0.0.1:8123/index.html#reference`
  after the final checks and push complete.
- Local server status: `python -m http.server 8123 --directory docs` is running
  on `127.0.0.1:8123` under process `20672`.
- Reference now uses one coherent source-browser workspace: compact
  source-derived totals, unified search/filter controls, coarse layer/family
  filters, a keyboard-accessible source list, and a selected generated snapshot
  preview as the default path. Full layer/family/fine-grained role/raw source
  maps remain generated and collapsed in provenance disclosure.
- Local validation passed: `python tools\build_docs_index.py`,
  `python tools\build_docs_index.py --check`,
  `python tools\check_docs_index_interactions.py`,
  `python tools\build_framework_pipeline.py`,
  `python tools\build_compiled_runtime.py`,
  `python tools\check_compiled_runtime_freshness.py`,
  `python tools\check_field_operator_architecture.py`, and `git diff --check`.
  A first freshness run was invalid because it ran concurrently with the rebuild;
  the sequential rerun passed.
- Browser QA passed on desktop and 390px narrow viewport, plus the latest
  in-app inspection: Reference no longer has a standalone summary panel or role
  chip wall, raw provenance is collapsed by default, generated breakdowns remain
  available in disclosure, and no page-wide horizontal overflow was observed.
- GitHub `main` was restored with a normal revert commit:
  `b276070 Revert "docs: refine docs index design hierarchy"`, reverting
  `28d7fc0 docs: refine docs index design hierarchy` and returning remote
  content to the `82b5733 docs: add docs index design refinement plan`
  configuration. No force push was used.
- No tag, release, smoke capture, or runtime semantic artifact was touched.
- Local `main` is being finalized into a normal commit on top of the restored
  `origin/main` lineage; do not force-push.

## Current P1 design-refinement implementation status

- The Phase B refinement plan was checkpointed and pushed before broad UI work:
  `82b5733 docs: add docs index design refinement plan` on `main`.
- P1 docs/index design refinement is implemented in source-owned docs/index files:
  component roles, Reference source-browser, Owners selected-detail workspace,
  Theory notation-first hierarchy, Architecture support disclosure, visual QA
  runbook, and checker hardening.
- Implementation evidence is tracked in
  `docs/audits/v0.4.2.0-docs-index-design-refinement-implementation.md`.
- Exact daee-epistemics notation was preserved. The checker now guards critical
  forms such as `𝓝`, `D₀`, `Ψᴺ`, `Ψᴵ`, `N∈𝓝`, `∇·T`, `∇×T`, `ⁿBᵢ[OPᵢ]`,
  `ΔⁿB{♥,ξ,Ω,σ,μ}/Δκ`, `LoopBreak(∇×T)`, `R(H, ΔⁿB{♥,ξ,Ω,σ,μ}, Δκ)`,
  `𝒞(Ψᴺ)`, `T_lang: Ψᴺ ⇢ Ψᴵ`, `N_fiṭrī ∧ ʿaql ṣarīḥ`, `fiṭrah`, and
  `ʿaql ṣarīḥ`.
- Latest source/build checks passed after implementation: docs/index build,
  docs/index freshness, docs/index interaction checker, framework pipeline
  build, compiled runtime build, compiled runtime freshness, field-operator
  architecture check, and `git diff --check`.
- Browser visual QA passed on desktop and narrow viewport for Architecture,
  Owners/TTP, Theory, and Reference Library. Reference is no longer table-first;
  Owners raw audit tables are collapsed; Theory notation context updates; and
  Architecture pipelines remain paired vertical columns on desktop and stacked on
  narrow.
- During validation, the compiled-runtime build initially found a stale
  zero-byte ignored generated file under `skill/`; the ignored generated runtime
  file was removed and the runtime rebuilt. No tracked runtime semantics changed.
- Remaining caveat: no pixel-perfect visual tests or screenshot helper were
  added. Visual QA is documented in `docs/index/VISUAL_QA.md` and checker
  coverage stays structural/source-bound by design.

## Current docs/index status

- `docs/index/DESIGN.md` is the docs/index-scoped visual design source. It owns visual
  tokens/rationale only, not runtime semantics.
- `docs/index/runtime-architecture.json` is the shared docs/index runtime architecture source
  for Architecture cards, paired Architecture pipelines, and Theory notation mappings.
- Generated `docs/index.html` and `docs/daee-epistemics-pipeline.html` are not canonical and
  must not be hand-edited directly.
- The generated page masthead sits above sticky tab navigation. The masthead scrolls away; tab
  navigation and the right-aligned `Download latest .skill` action remain sticky while scrolling.
- `System Boundaries` and `Current Bridge Note` live in the generated Boundaries tab after
  Reference Library, not inside Architecture.
- Architecture carousel status: selected-primary carousel, scaled side-card previews, generated
  stage-card content, card 3/4 two-column internal layouts, bounded example chip grids,
  side-by-side color/phase-grouped vertical Architecture pipelines with arrows, generated
  copyable formal algebraic trace, and no auto-rotation.
- Theory Deep Dive status: compact operator card bank on wide viewports, card-to-notation
  interaction restored, notation colors derive from Architecture stage colors, and Highlighted
  notation panel shows meaning/runtime role/source owners plus related highlighted notation.
  Full notation source map is hidden provenance metadata, not default-visible UI.
- Reference Library snapshots are generated from atomics source; Owner/TTP, Architecture trace,
  large runtime/control JS constants, and Reference Library parity checks are active.

## Design-discipline pass status

- Design-quality audit performed against actual files from `awesome-design-systems`, `Kami`, and
  `html-anything`, recorded in
  `docs/audits/v0.4.2.0-docs-index-design-quality-audit.md` with verdict
  `TARGETED DESIGN DISCIPLINE NEEDED`.
- `docs/index/DESIGN.md` now includes practical design-quality discipline for layout, dense
  notation, carousel, interaction, progressive disclosure, visual QA, and concrete Do/Do not
  rules to prevent generic gray/cramped output.
- `AGENTS.md` now requires future docs/index UI changes to run the DESIGN.md
  token/source-boundary pass, Nielsen-style heuristic pass, WCAG/accessibility pass,
  dense-interface pass, local browser visual regression pass, and a poka-yoke checker update
  for the exact fixed regression class.
- `tools/check_docs_index_interactions.py` has non-brittle design-quality guards for the
  audit/DESIGN.md rules, contextual provenance, selected-primary carousel structure,
  side-preview discipline markers, Theory card interaction, and dense-interface containment
  tokens.
- Local validation for the design-quality pass completed: docs/index build and check,
  docs/index interaction checker, framework pipeline build, compiled runtime build/freshness
  check, field-operator architecture check, and `git diff --check` all passed. Browser visual
  QA at `http://127.0.0.1:8123/index.html` passed for desktop Architecture carousel,
  side-by-side vertical Architecture pipeline, Theory notation/context panel, Reference Library,
  and narrow/mobile Architecture behavior.
- Remaining docs/index design caveat: no pixel-perfect visual tests were added; the checker
  intentionally guards durable structure and regression classes, not viewport-specific
  coordinates.

## Current release/smoke status

- Latest release-line result reported the v0.4.2.0 three-smoke gate passed: `CR-01` PASS,
  `CR-02` PASS, `CR-03` PASS.
- Raw smoke captures remain local/ignored diagnostics and must not be committed or uploaded.
- v0.4.2.0 evidence is bounded local package-bound three-smoke release-gate evidence only, not
  broad live-output proof.
- Package SHA remains
  `21B25FF08AD36E26A57BACEC785C635F49DEC06E84A6EE191F4F8A61870913A7`; package size `594097`
  bytes; entries `20`.
- Package path: `build\daee-epistemics-v0.4.2.0.skill`.
- Provenance path: `build\daee-epistemics-v0.4.2.0.provenance.json`.
- The existing `v0.4.2.0` GitHub Release was previously updated in place; this pass must not
  create/update a GitHub Release or tag.

## Current worktree caveats

- P1 docs/index design hierarchy work is implemented locally and ready for
  careful staging, commit, and push after raw-smoke tracking verification.
- Root `HANDOFF.md` itself is local/ignored and should remain unstaged unless the owner
  explicitly changes repo policy.
- No release assets, tags, GitHub Release body, package/provenance files, raw
  smoke captures, heuristics/noetic-profiles migration, or runtime semantics
  were changed in this docs/index implementation pass.
- `skill/**` is generated/ignored runtime output. Do not stage it.

## Deferred work

- Expanded 10-case/generalization suite is deferred to v0.4.3.0/v0.5.
- Heuristics/noetic-profiles catalogue migration is deferred to v0.4.3.0/v0.5.
- Remaining docs/index design-system maturation is P2 and does not block
  v0.4.2.0.

## Next action

1. Verify raw smoke captures are not tracked.
2. Stage only intended docs/index source/docs/checker/generated docs files, including this tracked handoff mirror and the implementation audit.
3. Commit and push `docs: refine docs index design hierarchy`.
