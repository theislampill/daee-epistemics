# A15 task-review package

Baseline and current HEAD: `6987c9ebf1de45af700b1fa74b1ed25ec0beeb7c` on `codex/v0.4.6.0-runtime-footprint-b10`.

The scoped implementation is uncommitted and every scoped file is an untracked addition relative to baseline. The complete added-file diff is the live content of:

- `schema/topology-capacity-spec.schema.json`
- `tools/topology_capacity_lib.py`
- `tools/generate_topology_capacity_cases.py`
- `tools/check_topology_capacity_properties.py`
- `tools/check_no_fixed_topology_floors.py`
- `tools/check_case_registry_taint.py`
- `tests/topology-capacity/**`
- `docs/topology-capacity-property-suite.md`

Requirements: Plans 02 through 11, 15, 17, 18, and 21 under `C:/Users/theis/Documents/Codex/2026-07-08/dae/outputs/Sol/`; Plan 21/latest owner decisions override stale prose. Global constraints: open-world arbitrary topology; burden/submove/pressure counts are independent dimensions; canary counts are capacity probes, never semantic floors; deterministic seed-derived neutral identities; no case/topic taint or answer banking; generated children re-enter the full pipeline; properties extracted independently from producer records; anti-padding must bind semantic body identity; negative records stop at earliest failed stage and omit downstream artifacts.

Fresh root evidence after the exact-high repair: `31/31` units; generator and property self-tests; `14` probes; `108` positive transformations; `108` matched sabotages; `113` total relation rows; the distinct `176/176` full matrix; no-floor; and the case-taint checker (`5` variants) all exit `0`. Formal register/recursion, state capsule, A07 MRP, witness binding/convergence, projection parity, and the shared handshake (`25` valid / `119` invalid) are also root-green. The lane remains intentionally partial until shared validation, package-faithful adapters, mutation/preflight/CI, and the serialized A14/A15 taint join are integrated.

Review both spec compliance and code quality. Do not edit. Treat passing tests and this package as claims; read every scoped source/schema/test/doc and probe only a concrete named risk.

## Post-repair residuals — REPAIRED; exact-xhigh re-review pending

The first repair passed 24 units, 14 probes/113 relation rows, and a distinct 176-case matrix, but native advisory plus root temp-only reproductions confirmed two Critical and two Important false passes:

- `generated-child` rewrites a diamond's dependency shape to `generated-chain` while the shallow oracle still returns `preserved`; the 108-positive probe suite reports zero sabotage rows;
- replacing `public-output.txt`, refreshing only the Stage08 map, and leaving Stage07's declared hash stale still returns `accepted`;
- field-corrupt Stage03/04 cycle depth/event/burden rows pass after derived bindings are refreshed because only cycle universes/references are joined;
- any truthy `derivative_proof`, including fabricated non-hash text, authorizes repeated normalized bodies.

The same exact-high worker resumed after rate-limit recovery and repaired all four classes red-first:

- generated-child preserves the base dependency shape and the independent oracle now compares dependency, generation, parent, projection, event, and lifecycle relations;
- every selected positive relation has a matched sabotage row and an explicit applicability row (`108` each);
- Stage07 binds its declared hash to exact public bytes and independently reconstructs those bytes; structurally rehashed state/witness/projection sidecars reject before Stage08 custody;
- Stage03/04/05 cycles are exact against checker-derived burden, origin, parent, depth, event, obligation, reread, lifecycle, and order relations;
- shared-operation authorization is a canonical source-bound hash relation rather than truthy text.

Fresh root focused tests for the four original false-pass families plus the canonical valid shared-operation neighbor pass `7/7`. Full root evidence is `31/31` units, `14` probes, `108` positives, `108` sabotages, `113` relation rows, and `176/176` matrix cases. JSON `20/20`, Python AST `6/6` including the read-only shared taint checker, whitespace/EOF, scoped-bytecode, and `git diff --check` are clean. HEAD/branch remain unchanged; `tools/check_case_registry_taint.py` remains byte-identical at `760e06d5d05d247c3d69af7df0c39c6c0b07316e0936f6693df94f33c9fcaac5`.

Do not accept this package solely from those green counts. Independently disposition every original and post-repair finding against current bytes. Bounded acceptance still requires an exact `gpt-5.6-sol/xhigh` review with no open Critical, Important, or Minor findings. Shared A14 taint integration and later package/runtime/preflight/CI wiring remain outside this bounded review.

## Exact-xhigh re-review — NEEDS FIXES

The same exact-xhigh reviewer completed a current-byte re-review after transport recovery. Seven original finding classes and the generated-child, Stage07/08 custody, and cycle-join residuals are repaired, but two bounded findings remain:

- **Critical — shared-operation/merge authority is still self-authored.** `tools/check_topology_capacity_properties.py:493-495,594-658` accepts any truthy Stage02 hyperedge `derivative_proof` and hashes a Stage03 shared-operation assertion without joining it to a canonical upstream split/merge decision. `tests/topology-capacity/test_topology_capacity.py:65-120` labels a locally added relation valid while carrying `fabricated-nonempty-legacy-proof`. The reviewer reproduced two distinct pressures/burdens with no covering upstream hyperedge reusing one normalized body through Stage08, and a merge with free-form `fabricated-non-hash-text` passing through Stage08. Repair must introduce a canonical upstream structured split/merge relation with independently recomputed hash and same-function/source-frame/restoration-vector evidence, then bind Stage03/04 shared-operation use to that exact decision/hash.
- **Important — permutation coverage is not the Plan 15 property claimed.** `tools/check_topology_capacity_properties.py:1274-1287,1313-1317,1387-1395` reverses generic arrays but does not permute independent burden/component declarations and their joined cycle/lifecycle/projection rows; its sabotage deletes a candidate and every pair is reported applicable. Repair must permute an actual independent burden/component relation with every join, report non-applicable shapes with a reason, and sabotage the burden join/order relation rather than candidate cardinality.

Verdict: **Noncompliant / Task quality: Needs fixes.** Keep A15 unaccepted and A14 shared mutation serialized until the same exact-high worker repairs these findings, root reruns focused and broad evidence, and the same exact-xhigh reviewer returns an Approved verdict.

## Third repair — ROOT VERIFIED; exact-xhigh re-review pending

The same exact-high worker closed both remaining findings red-first on the current uncommitted bytes:

- retained source data now partitions every pressure exactly once and carries ordered, independently hashed split/merge/shared-operation authority with all seven Plan03 axes;
- Stage03 references the exact upstream partition/shared decision IDs and hashes, and Stage04 ACT rows cite the exact authorization;
- free-form proof, missing/wrong source hash, unused authority, cross-decision substitution, local-only shared authority, distinct-pressure reuse without source authority, and ACT citation mismatch have pinned negatives plus a hand-frozen golden hash;
- permutation applicability is derived structurally from undirected pre-LoopBreak noetic edges plus generated-parent edges; only whole independent component blocks move, connected shapes report `fewer-than-two-independent-components`, and the sabotage fails at Stage04 with `cross-stage-component-order-mismatch`.

Fresh root evidence on the final hashes:

- focused source-authority/permutation probes: `8/8` PASS in `1.108s`;
- truthful suite-count probe: PASS in `27.491s`;
- complete A15 units: `38/38` PASS in `129.008s`;
- generator self-test and check-only PASS; manifest hash `8020b12f27bff60669722695332d3ff70411be9a9d627ec278e56798641913d0`;
- property self-test PASS with `14` probes and `103` executed relation rows; Stage04, Stage05, and Stage08 probe-set CLIs PASS;
- metamorphic CLI PASS with exactly `108` selected rows, `98` executed positives, `10` explicit not-applicable rows, `98` executed sabotages, and `103` total executed relation rows;
- distinct full matrix `176/176` PASS with `71,529,270` generated bytes and `3,830,836` peak traced bytes as telemetry only;
- no-fixed-floor and read-only case-taint checks PASS with zero findings and `5` taint variants;
- register formalism, formal reread (`8` valid / `14` invalid), state capsule (`68` embedded; `4/15` v1; `14/31` v2), A07 (`15` valid / `51` invalid), witness binding/convergence, projection parity, and handshake (`25` valid / `119` invalid) are root-green;
- JSON `20/20`, Python AST `6/6`, trailing-whitespace/EOF, scoped bytecode, and `git diff --check` are clean. `git diff --check` emitted only unrelated line-ending warnings.

Current primary hashes are `8012d15a...3e0eb` (schema), `afd33201...40c76` (library), `7a2495fe...dc779` (generator), `402bf418...5f54a` (property checker), `e072cb5d...5f4a9` (no-floor checker), `8b3fa459...71d27` (units), and `5caab100...e3c15` (doc). The shared taint checker remains byte-identical at `760e06d5d05d247c3d69af7df0c39c6c0b07316e0936f6693df94f33c9fcaac5`; HEAD/branch remain `6987c9ebf1de45af700b1fa74b1ed25ec0beeb7c` / `codex/v0.4.6.0-runtime-footprint-b10`.

Do not accept from green counts alone. The same exact `gpt-5.6-sol/xhigh` reviewer must read and adversarially probe the current bytes, explicitly disposition the prior Critical and Important findings plus all earlier repaired classes, and return no open Critical, Important, or Minor finding. Shared A14 taint integration and later package/runtime/preflight/CI wiring remain outside this bounded re-review.

## Exact-xhigh third-repair re-review — COMPLIANT / APPROVED

The same independent exact-xhigh reviewer read the current scoped bytes, source-traced every earlier finding, executed the focused authority/permutation suite and additional temp-only adversarial probes, reran the truthful metamorphic readback, and reran the distinct `176/176` full matrix.

It explicitly confirmed:

- canonical source authorization, independently recomputed hashes, exact Stage02/03/04 custody, and rejection of ordered-scope mutation, orphan/duplicate authority, source-rehashed invalid merge, missing/unused/cross-substituted authority, free-form proof, and ACT citation drift;
- structural component applicability, explicit `fewer-than-two-independent-components` for chain and mixed, real declaration-order changes for unknown-route and pre-emption, and the dedicated Stage04 `cross-stage-component-order-mismatch` sabotage;
- exact `108` selected / `98` executed positive / `10` not applicable / `98` sabotage / `103` executed relation-row accounting;
- all earlier generated-child, source-oracle, HOLD/PARTIAL, anti-padding, ordered-cycle, Stage07 reconstruction, Stage08 custody, full-matrix, neutral-ID, no-floor, and taint findings remain repaired.

Final verdict: **Compliant for the bounded A15 deterministic core. Task quality: Approved. No Critical, Important, or Minor findings.** This acceptance does not establish semantic correctness, shared A14 integration, package/runtime/preflight/CI integration, model behavior, terminal deterministic closure, or release readiness.
