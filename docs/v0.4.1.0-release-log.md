# v0.4.1.0 Release Log

## Release Scope

v0.4.1.0 is a cleanup/hardening release on the `0.4.0.0` operative contract
line. The release scope is governance, docs consolidation, formalism
operativity auditing, CI artifact-build automation, and corrected package
provenance.

## Included Changes

- Root `atomics/skill/SKILL.md` remains below the control-plane drift budget and
  points detailed banner/release/reread rules to the actual owner files.
- The noetic-field banner, algebraic-control, and explanation-faithfulness
  invariants remain source-owned and checker-enforced.
- Historical audit and release surfaces are consolidated into history files
  while preserving provenance.
- Level3 active execution language is deprecated; compatibility harness
  filenames remain until a separate coordinated harness rename.
- Branch-era pipeline labels are removed from active architecture language;
  compatibility checker wrapper remains for old command paths.
- Manual `release-skill.yml` builds artifacts from atomics, writes provenance,
  and does not publish releases or create tags.
- Generated `skill/**` is untracked and ignored; CI/local release builds now
  regenerate it from tracked atomics before packaging.

## Verification Plan

The final gate runs the structural checker suite, register-formalism checker,
compatibility wrapper, Python tool compilation, diff checks, local package build,
archive shape inspection, provenance generation, and stale-reference sweep.

## Published Corrected Artifact

Corrected published package metadata is recorded in `docs/release-artifacts.md`.
The public v0.4.1.0 release asset/provenance was replaced after generated
`skill/**` untracking and final formalism/NLA/release-claim corrections. The
asset is built from tracked atomics through ignored generated runtime; raw
atomics are not packaged.

| Field | Value |
| --- | --- |
| Intended asset | `daee-epistemics-v0.4.1.0.skill` |
| SHA256 | `532C47E100607627006C214FC2F43BD36F4C351A4803A489E023184601AEEE37` |
| Size | `584569` bytes |
| Entries | `20` |
| Package source commit | `676396e0edc04103d603c9a68ecbc6f3d4e30356` |
| Contract version | `0.4.0.0` |
| Archive root | `README.md`, `SKILL.md`, `build-manifest.json`, `compiled-module-map.json`, `references` |

## Non-Claims

- No hard schema/register migration.
- No activation-level interpretability claim from NLA-inspired language.
- No default decorative algebra, proof-by-symbol, or untargeted divergence/curl
  notation. Compact target-explicit `∇·` / `∇×` markers, including
  `del-dot` / `del-cross` ASCII aliases, remain allowed when control-bound.
- No universal semantic grading claim from local checks, fixtures, or release
  publication.
