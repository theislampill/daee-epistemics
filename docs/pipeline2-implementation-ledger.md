# Pipeline #2 Implementation Ledger

This ledger adjudicates the Pipeline #2 repo plan recovered from `docs/index.html`.
`IMPLEMENTED` means the item is now canonical as a derived/conditional runtime bridge, checker
guard, or source-aligned documentation. It does not mean the Diagnostic IR JSON schema has been
expanded with mandatory fields. Hard schema migration and v0.4.0.0 release marking remain
separate decisions.

| Pipeline #2 item | Source files touched | Runtime effect | Checker/fixture effect | Smoke implication | Status | Evidence |
|---|---|---|---|---|---|---|
| `𝓝` noetic-structure selection space | `nomenclature-normalization.md`; `diagnostic-ir.md`; `noetic-reading-checklist.md`; formalism spec; index | Governs design/read domain for possible noetic structures before a live frame is selected or held. | Pipeline #2 bridge checker requires symbol/fallback and source anchors. | Hard smokes must not family-lock from thin input; register stress can verify held selection. | IMPLEMENTED | Canon states `N_space` / `𝓝` is canonical bridge, not schema. |
| `D₀` surface discourse / signal | `nomenclature-normalization.md`; `diagnostic-ir.md`; `noetic-reading-checklist.md`; formalism spec; index | Names surface discourse before diagnostic reduction. | Bridge checker requires `D0 -> PsiN` chain. | Smoke input remains source-local; no detached source-stack compression. | IMPLEMENTED | `diagnostic-ir.md` maps `D0 -> PsiN -> IR`. |
| `Ψᴺ` encoded noetic signal-state | `nomenclature-normalization.md`; `diagnostic-ir.md`; `noetic-reading-checklist.md`; formalism spec; index | Names encoded noetic signal-state over current IR/case-state. | Bridge checker requires `PsiN` and `Ψᴺ` anchors. | Register stress may show encoded state changes held material or reread. | IMPLEMENTED | `diagnostic-ir.md` states `PsiN` produces selected/held frame and IR. |
| `N∈𝓝` runtime-selected noetic frame | `nomenclature-normalization.md`; `diagnostic-ir.md`; `noetic-reading-checklist.md`; formalism spec; index | Makes selected `N` a runtime/held choice, not assumed by label. | Bridge checker requires `N in N_space` fallback and no schema field. | Hard smokes must preserve source-status/noetic-frame non-equivalence. | IMPLEMENTED | Canon says thin inputs remain held or PARTIAL instead of family-locked. |
| `IR(N,m,τ,σ,♥,ξ,Ω,μ,κ)` derived bridge | `diagnostic-ir.md`; `nomenclature-normalization.md`; `framework-pipeline.yaml`; output/render docs; formalism spec; index | Registers are conditionally live analytic functions over existing IR; they affect owner eligibility, hold/release, collapse radius, reread, or restoration when live. | Bridge checker enforces derived bridge and absence of hard schema fields. | Register stress must show control effect if notation appears. | IMPLEMENTED | Canon names derived/conditional runtime bridge while preserving compact schema. |
| `♥` affective-discursive register / release-posture control | `diagnostic-ir.md`; `noetic-reading-checklist.md`; `output-release.md`; render contract; formalism spec; index | Governs hold, sequence, tone, directness, softness, closure posture when live. | Bridge checker requires release-behavior language. | Hard moral-protest smoke must preserve grief/identity/performance holds where live. | IMPLEMENTED | Output release says `♥` may alter hold, sequence, softness, directness, or closure posture. |
| `ξ` epistemic/warrant grammar | `diagnostic-ir.md`; `noetic-reading-checklist.md`; `recursive-state-transitions.md`; formalism spec; index | Tracks warrant/testimony/proof-method/authority/defeater grammar when it changes routing or reread. | Bridge checker requires `Sameξ` and warrant anchors. | Tawatur/scientism smoke must show correct first-/higher-order warrant handling. | IMPLEMENTED | Canon ties `ξ` to proof/warrant/testimony release and same-burden sameness. |
| `Ω` ontological/predication grammar | `diagnostic-ir.md`; `noetic-reading-checklist.md`; `recursive-state-transitions.md`; formalism spec; index | Tracks predication, modality, dependence, causality, and creator/creation grammar when live. | Bridge checker requires `SameΩ` and ontology anchors. | Predication smoke must show M9/V8 owner activation where live. | IMPLEMENTED | Canon ties `Ω` to predicate/category/modality/dependence release. |
| `μ` meta-noetic memetic carrier/stabilizer | `diagnostic-ir.md`; `noetic-reading-checklist.md`; `output-release.md`; formalism spec; index | Tracks carrier, compression, stabilizer, defensive move, reproduction, or mutation pattern when it changes existing controls. | Bridge checker requires anti-decorative `μ` control effect. | Naturalist/scientistic smoke should not turn `μ` into rhetoric. | IMPLEMENTED | Diagnostic IR forbids meta-noetic vocabulary without a control-surface consequence. |
| `κ` collapse radius / downstream dependency set | `diagnostic-ir.md`; `recursive-state-transitions.md`; `output-release.md`; formalism spec; index | Tracks downstream claims/routes to reread after a load-bearing node changes. | Bridge checker requires `κ` not TODO language and `Delta-kappa` anchors. | Hard canary must preserve visible closure audit and no premature closure. | IMPLEMENTED | Recursive state says `κ` is consumed by reread, not a generic TODO list. |
| `Δκ` reread input after burden landing | `diagnostic-ir.md`; `recursive-state-transitions.md`; `output-release.md`; `framework-pipeline.yaml`; formalism spec; index | Forces dependency-radius delta into `R(H,Delta)` before STOP/HOLD/PARTIAL/RECURSE. | Bridge checker requires `Δκ` / `Delta-kappa` and reread consumption. | Register stress should show reread affected by dependency-radius change if notation is claimed. | IMPLEMENTED | Framework pipeline post-render gate consumes `Delta-kappa` before closure. |
| `ⁿB`, `ⁿBᵢ`, `ΔⁿB`, `ⁿ⁺¹B` | `recursive-state-transitions.md`; render contract; output release; formalism spec; index | Separates burden-cycle, submove, local landed delta, and next burden license. | Existing recursion/render checks plus bridge checker guard delta/next-burden distinction. | Hard smokes must preserve hard-case depth floor and same-burden discipline. | IMPLEMENTED | Canon says `ΔⁿB` is local and `ⁿ⁺¹B` requires `R(H,Delta)` license. |
| `ⁿBᵢ[OP] : target -> operation -> result -> ΔⁿB{♥,ξ,Ω,σ,μ} / Δκ` | `recursive-state-transitions.md`; render contract; output release; framework pipeline; formalism spec; index | Makes each operative submove a local owner-backed state change, not a route label. | Bridge checker requires operator signature; existing render checks require target/operation/result. | Hard canary must show owner/TTP/source matrix pass and no route harness contamination. | IMPLEMENTED | Recursive state and framework pipeline include the operator signature. |
| `R(H,ΔⁿB{♥,ξ,Ω,σ,μ},Δκ)` | `recursive-state-transitions.md`; output release; framework pipeline; formalism spec; index | Expands reread to consume held material, register deltas, and dependency radius before decision. | Bridge checker requires expanded reread and STOP/HOLD/PARTIAL/RECURSE. | Hard smokes must preserve visible closure audit and P1/P7 closure requirement. | IMPLEMENTED | Canon maps `Land(ⁿB) -> R(...) -> STOP/HOLD/PARTIAL/RECURSE or ⁿ⁺¹B`. |
| `𝒞(Ψᴺ)` constrained noetic collapse / discursive resolution | `recursive-state-transitions.md`; output release; render contract; formalism spec; index | Names terminal resolution only after landed burden and reread decision. | Bridge checker requires terminal boundary and anti-symbol-theater rule. | Smoke must not expose unimplemented symbols as default decorative runtime. | IMPLEMENTED | Output release forbids terminal notation before closure/audit obligations are satisfied. |
| `N_fiṭrī ∧ ʿaql ṣarīḥ` restorative terminal-state formalism | `recursive-state-transitions.md`; output release; render contract; formalism spec; index | Names final restorative orientation after governing misread loses control. | Bridge checker requires terminal formalism anchors. | Hard smokes must preserve P1/P7 closure and restoration after live burdens are cleared/held/PARTIALed. | IMPLEMENTED | Recursive state defines terminal orientation and says it is not a shortcut around burden landing. |
| Same-burden / next-burden algebra `Sameτ ∧ Sameξ ∧ SameΩ ∧ Sameσ ∧ Sameκ` | `recursive-state-transitions.md`; render contract; formalism spec; index | Prevents facet splitting into fake recursion when tribunal, warrant, ontology, source-status, and collapse radius are same. | Bridge checker requires same-burden algebra; existing recursion checker guards traversal. | Hard smokes preserve no detached source-stack compression and no route/check contamination. | IMPLEMENTED | Recursive state has both compact and derived same-burden equations. |
| Shannon analogy boundary | formalism spec; index | Allows signal/encoding/noise/capacity/error-correction analogy only; forbids truth/meaning/warrant entropy claims. | Bridge checker requires boundary text in spec/index. | Register stress must not replace warrant with signal math. | IMPLEMENTED | Spec states signal analogy limits and anti-patterns. |
| Anti-symbol-theater guard | `diagnostic-ir.md`; render contract; output release; formalism spec; index | Notation may appear only when it changes routing, release, reread, owner selection, burden delta, or restoration. | Bridge checker enforces anti-symbol-theater anchors. | Targeted register smoke/check assertion shows notation affects runtime control or stays unprinted. | IMPLEMENTED | Render contract includes anti-symbol-theater rule. |
| Anti-schema-bloat guard | `diagnostic-ir.md`; `nomenclature-normalization.md`; render contract; formalism spec; `AGENTS.md`; checker | Preserves compact Diagnostic IR schema while accepting derived bridge semantics. | Bridge checker fails if hard schema fields appear prematurely. | Full suite must pass with v0.3.2.0 schema contract. | IMPLEMENTED | Canon says bridge does not add mandatory JSON/schema fields. |
| Hard mandatory schema fields for `heart`/`xi`/`Omega`/`mu`/`kappa` | `TODO.md`; `AGENTS.md`; formalism spec; checker | No runtime field migration in this slice; derived bridge maps to existing fields. | Bridge checker explicitly guards against accidental hard-field schema expansion. | Would require new positive/negative fixtures and hard smokes before release. | DEFERRED WITH BLOCKER | Blocker: breaks current Diagnostic IR schema and requires package/runtime version migration plus schema/checker/fixture/smoke migration. |
| v0.4.0.0 contract/release marker migration | `TODO.md`; release docs unchanged | Not claimed by this patch. | Existing frontmatter contract check remains v0.3.2.0 unless explicitly migrated. | Release canaries must be re-run and reviewed before readiness claim. | DEFERRED WITH BLOCKER | Blocker: requires explicit release/version migration decision and full smoke evidence after user review. |

## Verification Plan

- Build generated framework/runtime from atomics.
- Run the existing checker suite plus `tools/check_pipeline2_bridge.py`.
- Parse `docs/index.html` and visually sanity-check the restored interactive page.
- Preserve recovered v0.3.2.0 hard-canary behavior; do not claim v0.4.0.0 readiness until hard smokes are reviewed.
