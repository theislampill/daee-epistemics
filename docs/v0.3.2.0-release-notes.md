# v0.3.2.0 Release Notes

**Date:** 2026-05-10
**State:** published GitHub Release with same-version asset refresh for golden-depth Level 3
hard-case governance and Level 1/2 scriptless pressure-dimension governance lift.

v0.3.2.0 adds an additive Level 3 route-first execution path for
Codex-capable script runtimes.

## What Changed

- Same-version asset refresh: the v0.3.2.0 `.skill` asset was rebuilt from
  commit `a62bbdc534a7de958882d6cfd864db5831fe0f3f`, replacing the previous
  v0.3.2.0 package hash while preserving the `v0.3.2.0` tag.
- Level 3 hard-case execution now treats route labels as insufficient:
  pressure dimensions, source-operation gates, diagnostic-frame gates, and
  burden-local state replay require substantive owner-floor execution rather
  than checker-shaped Target / Operation / Result labels.
- The same pressure-execution rule was lifted into Level 1/2 scriptless
  governance without exposing raw Level 3 JSON or checker fields.
- Real Codex CLI Level 3 hard moral-protest / worship-worthiness /
  source-worldview canary evidence was retained locally and reviewed
  adversarially against the v0.3.1.0/Sonnet golden as installed-package Level 3
  evidence. Raw smoke artifacts are not committed or packaged.
- `--simulate-output` is structural verification only; it is not behavioral
  evidence and does not prove model execution or shrinkage recovery.
- Final spec-side hardening clarifies that compact/governed output does not mean
  thin output. Hard, compound, or deformed Level 1/2 cases should preserve
  recursive per-burden Layer A re-entry, distinct active TTP/operator submoves,
  burden-complete Layer B execution, and real `R(H,Delta)` state-transition
  judgments.
- Qurʾān/ḥadīth guidance now requires clean, operative presentation when quoted:
  revealed texts should name diagnostic/restorative mechanisms, not become
  citation padding or collapsed prose.
- Kalām terminology and source-status labels were normalized: `kalām` is
  controlled as Speculative Theology, not Rational Theology; Ashʿarī/Māturīdī
  labels remain varied speculative-theological families; and repository
  operative use of Islamic scholar/scholarship follows the Salafī/Atharī-aligned
  source-status convention.
- `AGENTS.md` now records durable release-cycle protocol so future updates check
  source/generation/package/release coherence, encoding hygiene, smoke evidence
  boundaries, explicit staging, and GitHub Release asset replacement after
  package rebakes.
- Post-release polish hardened closure-gate burden-label matching, aligned
  README/SKILL fallback wording, made the execution prompt require the exact
  `Owner-floor: <owner-id>` checker format, and excluded Python cache files from
  package archives.
- `/daee-epistemics [input]` is documented to use Level 3 by default in Codex
  when bundled scripts are available.
- Scriptless runtimes visibly fall back to Level 1/2 behavior.
- Level 3 extracts span-backed features, builds a deterministic route plan given
  those features, validates reconstruction and ontology licensing, generates an
  execution prompt, and checks post-output execution fidelity.
- Route plans now support `continuation_queue` so input-anchored downstream
  burdens can be released after `Land(B)` and `R(H,Delta)`.
- Fixture and stability tests were added for Level 3 routing, reconstruction,
  neighboring-TTP contrast, and repeated same-input stability.
- v15 owner adjudication resolved the proof denominator: support/index surfaces
  are not counted as operative owner gaps, and no release-blocking routing or
  loadform gap remained.
- The codon pilot was rejected/not needed; no owner packs or broad codon DSL
  were introduced.
- Nomenclature was normalized across noetic-state, aqidah-family, DSL/IR,
  Level 3, and TTP/owner/operator terms.
- Large smoke outputs, raw live transcripts, temporary owner lists, and
  developer live-run helper scripts were intentionally left out of the release
  commit.

## Honest Claim

daee-epistemics v0.3.2.0 includes additive Level 3 route-first execution for
Codex-capable script runtimes. Level 3 performs deterministic routing given
extracted features, validates route reconstruction, and checks post-output
execution fidelity.

Feature extraction still includes span-backed interpretive components.
Transformer execution remains probabilistic. Scriptless runtimes fall back to
Level 1/2 behavior.

The repo/spec side has been hardened against the Level 1/2 thinning regression,
but behavioral recovery is not claimed until a manual Claude Level 1/2 rerun
confirms it.

## Non-Claims

- No deterministic feature extraction claim.
- No guaranteed long-output claim.
- No guaranteed all-runtime Level 3 claim.
- No pure-Hermes parity claim.
- No claim that fixture-18-style capability ceilings are solved.
- No claim that every TTP in the catalogue has full executable Level 3 rules
  beyond the implemented pilot/covered scope.
- No claim that every noetic family is fully solved by live smoke.
- No operative canary-specific routing logic.
- No codon pilot, broad codon DSL, or owner-pack rollout.
- No claim that the Level 1/2 hard moral-protest behavioral regression is recovered
  without a fresh manual Claude rerun.

## Package Boundary

The current package artifact is recorded in `docs/release-artifacts.md`:

- `daee-epistemics-v0.3.2.0.skill`
- SHA256 `1C1C3E59E72366689926922BE2117FF477B66E6A35B53004210E55A19559AA10`
- 572004 bytes
- 45 entries

Binary skill archives are not committed to the repository. Current-package
smoke evidence still requires running smoke prompts against this package and
marking them with matching package provenance.
Claude Level 1/2 scriptless behavioral recovery remains a separate manual
rerun gate.
